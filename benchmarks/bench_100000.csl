§ block_0
  def Row0't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle0 (r : &Row0't) -> bool = true
  fn process0 (r : &Row0't, dt : f32) -> bool = true
  fn score0 (r : &Row0't) -> f32 = 0.0
§ block_1
  def Row1't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1 (r : &Row1't) -> bool = true
  fn process1 (r : &Row1't, dt : f32) -> bool = true
  fn score1 (r : &Row1't) -> f32 = 0.0
§ block_2
  def Row2't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle2 (r : &Row2't) -> bool = true
  fn process2 (r : &Row2't, dt : f32) -> bool = true
  fn score2 (r : &Row2't) -> f32 = 0.0
§ block_3
  def Row3't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle3 (r : &Row3't) -> bool = true
  fn process3 (r : &Row3't, dt : f32) -> bool = true
  fn score3 (r : &Row3't) -> f32 = 0.0
§ block_4
  def Row4't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle4 (r : &Row4't) -> bool = true
  fn process4 (r : &Row4't, dt : f32) -> bool = true
  fn score4 (r : &Row4't) -> f32 = 0.0
§ block_5
  def Row5't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle5 (r : &Row5't) -> bool = true
  fn process5 (r : &Row5't, dt : f32) -> bool = true
  fn score5 (r : &Row5't) -> f32 = 0.0
§ block_6
  def Row6't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle6 (r : &Row6't) -> bool = true
  fn process6 (r : &Row6't, dt : f32) -> bool = true
  fn score6 (r : &Row6't) -> f32 = 0.0
§ block_7
  def Row7't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle7 (r : &Row7't) -> bool = true
  fn process7 (r : &Row7't, dt : f32) -> bool = true
  fn score7 (r : &Row7't) -> f32 = 0.0
§ block_8
  def Row8't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle8 (r : &Row8't) -> bool = true
  fn process8 (r : &Row8't, dt : f32) -> bool = true
  fn score8 (r : &Row8't) -> f32 = 0.0
§ block_9
  def Row9't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle9 (r : &Row9't) -> bool = true
  fn process9 (r : &Row9't, dt : f32) -> bool = true
  fn score9 (r : &Row9't) -> f32 = 0.0
§ block_10
  def Row10't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle10 (r : &Row10't) -> bool = true
  fn process10 (r : &Row10't, dt : f32) -> bool = true
  fn score10 (r : &Row10't) -> f32 = 0.0
§ block_11
  def Row11't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle11 (r : &Row11't) -> bool = true
  fn process11 (r : &Row11't, dt : f32) -> bool = true
  fn score11 (r : &Row11't) -> f32 = 0.0
§ block_12
  def Row12't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle12 (r : &Row12't) -> bool = true
  fn process12 (r : &Row12't, dt : f32) -> bool = true
  fn score12 (r : &Row12't) -> f32 = 0.0
§ block_13
  def Row13't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle13 (r : &Row13't) -> bool = true
  fn process13 (r : &Row13't, dt : f32) -> bool = true
  fn score13 (r : &Row13't) -> f32 = 0.0
§ block_14
  def Row14't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle14 (r : &Row14't) -> bool = true
  fn process14 (r : &Row14't, dt : f32) -> bool = true
  fn score14 (r : &Row14't) -> f32 = 0.0
§ block_15
  def Row15't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle15 (r : &Row15't) -> bool = true
  fn process15 (r : &Row15't, dt : f32) -> bool = true
  fn score15 (r : &Row15't) -> f32 = 0.0
§ block_16
  def Row16't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle16 (r : &Row16't) -> bool = true
  fn process16 (r : &Row16't, dt : f32) -> bool = true
  fn score16 (r : &Row16't) -> f32 = 0.0
§ block_17
  def Row17't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle17 (r : &Row17't) -> bool = true
  fn process17 (r : &Row17't, dt : f32) -> bool = true
  fn score17 (r : &Row17't) -> f32 = 0.0
§ block_18
  def Row18't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle18 (r : &Row18't) -> bool = true
  fn process18 (r : &Row18't, dt : f32) -> bool = true
  fn score18 (r : &Row18't) -> f32 = 0.0
§ block_19
  def Row19't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle19 (r : &Row19't) -> bool = true
  fn process19 (r : &Row19't, dt : f32) -> bool = true
  fn score19 (r : &Row19't) -> f32 = 0.0
§ block_20
  def Row20't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle20 (r : &Row20't) -> bool = true
  fn process20 (r : &Row20't, dt : f32) -> bool = true
  fn score20 (r : &Row20't) -> f32 = 0.0
§ block_21
  def Row21't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle21 (r : &Row21't) -> bool = true
  fn process21 (r : &Row21't, dt : f32) -> bool = true
  fn score21 (r : &Row21't) -> f32 = 0.0
§ block_22
  def Row22't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle22 (r : &Row22't) -> bool = true
  fn process22 (r : &Row22't, dt : f32) -> bool = true
  fn score22 (r : &Row22't) -> f32 = 0.0
§ block_23
  def Row23't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle23 (r : &Row23't) -> bool = true
  fn process23 (r : &Row23't, dt : f32) -> bool = true
  fn score23 (r : &Row23't) -> f32 = 0.0
§ block_24
  def Row24't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle24 (r : &Row24't) -> bool = true
  fn process24 (r : &Row24't, dt : f32) -> bool = true
  fn score24 (r : &Row24't) -> f32 = 0.0
§ block_25
  def Row25't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle25 (r : &Row25't) -> bool = true
  fn process25 (r : &Row25't, dt : f32) -> bool = true
  fn score25 (r : &Row25't) -> f32 = 0.0
§ block_26
  def Row26't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle26 (r : &Row26't) -> bool = true
  fn process26 (r : &Row26't, dt : f32) -> bool = true
  fn score26 (r : &Row26't) -> f32 = 0.0
§ block_27
  def Row27't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle27 (r : &Row27't) -> bool = true
  fn process27 (r : &Row27't, dt : f32) -> bool = true
  fn score27 (r : &Row27't) -> f32 = 0.0
§ block_28
  def Row28't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle28 (r : &Row28't) -> bool = true
  fn process28 (r : &Row28't, dt : f32) -> bool = true
  fn score28 (r : &Row28't) -> f32 = 0.0
§ block_29
  def Row29't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle29 (r : &Row29't) -> bool = true
  fn process29 (r : &Row29't, dt : f32) -> bool = true
  fn score29 (r : &Row29't) -> f32 = 0.0
§ block_30
  def Row30't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle30 (r : &Row30't) -> bool = true
  fn process30 (r : &Row30't, dt : f32) -> bool = true
  fn score30 (r : &Row30't) -> f32 = 0.0
§ block_31
  def Row31't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle31 (r : &Row31't) -> bool = true
  fn process31 (r : &Row31't, dt : f32) -> bool = true
  fn score31 (r : &Row31't) -> f32 = 0.0
§ block_32
  def Row32't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle32 (r : &Row32't) -> bool = true
  fn process32 (r : &Row32't, dt : f32) -> bool = true
  fn score32 (r : &Row32't) -> f32 = 0.0
§ block_33
  def Row33't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle33 (r : &Row33't) -> bool = true
  fn process33 (r : &Row33't, dt : f32) -> bool = true
  fn score33 (r : &Row33't) -> f32 = 0.0
§ block_34
  def Row34't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle34 (r : &Row34't) -> bool = true
  fn process34 (r : &Row34't, dt : f32) -> bool = true
  fn score34 (r : &Row34't) -> f32 = 0.0
§ block_35
  def Row35't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle35 (r : &Row35't) -> bool = true
  fn process35 (r : &Row35't, dt : f32) -> bool = true
  fn score35 (r : &Row35't) -> f32 = 0.0
§ block_36
  def Row36't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle36 (r : &Row36't) -> bool = true
  fn process36 (r : &Row36't, dt : f32) -> bool = true
  fn score36 (r : &Row36't) -> f32 = 0.0
§ block_37
  def Row37't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle37 (r : &Row37't) -> bool = true
  fn process37 (r : &Row37't, dt : f32) -> bool = true
  fn score37 (r : &Row37't) -> f32 = 0.0
§ block_38
  def Row38't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle38 (r : &Row38't) -> bool = true
  fn process38 (r : &Row38't, dt : f32) -> bool = true
  fn score38 (r : &Row38't) -> f32 = 0.0
§ block_39
  def Row39't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle39 (r : &Row39't) -> bool = true
  fn process39 (r : &Row39't, dt : f32) -> bool = true
  fn score39 (r : &Row39't) -> f32 = 0.0
§ block_40
  def Row40't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle40 (r : &Row40't) -> bool = true
  fn process40 (r : &Row40't, dt : f32) -> bool = true
  fn score40 (r : &Row40't) -> f32 = 0.0
§ block_41
  def Row41't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle41 (r : &Row41't) -> bool = true
  fn process41 (r : &Row41't, dt : f32) -> bool = true
  fn score41 (r : &Row41't) -> f32 = 0.0
§ block_42
  def Row42't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle42 (r : &Row42't) -> bool = true
  fn process42 (r : &Row42't, dt : f32) -> bool = true
  fn score42 (r : &Row42't) -> f32 = 0.0
§ block_43
  def Row43't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle43 (r : &Row43't) -> bool = true
  fn process43 (r : &Row43't, dt : f32) -> bool = true
  fn score43 (r : &Row43't) -> f32 = 0.0
§ block_44
  def Row44't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle44 (r : &Row44't) -> bool = true
  fn process44 (r : &Row44't, dt : f32) -> bool = true
  fn score44 (r : &Row44't) -> f32 = 0.0
§ block_45
  def Row45't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle45 (r : &Row45't) -> bool = true
  fn process45 (r : &Row45't, dt : f32) -> bool = true
  fn score45 (r : &Row45't) -> f32 = 0.0
§ block_46
  def Row46't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle46 (r : &Row46't) -> bool = true
  fn process46 (r : &Row46't, dt : f32) -> bool = true
  fn score46 (r : &Row46't) -> f32 = 0.0
§ block_47
  def Row47't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle47 (r : &Row47't) -> bool = true
  fn process47 (r : &Row47't, dt : f32) -> bool = true
  fn score47 (r : &Row47't) -> f32 = 0.0
§ block_48
  def Row48't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle48 (r : &Row48't) -> bool = true
  fn process48 (r : &Row48't, dt : f32) -> bool = true
  fn score48 (r : &Row48't) -> f32 = 0.0
§ block_49
  def Row49't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle49 (r : &Row49't) -> bool = true
  fn process49 (r : &Row49't, dt : f32) -> bool = true
  fn score49 (r : &Row49't) -> f32 = 0.0
§ block_50
  def Row50't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle50 (r : &Row50't) -> bool = true
  fn process50 (r : &Row50't, dt : f32) -> bool = true
  fn score50 (r : &Row50't) -> f32 = 0.0
§ block_51
  def Row51't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle51 (r : &Row51't) -> bool = true
  fn process51 (r : &Row51't, dt : f32) -> bool = true
  fn score51 (r : &Row51't) -> f32 = 0.0
§ block_52
  def Row52't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle52 (r : &Row52't) -> bool = true
  fn process52 (r : &Row52't, dt : f32) -> bool = true
  fn score52 (r : &Row52't) -> f32 = 0.0
§ block_53
  def Row53't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle53 (r : &Row53't) -> bool = true
  fn process53 (r : &Row53't, dt : f32) -> bool = true
  fn score53 (r : &Row53't) -> f32 = 0.0
§ block_54
  def Row54't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle54 (r : &Row54't) -> bool = true
  fn process54 (r : &Row54't, dt : f32) -> bool = true
  fn score54 (r : &Row54't) -> f32 = 0.0
§ block_55
  def Row55't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle55 (r : &Row55't) -> bool = true
  fn process55 (r : &Row55't, dt : f32) -> bool = true
  fn score55 (r : &Row55't) -> f32 = 0.0
§ block_56
  def Row56't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle56 (r : &Row56't) -> bool = true
  fn process56 (r : &Row56't, dt : f32) -> bool = true
  fn score56 (r : &Row56't) -> f32 = 0.0
§ block_57
  def Row57't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle57 (r : &Row57't) -> bool = true
  fn process57 (r : &Row57't, dt : f32) -> bool = true
  fn score57 (r : &Row57't) -> f32 = 0.0
§ block_58
  def Row58't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle58 (r : &Row58't) -> bool = true
  fn process58 (r : &Row58't, dt : f32) -> bool = true
  fn score58 (r : &Row58't) -> f32 = 0.0
§ block_59
  def Row59't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle59 (r : &Row59't) -> bool = true
  fn process59 (r : &Row59't, dt : f32) -> bool = true
  fn score59 (r : &Row59't) -> f32 = 0.0
§ block_60
  def Row60't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle60 (r : &Row60't) -> bool = true
  fn process60 (r : &Row60't, dt : f32) -> bool = true
  fn score60 (r : &Row60't) -> f32 = 0.0
§ block_61
  def Row61't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle61 (r : &Row61't) -> bool = true
  fn process61 (r : &Row61't, dt : f32) -> bool = true
  fn score61 (r : &Row61't) -> f32 = 0.0
§ block_62
  def Row62't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle62 (r : &Row62't) -> bool = true
  fn process62 (r : &Row62't, dt : f32) -> bool = true
  fn score62 (r : &Row62't) -> f32 = 0.0
§ block_63
  def Row63't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle63 (r : &Row63't) -> bool = true
  fn process63 (r : &Row63't, dt : f32) -> bool = true
  fn score63 (r : &Row63't) -> f32 = 0.0
§ block_64
  def Row64't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle64 (r : &Row64't) -> bool = true
  fn process64 (r : &Row64't, dt : f32) -> bool = true
  fn score64 (r : &Row64't) -> f32 = 0.0
§ block_65
  def Row65't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle65 (r : &Row65't) -> bool = true
  fn process65 (r : &Row65't, dt : f32) -> bool = true
  fn score65 (r : &Row65't) -> f32 = 0.0
§ block_66
  def Row66't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle66 (r : &Row66't) -> bool = true
  fn process66 (r : &Row66't, dt : f32) -> bool = true
  fn score66 (r : &Row66't) -> f32 = 0.0
§ block_67
  def Row67't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle67 (r : &Row67't) -> bool = true
  fn process67 (r : &Row67't, dt : f32) -> bool = true
  fn score67 (r : &Row67't) -> f32 = 0.0
§ block_68
  def Row68't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle68 (r : &Row68't) -> bool = true
  fn process68 (r : &Row68't, dt : f32) -> bool = true
  fn score68 (r : &Row68't) -> f32 = 0.0
§ block_69
  def Row69't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle69 (r : &Row69't) -> bool = true
  fn process69 (r : &Row69't, dt : f32) -> bool = true
  fn score69 (r : &Row69't) -> f32 = 0.0
§ block_70
  def Row70't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle70 (r : &Row70't) -> bool = true
  fn process70 (r : &Row70't, dt : f32) -> bool = true
  fn score70 (r : &Row70't) -> f32 = 0.0
§ block_71
  def Row71't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle71 (r : &Row71't) -> bool = true
  fn process71 (r : &Row71't, dt : f32) -> bool = true
  fn score71 (r : &Row71't) -> f32 = 0.0
§ block_72
  def Row72't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle72 (r : &Row72't) -> bool = true
  fn process72 (r : &Row72't, dt : f32) -> bool = true
  fn score72 (r : &Row72't) -> f32 = 0.0
§ block_73
  def Row73't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle73 (r : &Row73't) -> bool = true
  fn process73 (r : &Row73't, dt : f32) -> bool = true
  fn score73 (r : &Row73't) -> f32 = 0.0
§ block_74
  def Row74't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle74 (r : &Row74't) -> bool = true
  fn process74 (r : &Row74't, dt : f32) -> bool = true
  fn score74 (r : &Row74't) -> f32 = 0.0
§ block_75
  def Row75't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle75 (r : &Row75't) -> bool = true
  fn process75 (r : &Row75't, dt : f32) -> bool = true
  fn score75 (r : &Row75't) -> f32 = 0.0
§ block_76
  def Row76't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle76 (r : &Row76't) -> bool = true
  fn process76 (r : &Row76't, dt : f32) -> bool = true
  fn score76 (r : &Row76't) -> f32 = 0.0
§ block_77
  def Row77't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle77 (r : &Row77't) -> bool = true
  fn process77 (r : &Row77't, dt : f32) -> bool = true
  fn score77 (r : &Row77't) -> f32 = 0.0
§ block_78
  def Row78't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle78 (r : &Row78't) -> bool = true
  fn process78 (r : &Row78't, dt : f32) -> bool = true
  fn score78 (r : &Row78't) -> f32 = 0.0
§ block_79
  def Row79't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle79 (r : &Row79't) -> bool = true
  fn process79 (r : &Row79't, dt : f32) -> bool = true
  fn score79 (r : &Row79't) -> f32 = 0.0
§ block_80
  def Row80't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle80 (r : &Row80't) -> bool = true
  fn process80 (r : &Row80't, dt : f32) -> bool = true
  fn score80 (r : &Row80't) -> f32 = 0.0
§ block_81
  def Row81't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle81 (r : &Row81't) -> bool = true
  fn process81 (r : &Row81't, dt : f32) -> bool = true
  fn score81 (r : &Row81't) -> f32 = 0.0
§ block_82
  def Row82't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle82 (r : &Row82't) -> bool = true
  fn process82 (r : &Row82't, dt : f32) -> bool = true
  fn score82 (r : &Row82't) -> f32 = 0.0
§ block_83
  def Row83't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle83 (r : &Row83't) -> bool = true
  fn process83 (r : &Row83't, dt : f32) -> bool = true
  fn score83 (r : &Row83't) -> f32 = 0.0
§ block_84
  def Row84't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle84 (r : &Row84't) -> bool = true
  fn process84 (r : &Row84't, dt : f32) -> bool = true
  fn score84 (r : &Row84't) -> f32 = 0.0
§ block_85
  def Row85't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle85 (r : &Row85't) -> bool = true
  fn process85 (r : &Row85't, dt : f32) -> bool = true
  fn score85 (r : &Row85't) -> f32 = 0.0
§ block_86
  def Row86't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle86 (r : &Row86't) -> bool = true
  fn process86 (r : &Row86't, dt : f32) -> bool = true
  fn score86 (r : &Row86't) -> f32 = 0.0
§ block_87
  def Row87't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle87 (r : &Row87't) -> bool = true
  fn process87 (r : &Row87't, dt : f32) -> bool = true
  fn score87 (r : &Row87't) -> f32 = 0.0
§ block_88
  def Row88't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle88 (r : &Row88't) -> bool = true
  fn process88 (r : &Row88't, dt : f32) -> bool = true
  fn score88 (r : &Row88't) -> f32 = 0.0
§ block_89
  def Row89't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle89 (r : &Row89't) -> bool = true
  fn process89 (r : &Row89't, dt : f32) -> bool = true
  fn score89 (r : &Row89't) -> f32 = 0.0
§ block_90
  def Row90't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle90 (r : &Row90't) -> bool = true
  fn process90 (r : &Row90't, dt : f32) -> bool = true
  fn score90 (r : &Row90't) -> f32 = 0.0
§ block_91
  def Row91't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle91 (r : &Row91't) -> bool = true
  fn process91 (r : &Row91't, dt : f32) -> bool = true
  fn score91 (r : &Row91't) -> f32 = 0.0
§ block_92
  def Row92't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle92 (r : &Row92't) -> bool = true
  fn process92 (r : &Row92't, dt : f32) -> bool = true
  fn score92 (r : &Row92't) -> f32 = 0.0
§ block_93
  def Row93't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle93 (r : &Row93't) -> bool = true
  fn process93 (r : &Row93't, dt : f32) -> bool = true
  fn score93 (r : &Row93't) -> f32 = 0.0
§ block_94
  def Row94't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle94 (r : &Row94't) -> bool = true
  fn process94 (r : &Row94't, dt : f32) -> bool = true
  fn score94 (r : &Row94't) -> f32 = 0.0
§ block_95
  def Row95't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle95 (r : &Row95't) -> bool = true
  fn process95 (r : &Row95't, dt : f32) -> bool = true
  fn score95 (r : &Row95't) -> f32 = 0.0
§ block_96
  def Row96't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle96 (r : &Row96't) -> bool = true
  fn process96 (r : &Row96't, dt : f32) -> bool = true
  fn score96 (r : &Row96't) -> f32 = 0.0
§ block_97
  def Row97't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle97 (r : &Row97't) -> bool = true
  fn process97 (r : &Row97't, dt : f32) -> bool = true
  fn score97 (r : &Row97't) -> f32 = 0.0
§ block_98
  def Row98't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle98 (r : &Row98't) -> bool = true
  fn process98 (r : &Row98't, dt : f32) -> bool = true
  fn score98 (r : &Row98't) -> f32 = 0.0
§ block_99
  def Row99't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle99 (r : &Row99't) -> bool = true
  fn process99 (r : &Row99't, dt : f32) -> bool = true
  fn score99 (r : &Row99't) -> f32 = 0.0
§ block_100
  def Row100't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle100 (r : &Row100't) -> bool = true
  fn process100 (r : &Row100't, dt : f32) -> bool = true
  fn score100 (r : &Row100't) -> f32 = 0.0
§ block_101
  def Row101't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle101 (r : &Row101't) -> bool = true
  fn process101 (r : &Row101't, dt : f32) -> bool = true
  fn score101 (r : &Row101't) -> f32 = 0.0
§ block_102
  def Row102't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle102 (r : &Row102't) -> bool = true
  fn process102 (r : &Row102't, dt : f32) -> bool = true
  fn score102 (r : &Row102't) -> f32 = 0.0
§ block_103
  def Row103't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle103 (r : &Row103't) -> bool = true
  fn process103 (r : &Row103't, dt : f32) -> bool = true
  fn score103 (r : &Row103't) -> f32 = 0.0
§ block_104
  def Row104't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle104 (r : &Row104't) -> bool = true
  fn process104 (r : &Row104't, dt : f32) -> bool = true
  fn score104 (r : &Row104't) -> f32 = 0.0
§ block_105
  def Row105't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle105 (r : &Row105't) -> bool = true
  fn process105 (r : &Row105't, dt : f32) -> bool = true
  fn score105 (r : &Row105't) -> f32 = 0.0
§ block_106
  def Row106't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle106 (r : &Row106't) -> bool = true
  fn process106 (r : &Row106't, dt : f32) -> bool = true
  fn score106 (r : &Row106't) -> f32 = 0.0
§ block_107
  def Row107't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle107 (r : &Row107't) -> bool = true
  fn process107 (r : &Row107't, dt : f32) -> bool = true
  fn score107 (r : &Row107't) -> f32 = 0.0
§ block_108
  def Row108't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle108 (r : &Row108't) -> bool = true
  fn process108 (r : &Row108't, dt : f32) -> bool = true
  fn score108 (r : &Row108't) -> f32 = 0.0
§ block_109
  def Row109't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle109 (r : &Row109't) -> bool = true
  fn process109 (r : &Row109't, dt : f32) -> bool = true
  fn score109 (r : &Row109't) -> f32 = 0.0
§ block_110
  def Row110't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle110 (r : &Row110't) -> bool = true
  fn process110 (r : &Row110't, dt : f32) -> bool = true
  fn score110 (r : &Row110't) -> f32 = 0.0
§ block_111
  def Row111't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle111 (r : &Row111't) -> bool = true
  fn process111 (r : &Row111't, dt : f32) -> bool = true
  fn score111 (r : &Row111't) -> f32 = 0.0
§ block_112
  def Row112't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle112 (r : &Row112't) -> bool = true
  fn process112 (r : &Row112't, dt : f32) -> bool = true
  fn score112 (r : &Row112't) -> f32 = 0.0
§ block_113
  def Row113't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle113 (r : &Row113't) -> bool = true
  fn process113 (r : &Row113't, dt : f32) -> bool = true
  fn score113 (r : &Row113't) -> f32 = 0.0
§ block_114
  def Row114't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle114 (r : &Row114't) -> bool = true
  fn process114 (r : &Row114't, dt : f32) -> bool = true
  fn score114 (r : &Row114't) -> f32 = 0.0
§ block_115
  def Row115't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle115 (r : &Row115't) -> bool = true
  fn process115 (r : &Row115't, dt : f32) -> bool = true
  fn score115 (r : &Row115't) -> f32 = 0.0
§ block_116
  def Row116't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle116 (r : &Row116't) -> bool = true
  fn process116 (r : &Row116't, dt : f32) -> bool = true
  fn score116 (r : &Row116't) -> f32 = 0.0
§ block_117
  def Row117't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle117 (r : &Row117't) -> bool = true
  fn process117 (r : &Row117't, dt : f32) -> bool = true
  fn score117 (r : &Row117't) -> f32 = 0.0
§ block_118
  def Row118't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle118 (r : &Row118't) -> bool = true
  fn process118 (r : &Row118't, dt : f32) -> bool = true
  fn score118 (r : &Row118't) -> f32 = 0.0
§ block_119
  def Row119't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle119 (r : &Row119't) -> bool = true
  fn process119 (r : &Row119't, dt : f32) -> bool = true
  fn score119 (r : &Row119't) -> f32 = 0.0
§ block_120
  def Row120't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle120 (r : &Row120't) -> bool = true
  fn process120 (r : &Row120't, dt : f32) -> bool = true
  fn score120 (r : &Row120't) -> f32 = 0.0
§ block_121
  def Row121't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle121 (r : &Row121't) -> bool = true
  fn process121 (r : &Row121't, dt : f32) -> bool = true
  fn score121 (r : &Row121't) -> f32 = 0.0
§ block_122
  def Row122't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle122 (r : &Row122't) -> bool = true
  fn process122 (r : &Row122't, dt : f32) -> bool = true
  fn score122 (r : &Row122't) -> f32 = 0.0
§ block_123
  def Row123't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle123 (r : &Row123't) -> bool = true
  fn process123 (r : &Row123't, dt : f32) -> bool = true
  fn score123 (r : &Row123't) -> f32 = 0.0
§ block_124
  def Row124't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle124 (r : &Row124't) -> bool = true
  fn process124 (r : &Row124't, dt : f32) -> bool = true
  fn score124 (r : &Row124't) -> f32 = 0.0
§ block_125
  def Row125't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle125 (r : &Row125't) -> bool = true
  fn process125 (r : &Row125't, dt : f32) -> bool = true
  fn score125 (r : &Row125't) -> f32 = 0.0
§ block_126
  def Row126't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle126 (r : &Row126't) -> bool = true
  fn process126 (r : &Row126't, dt : f32) -> bool = true
  fn score126 (r : &Row126't) -> f32 = 0.0
§ block_127
  def Row127't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle127 (r : &Row127't) -> bool = true
  fn process127 (r : &Row127't, dt : f32) -> bool = true
  fn score127 (r : &Row127't) -> f32 = 0.0
§ block_128
  def Row128't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle128 (r : &Row128't) -> bool = true
  fn process128 (r : &Row128't, dt : f32) -> bool = true
  fn score128 (r : &Row128't) -> f32 = 0.0
§ block_129
  def Row129't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle129 (r : &Row129't) -> bool = true
  fn process129 (r : &Row129't, dt : f32) -> bool = true
  fn score129 (r : &Row129't) -> f32 = 0.0
§ block_130
  def Row130't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle130 (r : &Row130't) -> bool = true
  fn process130 (r : &Row130't, dt : f32) -> bool = true
  fn score130 (r : &Row130't) -> f32 = 0.0
§ block_131
  def Row131't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle131 (r : &Row131't) -> bool = true
  fn process131 (r : &Row131't, dt : f32) -> bool = true
  fn score131 (r : &Row131't) -> f32 = 0.0
§ block_132
  def Row132't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle132 (r : &Row132't) -> bool = true
  fn process132 (r : &Row132't, dt : f32) -> bool = true
  fn score132 (r : &Row132't) -> f32 = 0.0
§ block_133
  def Row133't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle133 (r : &Row133't) -> bool = true
  fn process133 (r : &Row133't, dt : f32) -> bool = true
  fn score133 (r : &Row133't) -> f32 = 0.0
§ block_134
  def Row134't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle134 (r : &Row134't) -> bool = true
  fn process134 (r : &Row134't, dt : f32) -> bool = true
  fn score134 (r : &Row134't) -> f32 = 0.0
§ block_135
  def Row135't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle135 (r : &Row135't) -> bool = true
  fn process135 (r : &Row135't, dt : f32) -> bool = true
  fn score135 (r : &Row135't) -> f32 = 0.0
§ block_136
  def Row136't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle136 (r : &Row136't) -> bool = true
  fn process136 (r : &Row136't, dt : f32) -> bool = true
  fn score136 (r : &Row136't) -> f32 = 0.0
§ block_137
  def Row137't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle137 (r : &Row137't) -> bool = true
  fn process137 (r : &Row137't, dt : f32) -> bool = true
  fn score137 (r : &Row137't) -> f32 = 0.0
§ block_138
  def Row138't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle138 (r : &Row138't) -> bool = true
  fn process138 (r : &Row138't, dt : f32) -> bool = true
  fn score138 (r : &Row138't) -> f32 = 0.0
§ block_139
  def Row139't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle139 (r : &Row139't) -> bool = true
  fn process139 (r : &Row139't, dt : f32) -> bool = true
  fn score139 (r : &Row139't) -> f32 = 0.0
§ block_140
  def Row140't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle140 (r : &Row140't) -> bool = true
  fn process140 (r : &Row140't, dt : f32) -> bool = true
  fn score140 (r : &Row140't) -> f32 = 0.0
§ block_141
  def Row141't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle141 (r : &Row141't) -> bool = true
  fn process141 (r : &Row141't, dt : f32) -> bool = true
  fn score141 (r : &Row141't) -> f32 = 0.0
§ block_142
  def Row142't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle142 (r : &Row142't) -> bool = true
  fn process142 (r : &Row142't, dt : f32) -> bool = true
  fn score142 (r : &Row142't) -> f32 = 0.0
§ block_143
  def Row143't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle143 (r : &Row143't) -> bool = true
  fn process143 (r : &Row143't, dt : f32) -> bool = true
  fn score143 (r : &Row143't) -> f32 = 0.0
§ block_144
  def Row144't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle144 (r : &Row144't) -> bool = true
  fn process144 (r : &Row144't, dt : f32) -> bool = true
  fn score144 (r : &Row144't) -> f32 = 0.0
§ block_145
  def Row145't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle145 (r : &Row145't) -> bool = true
  fn process145 (r : &Row145't, dt : f32) -> bool = true
  fn score145 (r : &Row145't) -> f32 = 0.0
§ block_146
  def Row146't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle146 (r : &Row146't) -> bool = true
  fn process146 (r : &Row146't, dt : f32) -> bool = true
  fn score146 (r : &Row146't) -> f32 = 0.0
§ block_147
  def Row147't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle147 (r : &Row147't) -> bool = true
  fn process147 (r : &Row147't, dt : f32) -> bool = true
  fn score147 (r : &Row147't) -> f32 = 0.0
§ block_148
  def Row148't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle148 (r : &Row148't) -> bool = true
  fn process148 (r : &Row148't, dt : f32) -> bool = true
  fn score148 (r : &Row148't) -> f32 = 0.0
§ block_149
  def Row149't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle149 (r : &Row149't) -> bool = true
  fn process149 (r : &Row149't, dt : f32) -> bool = true
  fn score149 (r : &Row149't) -> f32 = 0.0
§ block_150
  def Row150't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle150 (r : &Row150't) -> bool = true
  fn process150 (r : &Row150't, dt : f32) -> bool = true
  fn score150 (r : &Row150't) -> f32 = 0.0
§ block_151
  def Row151't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle151 (r : &Row151't) -> bool = true
  fn process151 (r : &Row151't, dt : f32) -> bool = true
  fn score151 (r : &Row151't) -> f32 = 0.0
§ block_152
  def Row152't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle152 (r : &Row152't) -> bool = true
  fn process152 (r : &Row152't, dt : f32) -> bool = true
  fn score152 (r : &Row152't) -> f32 = 0.0
§ block_153
  def Row153't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle153 (r : &Row153't) -> bool = true
  fn process153 (r : &Row153't, dt : f32) -> bool = true
  fn score153 (r : &Row153't) -> f32 = 0.0
§ block_154
  def Row154't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle154 (r : &Row154't) -> bool = true
  fn process154 (r : &Row154't, dt : f32) -> bool = true
  fn score154 (r : &Row154't) -> f32 = 0.0
§ block_155
  def Row155't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle155 (r : &Row155't) -> bool = true
  fn process155 (r : &Row155't, dt : f32) -> bool = true
  fn score155 (r : &Row155't) -> f32 = 0.0
§ block_156
  def Row156't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle156 (r : &Row156't) -> bool = true
  fn process156 (r : &Row156't, dt : f32) -> bool = true
  fn score156 (r : &Row156't) -> f32 = 0.0
§ block_157
  def Row157't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle157 (r : &Row157't) -> bool = true
  fn process157 (r : &Row157't, dt : f32) -> bool = true
  fn score157 (r : &Row157't) -> f32 = 0.0
§ block_158
  def Row158't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle158 (r : &Row158't) -> bool = true
  fn process158 (r : &Row158't, dt : f32) -> bool = true
  fn score158 (r : &Row158't) -> f32 = 0.0
§ block_159
  def Row159't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle159 (r : &Row159't) -> bool = true
  fn process159 (r : &Row159't, dt : f32) -> bool = true
  fn score159 (r : &Row159't) -> f32 = 0.0
§ block_160
  def Row160't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle160 (r : &Row160't) -> bool = true
  fn process160 (r : &Row160't, dt : f32) -> bool = true
  fn score160 (r : &Row160't) -> f32 = 0.0
§ block_161
  def Row161't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle161 (r : &Row161't) -> bool = true
  fn process161 (r : &Row161't, dt : f32) -> bool = true
  fn score161 (r : &Row161't) -> f32 = 0.0
§ block_162
  def Row162't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle162 (r : &Row162't) -> bool = true
  fn process162 (r : &Row162't, dt : f32) -> bool = true
  fn score162 (r : &Row162't) -> f32 = 0.0
§ block_163
  def Row163't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle163 (r : &Row163't) -> bool = true
  fn process163 (r : &Row163't, dt : f32) -> bool = true
  fn score163 (r : &Row163't) -> f32 = 0.0
§ block_164
  def Row164't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle164 (r : &Row164't) -> bool = true
  fn process164 (r : &Row164't, dt : f32) -> bool = true
  fn score164 (r : &Row164't) -> f32 = 0.0
§ block_165
  def Row165't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle165 (r : &Row165't) -> bool = true
  fn process165 (r : &Row165't, dt : f32) -> bool = true
  fn score165 (r : &Row165't) -> f32 = 0.0
§ block_166
  def Row166't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle166 (r : &Row166't) -> bool = true
  fn process166 (r : &Row166't, dt : f32) -> bool = true
  fn score166 (r : &Row166't) -> f32 = 0.0
§ block_167
  def Row167't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle167 (r : &Row167't) -> bool = true
  fn process167 (r : &Row167't, dt : f32) -> bool = true
  fn score167 (r : &Row167't) -> f32 = 0.0
§ block_168
  def Row168't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle168 (r : &Row168't) -> bool = true
  fn process168 (r : &Row168't, dt : f32) -> bool = true
  fn score168 (r : &Row168't) -> f32 = 0.0
§ block_169
  def Row169't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle169 (r : &Row169't) -> bool = true
  fn process169 (r : &Row169't, dt : f32) -> bool = true
  fn score169 (r : &Row169't) -> f32 = 0.0
§ block_170
  def Row170't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle170 (r : &Row170't) -> bool = true
  fn process170 (r : &Row170't, dt : f32) -> bool = true
  fn score170 (r : &Row170't) -> f32 = 0.0
§ block_171
  def Row171't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle171 (r : &Row171't) -> bool = true
  fn process171 (r : &Row171't, dt : f32) -> bool = true
  fn score171 (r : &Row171't) -> f32 = 0.0
§ block_172
  def Row172't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle172 (r : &Row172't) -> bool = true
  fn process172 (r : &Row172't, dt : f32) -> bool = true
  fn score172 (r : &Row172't) -> f32 = 0.0
§ block_173
  def Row173't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle173 (r : &Row173't) -> bool = true
  fn process173 (r : &Row173't, dt : f32) -> bool = true
  fn score173 (r : &Row173't) -> f32 = 0.0
§ block_174
  def Row174't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle174 (r : &Row174't) -> bool = true
  fn process174 (r : &Row174't, dt : f32) -> bool = true
  fn score174 (r : &Row174't) -> f32 = 0.0
§ block_175
  def Row175't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle175 (r : &Row175't) -> bool = true
  fn process175 (r : &Row175't, dt : f32) -> bool = true
  fn score175 (r : &Row175't) -> f32 = 0.0
§ block_176
  def Row176't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle176 (r : &Row176't) -> bool = true
  fn process176 (r : &Row176't, dt : f32) -> bool = true
  fn score176 (r : &Row176't) -> f32 = 0.0
§ block_177
  def Row177't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle177 (r : &Row177't) -> bool = true
  fn process177 (r : &Row177't, dt : f32) -> bool = true
  fn score177 (r : &Row177't) -> f32 = 0.0
§ block_178
  def Row178't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle178 (r : &Row178't) -> bool = true
  fn process178 (r : &Row178't, dt : f32) -> bool = true
  fn score178 (r : &Row178't) -> f32 = 0.0
§ block_179
  def Row179't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle179 (r : &Row179't) -> bool = true
  fn process179 (r : &Row179't, dt : f32) -> bool = true
  fn score179 (r : &Row179't) -> f32 = 0.0
§ block_180
  def Row180't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle180 (r : &Row180't) -> bool = true
  fn process180 (r : &Row180't, dt : f32) -> bool = true
  fn score180 (r : &Row180't) -> f32 = 0.0
§ block_181
  def Row181't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle181 (r : &Row181't) -> bool = true
  fn process181 (r : &Row181't, dt : f32) -> bool = true
  fn score181 (r : &Row181't) -> f32 = 0.0
§ block_182
  def Row182't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle182 (r : &Row182't) -> bool = true
  fn process182 (r : &Row182't, dt : f32) -> bool = true
  fn score182 (r : &Row182't) -> f32 = 0.0
§ block_183
  def Row183't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle183 (r : &Row183't) -> bool = true
  fn process183 (r : &Row183't, dt : f32) -> bool = true
  fn score183 (r : &Row183't) -> f32 = 0.0
§ block_184
  def Row184't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle184 (r : &Row184't) -> bool = true
  fn process184 (r : &Row184't, dt : f32) -> bool = true
  fn score184 (r : &Row184't) -> f32 = 0.0
§ block_185
  def Row185't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle185 (r : &Row185't) -> bool = true
  fn process185 (r : &Row185't, dt : f32) -> bool = true
  fn score185 (r : &Row185't) -> f32 = 0.0
§ block_186
  def Row186't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle186 (r : &Row186't) -> bool = true
  fn process186 (r : &Row186't, dt : f32) -> bool = true
  fn score186 (r : &Row186't) -> f32 = 0.0
§ block_187
  def Row187't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle187 (r : &Row187't) -> bool = true
  fn process187 (r : &Row187't, dt : f32) -> bool = true
  fn score187 (r : &Row187't) -> f32 = 0.0
§ block_188
  def Row188't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle188 (r : &Row188't) -> bool = true
  fn process188 (r : &Row188't, dt : f32) -> bool = true
  fn score188 (r : &Row188't) -> f32 = 0.0
§ block_189
  def Row189't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle189 (r : &Row189't) -> bool = true
  fn process189 (r : &Row189't, dt : f32) -> bool = true
  fn score189 (r : &Row189't) -> f32 = 0.0
§ block_190
  def Row190't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle190 (r : &Row190't) -> bool = true
  fn process190 (r : &Row190't, dt : f32) -> bool = true
  fn score190 (r : &Row190't) -> f32 = 0.0
§ block_191
  def Row191't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle191 (r : &Row191't) -> bool = true
  fn process191 (r : &Row191't, dt : f32) -> bool = true
  fn score191 (r : &Row191't) -> f32 = 0.0
§ block_192
  def Row192't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle192 (r : &Row192't) -> bool = true
  fn process192 (r : &Row192't, dt : f32) -> bool = true
  fn score192 (r : &Row192't) -> f32 = 0.0
§ block_193
  def Row193't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle193 (r : &Row193't) -> bool = true
  fn process193 (r : &Row193't, dt : f32) -> bool = true
  fn score193 (r : &Row193't) -> f32 = 0.0
§ block_194
  def Row194't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle194 (r : &Row194't) -> bool = true
  fn process194 (r : &Row194't, dt : f32) -> bool = true
  fn score194 (r : &Row194't) -> f32 = 0.0
§ block_195
  def Row195't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle195 (r : &Row195't) -> bool = true
  fn process195 (r : &Row195't, dt : f32) -> bool = true
  fn score195 (r : &Row195't) -> f32 = 0.0
§ block_196
  def Row196't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle196 (r : &Row196't) -> bool = true
  fn process196 (r : &Row196't, dt : f32) -> bool = true
  fn score196 (r : &Row196't) -> f32 = 0.0
§ block_197
  def Row197't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle197 (r : &Row197't) -> bool = true
  fn process197 (r : &Row197't, dt : f32) -> bool = true
  fn score197 (r : &Row197't) -> f32 = 0.0
§ block_198
  def Row198't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle198 (r : &Row198't) -> bool = true
  fn process198 (r : &Row198't, dt : f32) -> bool = true
  fn score198 (r : &Row198't) -> f32 = 0.0
§ block_199
  def Row199't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle199 (r : &Row199't) -> bool = true
  fn process199 (r : &Row199't, dt : f32) -> bool = true
  fn score199 (r : &Row199't) -> f32 = 0.0
§ block_200
  def Row200't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle200 (r : &Row200't) -> bool = true
  fn process200 (r : &Row200't, dt : f32) -> bool = true
  fn score200 (r : &Row200't) -> f32 = 0.0
§ block_201
  def Row201't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle201 (r : &Row201't) -> bool = true
  fn process201 (r : &Row201't, dt : f32) -> bool = true
  fn score201 (r : &Row201't) -> f32 = 0.0
§ block_202
  def Row202't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle202 (r : &Row202't) -> bool = true
  fn process202 (r : &Row202't, dt : f32) -> bool = true
  fn score202 (r : &Row202't) -> f32 = 0.0
§ block_203
  def Row203't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle203 (r : &Row203't) -> bool = true
  fn process203 (r : &Row203't, dt : f32) -> bool = true
  fn score203 (r : &Row203't) -> f32 = 0.0
§ block_204
  def Row204't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle204 (r : &Row204't) -> bool = true
  fn process204 (r : &Row204't, dt : f32) -> bool = true
  fn score204 (r : &Row204't) -> f32 = 0.0
§ block_205
  def Row205't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle205 (r : &Row205't) -> bool = true
  fn process205 (r : &Row205't, dt : f32) -> bool = true
  fn score205 (r : &Row205't) -> f32 = 0.0
§ block_206
  def Row206't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle206 (r : &Row206't) -> bool = true
  fn process206 (r : &Row206't, dt : f32) -> bool = true
  fn score206 (r : &Row206't) -> f32 = 0.0
§ block_207
  def Row207't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle207 (r : &Row207't) -> bool = true
  fn process207 (r : &Row207't, dt : f32) -> bool = true
  fn score207 (r : &Row207't) -> f32 = 0.0
§ block_208
  def Row208't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle208 (r : &Row208't) -> bool = true
  fn process208 (r : &Row208't, dt : f32) -> bool = true
  fn score208 (r : &Row208't) -> f32 = 0.0
§ block_209
  def Row209't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle209 (r : &Row209't) -> bool = true
  fn process209 (r : &Row209't, dt : f32) -> bool = true
  fn score209 (r : &Row209't) -> f32 = 0.0
§ block_210
  def Row210't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle210 (r : &Row210't) -> bool = true
  fn process210 (r : &Row210't, dt : f32) -> bool = true
  fn score210 (r : &Row210't) -> f32 = 0.0
§ block_211
  def Row211't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle211 (r : &Row211't) -> bool = true
  fn process211 (r : &Row211't, dt : f32) -> bool = true
  fn score211 (r : &Row211't) -> f32 = 0.0
§ block_212
  def Row212't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle212 (r : &Row212't) -> bool = true
  fn process212 (r : &Row212't, dt : f32) -> bool = true
  fn score212 (r : &Row212't) -> f32 = 0.0
§ block_213
  def Row213't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle213 (r : &Row213't) -> bool = true
  fn process213 (r : &Row213't, dt : f32) -> bool = true
  fn score213 (r : &Row213't) -> f32 = 0.0
§ block_214
  def Row214't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle214 (r : &Row214't) -> bool = true
  fn process214 (r : &Row214't, dt : f32) -> bool = true
  fn score214 (r : &Row214't) -> f32 = 0.0
§ block_215
  def Row215't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle215 (r : &Row215't) -> bool = true
  fn process215 (r : &Row215't, dt : f32) -> bool = true
  fn score215 (r : &Row215't) -> f32 = 0.0
§ block_216
  def Row216't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle216 (r : &Row216't) -> bool = true
  fn process216 (r : &Row216't, dt : f32) -> bool = true
  fn score216 (r : &Row216't) -> f32 = 0.0
§ block_217
  def Row217't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle217 (r : &Row217't) -> bool = true
  fn process217 (r : &Row217't, dt : f32) -> bool = true
  fn score217 (r : &Row217't) -> f32 = 0.0
§ block_218
  def Row218't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle218 (r : &Row218't) -> bool = true
  fn process218 (r : &Row218't, dt : f32) -> bool = true
  fn score218 (r : &Row218't) -> f32 = 0.0
§ block_219
  def Row219't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle219 (r : &Row219't) -> bool = true
  fn process219 (r : &Row219't, dt : f32) -> bool = true
  fn score219 (r : &Row219't) -> f32 = 0.0
§ block_220
  def Row220't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle220 (r : &Row220't) -> bool = true
  fn process220 (r : &Row220't, dt : f32) -> bool = true
  fn score220 (r : &Row220't) -> f32 = 0.0
§ block_221
  def Row221't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle221 (r : &Row221't) -> bool = true
  fn process221 (r : &Row221't, dt : f32) -> bool = true
  fn score221 (r : &Row221't) -> f32 = 0.0
§ block_222
  def Row222't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle222 (r : &Row222't) -> bool = true
  fn process222 (r : &Row222't, dt : f32) -> bool = true
  fn score222 (r : &Row222't) -> f32 = 0.0
§ block_223
  def Row223't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle223 (r : &Row223't) -> bool = true
  fn process223 (r : &Row223't, dt : f32) -> bool = true
  fn score223 (r : &Row223't) -> f32 = 0.0
§ block_224
  def Row224't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle224 (r : &Row224't) -> bool = true
  fn process224 (r : &Row224't, dt : f32) -> bool = true
  fn score224 (r : &Row224't) -> f32 = 0.0
§ block_225
  def Row225't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle225 (r : &Row225't) -> bool = true
  fn process225 (r : &Row225't, dt : f32) -> bool = true
  fn score225 (r : &Row225't) -> f32 = 0.0
§ block_226
  def Row226't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle226 (r : &Row226't) -> bool = true
  fn process226 (r : &Row226't, dt : f32) -> bool = true
  fn score226 (r : &Row226't) -> f32 = 0.0
§ block_227
  def Row227't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle227 (r : &Row227't) -> bool = true
  fn process227 (r : &Row227't, dt : f32) -> bool = true
  fn score227 (r : &Row227't) -> f32 = 0.0
§ block_228
  def Row228't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle228 (r : &Row228't) -> bool = true
  fn process228 (r : &Row228't, dt : f32) -> bool = true
  fn score228 (r : &Row228't) -> f32 = 0.0
§ block_229
  def Row229't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle229 (r : &Row229't) -> bool = true
  fn process229 (r : &Row229't, dt : f32) -> bool = true
  fn score229 (r : &Row229't) -> f32 = 0.0
§ block_230
  def Row230't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle230 (r : &Row230't) -> bool = true
  fn process230 (r : &Row230't, dt : f32) -> bool = true
  fn score230 (r : &Row230't) -> f32 = 0.0
§ block_231
  def Row231't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle231 (r : &Row231't) -> bool = true
  fn process231 (r : &Row231't, dt : f32) -> bool = true
  fn score231 (r : &Row231't) -> f32 = 0.0
§ block_232
  def Row232't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle232 (r : &Row232't) -> bool = true
  fn process232 (r : &Row232't, dt : f32) -> bool = true
  fn score232 (r : &Row232't) -> f32 = 0.0
§ block_233
  def Row233't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle233 (r : &Row233't) -> bool = true
  fn process233 (r : &Row233't, dt : f32) -> bool = true
  fn score233 (r : &Row233't) -> f32 = 0.0
§ block_234
  def Row234't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle234 (r : &Row234't) -> bool = true
  fn process234 (r : &Row234't, dt : f32) -> bool = true
  fn score234 (r : &Row234't) -> f32 = 0.0
§ block_235
  def Row235't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle235 (r : &Row235't) -> bool = true
  fn process235 (r : &Row235't, dt : f32) -> bool = true
  fn score235 (r : &Row235't) -> f32 = 0.0
§ block_236
  def Row236't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle236 (r : &Row236't) -> bool = true
  fn process236 (r : &Row236't, dt : f32) -> bool = true
  fn score236 (r : &Row236't) -> f32 = 0.0
§ block_237
  def Row237't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle237 (r : &Row237't) -> bool = true
  fn process237 (r : &Row237't, dt : f32) -> bool = true
  fn score237 (r : &Row237't) -> f32 = 0.0
§ block_238
  def Row238't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle238 (r : &Row238't) -> bool = true
  fn process238 (r : &Row238't, dt : f32) -> bool = true
  fn score238 (r : &Row238't) -> f32 = 0.0
§ block_239
  def Row239't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle239 (r : &Row239't) -> bool = true
  fn process239 (r : &Row239't, dt : f32) -> bool = true
  fn score239 (r : &Row239't) -> f32 = 0.0
§ block_240
  def Row240't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle240 (r : &Row240't) -> bool = true
  fn process240 (r : &Row240't, dt : f32) -> bool = true
  fn score240 (r : &Row240't) -> f32 = 0.0
§ block_241
  def Row241't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle241 (r : &Row241't) -> bool = true
  fn process241 (r : &Row241't, dt : f32) -> bool = true
  fn score241 (r : &Row241't) -> f32 = 0.0
§ block_242
  def Row242't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle242 (r : &Row242't) -> bool = true
  fn process242 (r : &Row242't, dt : f32) -> bool = true
  fn score242 (r : &Row242't) -> f32 = 0.0
§ block_243
  def Row243't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle243 (r : &Row243't) -> bool = true
  fn process243 (r : &Row243't, dt : f32) -> bool = true
  fn score243 (r : &Row243't) -> f32 = 0.0
§ block_244
  def Row244't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle244 (r : &Row244't) -> bool = true
  fn process244 (r : &Row244't, dt : f32) -> bool = true
  fn score244 (r : &Row244't) -> f32 = 0.0
§ block_245
  def Row245't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle245 (r : &Row245't) -> bool = true
  fn process245 (r : &Row245't, dt : f32) -> bool = true
  fn score245 (r : &Row245't) -> f32 = 0.0
§ block_246
  def Row246't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle246 (r : &Row246't) -> bool = true
  fn process246 (r : &Row246't, dt : f32) -> bool = true
  fn score246 (r : &Row246't) -> f32 = 0.0
§ block_247
  def Row247't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle247 (r : &Row247't) -> bool = true
  fn process247 (r : &Row247't, dt : f32) -> bool = true
  fn score247 (r : &Row247't) -> f32 = 0.0
§ block_248
  def Row248't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle248 (r : &Row248't) -> bool = true
  fn process248 (r : &Row248't, dt : f32) -> bool = true
  fn score248 (r : &Row248't) -> f32 = 0.0
§ block_249
  def Row249't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle249 (r : &Row249't) -> bool = true
  fn process249 (r : &Row249't, dt : f32) -> bool = true
  fn score249 (r : &Row249't) -> f32 = 0.0
§ block_250
  def Row250't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle250 (r : &Row250't) -> bool = true
  fn process250 (r : &Row250't, dt : f32) -> bool = true
  fn score250 (r : &Row250't) -> f32 = 0.0
§ block_251
  def Row251't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle251 (r : &Row251't) -> bool = true
  fn process251 (r : &Row251't, dt : f32) -> bool = true
  fn score251 (r : &Row251't) -> f32 = 0.0
§ block_252
  def Row252't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle252 (r : &Row252't) -> bool = true
  fn process252 (r : &Row252't, dt : f32) -> bool = true
  fn score252 (r : &Row252't) -> f32 = 0.0
§ block_253
  def Row253't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle253 (r : &Row253't) -> bool = true
  fn process253 (r : &Row253't, dt : f32) -> bool = true
  fn score253 (r : &Row253't) -> f32 = 0.0
§ block_254
  def Row254't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle254 (r : &Row254't) -> bool = true
  fn process254 (r : &Row254't, dt : f32) -> bool = true
  fn score254 (r : &Row254't) -> f32 = 0.0
§ block_255
  def Row255't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle255 (r : &Row255't) -> bool = true
  fn process255 (r : &Row255't, dt : f32) -> bool = true
  fn score255 (r : &Row255't) -> f32 = 0.0
§ block_256
  def Row256't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle256 (r : &Row256't) -> bool = true
  fn process256 (r : &Row256't, dt : f32) -> bool = true
  fn score256 (r : &Row256't) -> f32 = 0.0
§ block_257
  def Row257't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle257 (r : &Row257't) -> bool = true
  fn process257 (r : &Row257't, dt : f32) -> bool = true
  fn score257 (r : &Row257't) -> f32 = 0.0
§ block_258
  def Row258't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle258 (r : &Row258't) -> bool = true
  fn process258 (r : &Row258't, dt : f32) -> bool = true
  fn score258 (r : &Row258't) -> f32 = 0.0
§ block_259
  def Row259't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle259 (r : &Row259't) -> bool = true
  fn process259 (r : &Row259't, dt : f32) -> bool = true
  fn score259 (r : &Row259't) -> f32 = 0.0
§ block_260
  def Row260't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle260 (r : &Row260't) -> bool = true
  fn process260 (r : &Row260't, dt : f32) -> bool = true
  fn score260 (r : &Row260't) -> f32 = 0.0
§ block_261
  def Row261't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle261 (r : &Row261't) -> bool = true
  fn process261 (r : &Row261't, dt : f32) -> bool = true
  fn score261 (r : &Row261't) -> f32 = 0.0
§ block_262
  def Row262't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle262 (r : &Row262't) -> bool = true
  fn process262 (r : &Row262't, dt : f32) -> bool = true
  fn score262 (r : &Row262't) -> f32 = 0.0
§ block_263
  def Row263't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle263 (r : &Row263't) -> bool = true
  fn process263 (r : &Row263't, dt : f32) -> bool = true
  fn score263 (r : &Row263't) -> f32 = 0.0
§ block_264
  def Row264't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle264 (r : &Row264't) -> bool = true
  fn process264 (r : &Row264't, dt : f32) -> bool = true
  fn score264 (r : &Row264't) -> f32 = 0.0
§ block_265
  def Row265't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle265 (r : &Row265't) -> bool = true
  fn process265 (r : &Row265't, dt : f32) -> bool = true
  fn score265 (r : &Row265't) -> f32 = 0.0
§ block_266
  def Row266't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle266 (r : &Row266't) -> bool = true
  fn process266 (r : &Row266't, dt : f32) -> bool = true
  fn score266 (r : &Row266't) -> f32 = 0.0
§ block_267
  def Row267't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle267 (r : &Row267't) -> bool = true
  fn process267 (r : &Row267't, dt : f32) -> bool = true
  fn score267 (r : &Row267't) -> f32 = 0.0
§ block_268
  def Row268't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle268 (r : &Row268't) -> bool = true
  fn process268 (r : &Row268't, dt : f32) -> bool = true
  fn score268 (r : &Row268't) -> f32 = 0.0
§ block_269
  def Row269't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle269 (r : &Row269't) -> bool = true
  fn process269 (r : &Row269't, dt : f32) -> bool = true
  fn score269 (r : &Row269't) -> f32 = 0.0
§ block_270
  def Row270't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle270 (r : &Row270't) -> bool = true
  fn process270 (r : &Row270't, dt : f32) -> bool = true
  fn score270 (r : &Row270't) -> f32 = 0.0
§ block_271
  def Row271't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle271 (r : &Row271't) -> bool = true
  fn process271 (r : &Row271't, dt : f32) -> bool = true
  fn score271 (r : &Row271't) -> f32 = 0.0
§ block_272
  def Row272't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle272 (r : &Row272't) -> bool = true
  fn process272 (r : &Row272't, dt : f32) -> bool = true
  fn score272 (r : &Row272't) -> f32 = 0.0
§ block_273
  def Row273't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle273 (r : &Row273't) -> bool = true
  fn process273 (r : &Row273't, dt : f32) -> bool = true
  fn score273 (r : &Row273't) -> f32 = 0.0
§ block_274
  def Row274't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle274 (r : &Row274't) -> bool = true
  fn process274 (r : &Row274't, dt : f32) -> bool = true
  fn score274 (r : &Row274't) -> f32 = 0.0
§ block_275
  def Row275't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle275 (r : &Row275't) -> bool = true
  fn process275 (r : &Row275't, dt : f32) -> bool = true
  fn score275 (r : &Row275't) -> f32 = 0.0
§ block_276
  def Row276't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle276 (r : &Row276't) -> bool = true
  fn process276 (r : &Row276't, dt : f32) -> bool = true
  fn score276 (r : &Row276't) -> f32 = 0.0
§ block_277
  def Row277't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle277 (r : &Row277't) -> bool = true
  fn process277 (r : &Row277't, dt : f32) -> bool = true
  fn score277 (r : &Row277't) -> f32 = 0.0
§ block_278
  def Row278't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle278 (r : &Row278't) -> bool = true
  fn process278 (r : &Row278't, dt : f32) -> bool = true
  fn score278 (r : &Row278't) -> f32 = 0.0
§ block_279
  def Row279't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle279 (r : &Row279't) -> bool = true
  fn process279 (r : &Row279't, dt : f32) -> bool = true
  fn score279 (r : &Row279't) -> f32 = 0.0
§ block_280
  def Row280't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle280 (r : &Row280't) -> bool = true
  fn process280 (r : &Row280't, dt : f32) -> bool = true
  fn score280 (r : &Row280't) -> f32 = 0.0
§ block_281
  def Row281't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle281 (r : &Row281't) -> bool = true
  fn process281 (r : &Row281't, dt : f32) -> bool = true
  fn score281 (r : &Row281't) -> f32 = 0.0
§ block_282
  def Row282't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle282 (r : &Row282't) -> bool = true
  fn process282 (r : &Row282't, dt : f32) -> bool = true
  fn score282 (r : &Row282't) -> f32 = 0.0
§ block_283
  def Row283't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle283 (r : &Row283't) -> bool = true
  fn process283 (r : &Row283't, dt : f32) -> bool = true
  fn score283 (r : &Row283't) -> f32 = 0.0
§ block_284
  def Row284't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle284 (r : &Row284't) -> bool = true
  fn process284 (r : &Row284't, dt : f32) -> bool = true
  fn score284 (r : &Row284't) -> f32 = 0.0
§ block_285
  def Row285't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle285 (r : &Row285't) -> bool = true
  fn process285 (r : &Row285't, dt : f32) -> bool = true
  fn score285 (r : &Row285't) -> f32 = 0.0
§ block_286
  def Row286't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle286 (r : &Row286't) -> bool = true
  fn process286 (r : &Row286't, dt : f32) -> bool = true
  fn score286 (r : &Row286't) -> f32 = 0.0
§ block_287
  def Row287't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle287 (r : &Row287't) -> bool = true
  fn process287 (r : &Row287't, dt : f32) -> bool = true
  fn score287 (r : &Row287't) -> f32 = 0.0
§ block_288
  def Row288't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle288 (r : &Row288't) -> bool = true
  fn process288 (r : &Row288't, dt : f32) -> bool = true
  fn score288 (r : &Row288't) -> f32 = 0.0
§ block_289
  def Row289't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle289 (r : &Row289't) -> bool = true
  fn process289 (r : &Row289't, dt : f32) -> bool = true
  fn score289 (r : &Row289't) -> f32 = 0.0
§ block_290
  def Row290't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle290 (r : &Row290't) -> bool = true
  fn process290 (r : &Row290't, dt : f32) -> bool = true
  fn score290 (r : &Row290't) -> f32 = 0.0
§ block_291
  def Row291't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle291 (r : &Row291't) -> bool = true
  fn process291 (r : &Row291't, dt : f32) -> bool = true
  fn score291 (r : &Row291't) -> f32 = 0.0
§ block_292
  def Row292't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle292 (r : &Row292't) -> bool = true
  fn process292 (r : &Row292't, dt : f32) -> bool = true
  fn score292 (r : &Row292't) -> f32 = 0.0
§ block_293
  def Row293't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle293 (r : &Row293't) -> bool = true
  fn process293 (r : &Row293't, dt : f32) -> bool = true
  fn score293 (r : &Row293't) -> f32 = 0.0
§ block_294
  def Row294't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle294 (r : &Row294't) -> bool = true
  fn process294 (r : &Row294't, dt : f32) -> bool = true
  fn score294 (r : &Row294't) -> f32 = 0.0
§ block_295
  def Row295't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle295 (r : &Row295't) -> bool = true
  fn process295 (r : &Row295't, dt : f32) -> bool = true
  fn score295 (r : &Row295't) -> f32 = 0.0
§ block_296
  def Row296't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle296 (r : &Row296't) -> bool = true
  fn process296 (r : &Row296't, dt : f32) -> bool = true
  fn score296 (r : &Row296't) -> f32 = 0.0
§ block_297
  def Row297't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle297 (r : &Row297't) -> bool = true
  fn process297 (r : &Row297't, dt : f32) -> bool = true
  fn score297 (r : &Row297't) -> f32 = 0.0
§ block_298
  def Row298't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle298 (r : &Row298't) -> bool = true
  fn process298 (r : &Row298't, dt : f32) -> bool = true
  fn score298 (r : &Row298't) -> f32 = 0.0
§ block_299
  def Row299't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle299 (r : &Row299't) -> bool = true
  fn process299 (r : &Row299't, dt : f32) -> bool = true
  fn score299 (r : &Row299't) -> f32 = 0.0
§ block_300
  def Row300't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle300 (r : &Row300't) -> bool = true
  fn process300 (r : &Row300't, dt : f32) -> bool = true
  fn score300 (r : &Row300't) -> f32 = 0.0
§ block_301
  def Row301't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle301 (r : &Row301't) -> bool = true
  fn process301 (r : &Row301't, dt : f32) -> bool = true
  fn score301 (r : &Row301't) -> f32 = 0.0
§ block_302
  def Row302't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle302 (r : &Row302't) -> bool = true
  fn process302 (r : &Row302't, dt : f32) -> bool = true
  fn score302 (r : &Row302't) -> f32 = 0.0
§ block_303
  def Row303't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle303 (r : &Row303't) -> bool = true
  fn process303 (r : &Row303't, dt : f32) -> bool = true
  fn score303 (r : &Row303't) -> f32 = 0.0
§ block_304
  def Row304't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle304 (r : &Row304't) -> bool = true
  fn process304 (r : &Row304't, dt : f32) -> bool = true
  fn score304 (r : &Row304't) -> f32 = 0.0
§ block_305
  def Row305't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle305 (r : &Row305't) -> bool = true
  fn process305 (r : &Row305't, dt : f32) -> bool = true
  fn score305 (r : &Row305't) -> f32 = 0.0
§ block_306
  def Row306't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle306 (r : &Row306't) -> bool = true
  fn process306 (r : &Row306't, dt : f32) -> bool = true
  fn score306 (r : &Row306't) -> f32 = 0.0
§ block_307
  def Row307't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle307 (r : &Row307't) -> bool = true
  fn process307 (r : &Row307't, dt : f32) -> bool = true
  fn score307 (r : &Row307't) -> f32 = 0.0
§ block_308
  def Row308't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle308 (r : &Row308't) -> bool = true
  fn process308 (r : &Row308't, dt : f32) -> bool = true
  fn score308 (r : &Row308't) -> f32 = 0.0
§ block_309
  def Row309't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle309 (r : &Row309't) -> bool = true
  fn process309 (r : &Row309't, dt : f32) -> bool = true
  fn score309 (r : &Row309't) -> f32 = 0.0
§ block_310
  def Row310't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle310 (r : &Row310't) -> bool = true
  fn process310 (r : &Row310't, dt : f32) -> bool = true
  fn score310 (r : &Row310't) -> f32 = 0.0
§ block_311
  def Row311't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle311 (r : &Row311't) -> bool = true
  fn process311 (r : &Row311't, dt : f32) -> bool = true
  fn score311 (r : &Row311't) -> f32 = 0.0
§ block_312
  def Row312't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle312 (r : &Row312't) -> bool = true
  fn process312 (r : &Row312't, dt : f32) -> bool = true
  fn score312 (r : &Row312't) -> f32 = 0.0
§ block_313
  def Row313't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle313 (r : &Row313't) -> bool = true
  fn process313 (r : &Row313't, dt : f32) -> bool = true
  fn score313 (r : &Row313't) -> f32 = 0.0
§ block_314
  def Row314't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle314 (r : &Row314't) -> bool = true
  fn process314 (r : &Row314't, dt : f32) -> bool = true
  fn score314 (r : &Row314't) -> f32 = 0.0
§ block_315
  def Row315't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle315 (r : &Row315't) -> bool = true
  fn process315 (r : &Row315't, dt : f32) -> bool = true
  fn score315 (r : &Row315't) -> f32 = 0.0
§ block_316
  def Row316't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle316 (r : &Row316't) -> bool = true
  fn process316 (r : &Row316't, dt : f32) -> bool = true
  fn score316 (r : &Row316't) -> f32 = 0.0
§ block_317
  def Row317't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle317 (r : &Row317't) -> bool = true
  fn process317 (r : &Row317't, dt : f32) -> bool = true
  fn score317 (r : &Row317't) -> f32 = 0.0
§ block_318
  def Row318't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle318 (r : &Row318't) -> bool = true
  fn process318 (r : &Row318't, dt : f32) -> bool = true
  fn score318 (r : &Row318't) -> f32 = 0.0
§ block_319
  def Row319't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle319 (r : &Row319't) -> bool = true
  fn process319 (r : &Row319't, dt : f32) -> bool = true
  fn score319 (r : &Row319't) -> f32 = 0.0
§ block_320
  def Row320't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle320 (r : &Row320't) -> bool = true
  fn process320 (r : &Row320't, dt : f32) -> bool = true
  fn score320 (r : &Row320't) -> f32 = 0.0
§ block_321
  def Row321't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle321 (r : &Row321't) -> bool = true
  fn process321 (r : &Row321't, dt : f32) -> bool = true
  fn score321 (r : &Row321't) -> f32 = 0.0
§ block_322
  def Row322't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle322 (r : &Row322't) -> bool = true
  fn process322 (r : &Row322't, dt : f32) -> bool = true
  fn score322 (r : &Row322't) -> f32 = 0.0
§ block_323
  def Row323't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle323 (r : &Row323't) -> bool = true
  fn process323 (r : &Row323't, dt : f32) -> bool = true
  fn score323 (r : &Row323't) -> f32 = 0.0
§ block_324
  def Row324't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle324 (r : &Row324't) -> bool = true
  fn process324 (r : &Row324't, dt : f32) -> bool = true
  fn score324 (r : &Row324't) -> f32 = 0.0
§ block_325
  def Row325't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle325 (r : &Row325't) -> bool = true
  fn process325 (r : &Row325't, dt : f32) -> bool = true
  fn score325 (r : &Row325't) -> f32 = 0.0
§ block_326
  def Row326't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle326 (r : &Row326't) -> bool = true
  fn process326 (r : &Row326't, dt : f32) -> bool = true
  fn score326 (r : &Row326't) -> f32 = 0.0
§ block_327
  def Row327't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle327 (r : &Row327't) -> bool = true
  fn process327 (r : &Row327't, dt : f32) -> bool = true
  fn score327 (r : &Row327't) -> f32 = 0.0
§ block_328
  def Row328't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle328 (r : &Row328't) -> bool = true
  fn process328 (r : &Row328't, dt : f32) -> bool = true
  fn score328 (r : &Row328't) -> f32 = 0.0
§ block_329
  def Row329't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle329 (r : &Row329't) -> bool = true
  fn process329 (r : &Row329't, dt : f32) -> bool = true
  fn score329 (r : &Row329't) -> f32 = 0.0
§ block_330
  def Row330't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle330 (r : &Row330't) -> bool = true
  fn process330 (r : &Row330't, dt : f32) -> bool = true
  fn score330 (r : &Row330't) -> f32 = 0.0
§ block_331
  def Row331't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle331 (r : &Row331't) -> bool = true
  fn process331 (r : &Row331't, dt : f32) -> bool = true
  fn score331 (r : &Row331't) -> f32 = 0.0
§ block_332
  def Row332't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle332 (r : &Row332't) -> bool = true
  fn process332 (r : &Row332't, dt : f32) -> bool = true
  fn score332 (r : &Row332't) -> f32 = 0.0
§ block_333
  def Row333't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle333 (r : &Row333't) -> bool = true
  fn process333 (r : &Row333't, dt : f32) -> bool = true
  fn score333 (r : &Row333't) -> f32 = 0.0
§ block_334
  def Row334't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle334 (r : &Row334't) -> bool = true
  fn process334 (r : &Row334't, dt : f32) -> bool = true
  fn score334 (r : &Row334't) -> f32 = 0.0
§ block_335
  def Row335't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle335 (r : &Row335't) -> bool = true
  fn process335 (r : &Row335't, dt : f32) -> bool = true
  fn score335 (r : &Row335't) -> f32 = 0.0
§ block_336
  def Row336't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle336 (r : &Row336't) -> bool = true
  fn process336 (r : &Row336't, dt : f32) -> bool = true
  fn score336 (r : &Row336't) -> f32 = 0.0
§ block_337
  def Row337't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle337 (r : &Row337't) -> bool = true
  fn process337 (r : &Row337't, dt : f32) -> bool = true
  fn score337 (r : &Row337't) -> f32 = 0.0
§ block_338
  def Row338't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle338 (r : &Row338't) -> bool = true
  fn process338 (r : &Row338't, dt : f32) -> bool = true
  fn score338 (r : &Row338't) -> f32 = 0.0
§ block_339
  def Row339't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle339 (r : &Row339't) -> bool = true
  fn process339 (r : &Row339't, dt : f32) -> bool = true
  fn score339 (r : &Row339't) -> f32 = 0.0
§ block_340
  def Row340't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle340 (r : &Row340't) -> bool = true
  fn process340 (r : &Row340't, dt : f32) -> bool = true
  fn score340 (r : &Row340't) -> f32 = 0.0
§ block_341
  def Row341't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle341 (r : &Row341't) -> bool = true
  fn process341 (r : &Row341't, dt : f32) -> bool = true
  fn score341 (r : &Row341't) -> f32 = 0.0
§ block_342
  def Row342't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle342 (r : &Row342't) -> bool = true
  fn process342 (r : &Row342't, dt : f32) -> bool = true
  fn score342 (r : &Row342't) -> f32 = 0.0
§ block_343
  def Row343't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle343 (r : &Row343't) -> bool = true
  fn process343 (r : &Row343't, dt : f32) -> bool = true
  fn score343 (r : &Row343't) -> f32 = 0.0
§ block_344
  def Row344't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle344 (r : &Row344't) -> bool = true
  fn process344 (r : &Row344't, dt : f32) -> bool = true
  fn score344 (r : &Row344't) -> f32 = 0.0
§ block_345
  def Row345't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle345 (r : &Row345't) -> bool = true
  fn process345 (r : &Row345't, dt : f32) -> bool = true
  fn score345 (r : &Row345't) -> f32 = 0.0
§ block_346
  def Row346't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle346 (r : &Row346't) -> bool = true
  fn process346 (r : &Row346't, dt : f32) -> bool = true
  fn score346 (r : &Row346't) -> f32 = 0.0
§ block_347
  def Row347't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle347 (r : &Row347't) -> bool = true
  fn process347 (r : &Row347't, dt : f32) -> bool = true
  fn score347 (r : &Row347't) -> f32 = 0.0
§ block_348
  def Row348't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle348 (r : &Row348't) -> bool = true
  fn process348 (r : &Row348't, dt : f32) -> bool = true
  fn score348 (r : &Row348't) -> f32 = 0.0
§ block_349
  def Row349't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle349 (r : &Row349't) -> bool = true
  fn process349 (r : &Row349't, dt : f32) -> bool = true
  fn score349 (r : &Row349't) -> f32 = 0.0
§ block_350
  def Row350't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle350 (r : &Row350't) -> bool = true
  fn process350 (r : &Row350't, dt : f32) -> bool = true
  fn score350 (r : &Row350't) -> f32 = 0.0
§ block_351
  def Row351't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle351 (r : &Row351't) -> bool = true
  fn process351 (r : &Row351't, dt : f32) -> bool = true
  fn score351 (r : &Row351't) -> f32 = 0.0
§ block_352
  def Row352't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle352 (r : &Row352't) -> bool = true
  fn process352 (r : &Row352't, dt : f32) -> bool = true
  fn score352 (r : &Row352't) -> f32 = 0.0
§ block_353
  def Row353't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle353 (r : &Row353't) -> bool = true
  fn process353 (r : &Row353't, dt : f32) -> bool = true
  fn score353 (r : &Row353't) -> f32 = 0.0
§ block_354
  def Row354't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle354 (r : &Row354't) -> bool = true
  fn process354 (r : &Row354't, dt : f32) -> bool = true
  fn score354 (r : &Row354't) -> f32 = 0.0
§ block_355
  def Row355't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle355 (r : &Row355't) -> bool = true
  fn process355 (r : &Row355't, dt : f32) -> bool = true
  fn score355 (r : &Row355't) -> f32 = 0.0
§ block_356
  def Row356't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle356 (r : &Row356't) -> bool = true
  fn process356 (r : &Row356't, dt : f32) -> bool = true
  fn score356 (r : &Row356't) -> f32 = 0.0
§ block_357
  def Row357't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle357 (r : &Row357't) -> bool = true
  fn process357 (r : &Row357't, dt : f32) -> bool = true
  fn score357 (r : &Row357't) -> f32 = 0.0
§ block_358
  def Row358't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle358 (r : &Row358't) -> bool = true
  fn process358 (r : &Row358't, dt : f32) -> bool = true
  fn score358 (r : &Row358't) -> f32 = 0.0
§ block_359
  def Row359't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle359 (r : &Row359't) -> bool = true
  fn process359 (r : &Row359't, dt : f32) -> bool = true
  fn score359 (r : &Row359't) -> f32 = 0.0
§ block_360
  def Row360't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle360 (r : &Row360't) -> bool = true
  fn process360 (r : &Row360't, dt : f32) -> bool = true
  fn score360 (r : &Row360't) -> f32 = 0.0
§ block_361
  def Row361't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle361 (r : &Row361't) -> bool = true
  fn process361 (r : &Row361't, dt : f32) -> bool = true
  fn score361 (r : &Row361't) -> f32 = 0.0
§ block_362
  def Row362't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle362 (r : &Row362't) -> bool = true
  fn process362 (r : &Row362't, dt : f32) -> bool = true
  fn score362 (r : &Row362't) -> f32 = 0.0
§ block_363
  def Row363't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle363 (r : &Row363't) -> bool = true
  fn process363 (r : &Row363't, dt : f32) -> bool = true
  fn score363 (r : &Row363't) -> f32 = 0.0
§ block_364
  def Row364't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle364 (r : &Row364't) -> bool = true
  fn process364 (r : &Row364't, dt : f32) -> bool = true
  fn score364 (r : &Row364't) -> f32 = 0.0
§ block_365
  def Row365't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle365 (r : &Row365't) -> bool = true
  fn process365 (r : &Row365't, dt : f32) -> bool = true
  fn score365 (r : &Row365't) -> f32 = 0.0
§ block_366
  def Row366't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle366 (r : &Row366't) -> bool = true
  fn process366 (r : &Row366't, dt : f32) -> bool = true
  fn score366 (r : &Row366't) -> f32 = 0.0
§ block_367
  def Row367't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle367 (r : &Row367't) -> bool = true
  fn process367 (r : &Row367't, dt : f32) -> bool = true
  fn score367 (r : &Row367't) -> f32 = 0.0
§ block_368
  def Row368't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle368 (r : &Row368't) -> bool = true
  fn process368 (r : &Row368't, dt : f32) -> bool = true
  fn score368 (r : &Row368't) -> f32 = 0.0
§ block_369
  def Row369't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle369 (r : &Row369't) -> bool = true
  fn process369 (r : &Row369't, dt : f32) -> bool = true
  fn score369 (r : &Row369't) -> f32 = 0.0
§ block_370
  def Row370't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle370 (r : &Row370't) -> bool = true
  fn process370 (r : &Row370't, dt : f32) -> bool = true
  fn score370 (r : &Row370't) -> f32 = 0.0
§ block_371
  def Row371't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle371 (r : &Row371't) -> bool = true
  fn process371 (r : &Row371't, dt : f32) -> bool = true
  fn score371 (r : &Row371't) -> f32 = 0.0
§ block_372
  def Row372't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle372 (r : &Row372't) -> bool = true
  fn process372 (r : &Row372't, dt : f32) -> bool = true
  fn score372 (r : &Row372't) -> f32 = 0.0
§ block_373
  def Row373't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle373 (r : &Row373't) -> bool = true
  fn process373 (r : &Row373't, dt : f32) -> bool = true
  fn score373 (r : &Row373't) -> f32 = 0.0
§ block_374
  def Row374't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle374 (r : &Row374't) -> bool = true
  fn process374 (r : &Row374't, dt : f32) -> bool = true
  fn score374 (r : &Row374't) -> f32 = 0.0
§ block_375
  def Row375't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle375 (r : &Row375't) -> bool = true
  fn process375 (r : &Row375't, dt : f32) -> bool = true
  fn score375 (r : &Row375't) -> f32 = 0.0
§ block_376
  def Row376't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle376 (r : &Row376't) -> bool = true
  fn process376 (r : &Row376't, dt : f32) -> bool = true
  fn score376 (r : &Row376't) -> f32 = 0.0
§ block_377
  def Row377't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle377 (r : &Row377't) -> bool = true
  fn process377 (r : &Row377't, dt : f32) -> bool = true
  fn score377 (r : &Row377't) -> f32 = 0.0
§ block_378
  def Row378't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle378 (r : &Row378't) -> bool = true
  fn process378 (r : &Row378't, dt : f32) -> bool = true
  fn score378 (r : &Row378't) -> f32 = 0.0
§ block_379
  def Row379't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle379 (r : &Row379't) -> bool = true
  fn process379 (r : &Row379't, dt : f32) -> bool = true
  fn score379 (r : &Row379't) -> f32 = 0.0
§ block_380
  def Row380't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle380 (r : &Row380't) -> bool = true
  fn process380 (r : &Row380't, dt : f32) -> bool = true
  fn score380 (r : &Row380't) -> f32 = 0.0
§ block_381
  def Row381't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle381 (r : &Row381't) -> bool = true
  fn process381 (r : &Row381't, dt : f32) -> bool = true
  fn score381 (r : &Row381't) -> f32 = 0.0
§ block_382
  def Row382't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle382 (r : &Row382't) -> bool = true
  fn process382 (r : &Row382't, dt : f32) -> bool = true
  fn score382 (r : &Row382't) -> f32 = 0.0
§ block_383
  def Row383't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle383 (r : &Row383't) -> bool = true
  fn process383 (r : &Row383't, dt : f32) -> bool = true
  fn score383 (r : &Row383't) -> f32 = 0.0
§ block_384
  def Row384't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle384 (r : &Row384't) -> bool = true
  fn process384 (r : &Row384't, dt : f32) -> bool = true
  fn score384 (r : &Row384't) -> f32 = 0.0
§ block_385
  def Row385't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle385 (r : &Row385't) -> bool = true
  fn process385 (r : &Row385't, dt : f32) -> bool = true
  fn score385 (r : &Row385't) -> f32 = 0.0
§ block_386
  def Row386't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle386 (r : &Row386't) -> bool = true
  fn process386 (r : &Row386't, dt : f32) -> bool = true
  fn score386 (r : &Row386't) -> f32 = 0.0
§ block_387
  def Row387't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle387 (r : &Row387't) -> bool = true
  fn process387 (r : &Row387't, dt : f32) -> bool = true
  fn score387 (r : &Row387't) -> f32 = 0.0
§ block_388
  def Row388't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle388 (r : &Row388't) -> bool = true
  fn process388 (r : &Row388't, dt : f32) -> bool = true
  fn score388 (r : &Row388't) -> f32 = 0.0
§ block_389
  def Row389't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle389 (r : &Row389't) -> bool = true
  fn process389 (r : &Row389't, dt : f32) -> bool = true
  fn score389 (r : &Row389't) -> f32 = 0.0
§ block_390
  def Row390't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle390 (r : &Row390't) -> bool = true
  fn process390 (r : &Row390't, dt : f32) -> bool = true
  fn score390 (r : &Row390't) -> f32 = 0.0
§ block_391
  def Row391't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle391 (r : &Row391't) -> bool = true
  fn process391 (r : &Row391't, dt : f32) -> bool = true
  fn score391 (r : &Row391't) -> f32 = 0.0
§ block_392
  def Row392't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle392 (r : &Row392't) -> bool = true
  fn process392 (r : &Row392't, dt : f32) -> bool = true
  fn score392 (r : &Row392't) -> f32 = 0.0
§ block_393
  def Row393't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle393 (r : &Row393't) -> bool = true
  fn process393 (r : &Row393't, dt : f32) -> bool = true
  fn score393 (r : &Row393't) -> f32 = 0.0
§ block_394
  def Row394't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle394 (r : &Row394't) -> bool = true
  fn process394 (r : &Row394't, dt : f32) -> bool = true
  fn score394 (r : &Row394't) -> f32 = 0.0
§ block_395
  def Row395't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle395 (r : &Row395't) -> bool = true
  fn process395 (r : &Row395't, dt : f32) -> bool = true
  fn score395 (r : &Row395't) -> f32 = 0.0
§ block_396
  def Row396't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle396 (r : &Row396't) -> bool = true
  fn process396 (r : &Row396't, dt : f32) -> bool = true
  fn score396 (r : &Row396't) -> f32 = 0.0
§ block_397
  def Row397't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle397 (r : &Row397't) -> bool = true
  fn process397 (r : &Row397't, dt : f32) -> bool = true
  fn score397 (r : &Row397't) -> f32 = 0.0
§ block_398
  def Row398't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle398 (r : &Row398't) -> bool = true
  fn process398 (r : &Row398't, dt : f32) -> bool = true
  fn score398 (r : &Row398't) -> f32 = 0.0
§ block_399
  def Row399't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle399 (r : &Row399't) -> bool = true
  fn process399 (r : &Row399't, dt : f32) -> bool = true
  fn score399 (r : &Row399't) -> f32 = 0.0
§ block_400
  def Row400't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle400 (r : &Row400't) -> bool = true
  fn process400 (r : &Row400't, dt : f32) -> bool = true
  fn score400 (r : &Row400't) -> f32 = 0.0
§ block_401
  def Row401't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle401 (r : &Row401't) -> bool = true
  fn process401 (r : &Row401't, dt : f32) -> bool = true
  fn score401 (r : &Row401't) -> f32 = 0.0
§ block_402
  def Row402't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle402 (r : &Row402't) -> bool = true
  fn process402 (r : &Row402't, dt : f32) -> bool = true
  fn score402 (r : &Row402't) -> f32 = 0.0
§ block_403
  def Row403't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle403 (r : &Row403't) -> bool = true
  fn process403 (r : &Row403't, dt : f32) -> bool = true
  fn score403 (r : &Row403't) -> f32 = 0.0
§ block_404
  def Row404't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle404 (r : &Row404't) -> bool = true
  fn process404 (r : &Row404't, dt : f32) -> bool = true
  fn score404 (r : &Row404't) -> f32 = 0.0
§ block_405
  def Row405't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle405 (r : &Row405't) -> bool = true
  fn process405 (r : &Row405't, dt : f32) -> bool = true
  fn score405 (r : &Row405't) -> f32 = 0.0
§ block_406
  def Row406't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle406 (r : &Row406't) -> bool = true
  fn process406 (r : &Row406't, dt : f32) -> bool = true
  fn score406 (r : &Row406't) -> f32 = 0.0
§ block_407
  def Row407't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle407 (r : &Row407't) -> bool = true
  fn process407 (r : &Row407't, dt : f32) -> bool = true
  fn score407 (r : &Row407't) -> f32 = 0.0
§ block_408
  def Row408't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle408 (r : &Row408't) -> bool = true
  fn process408 (r : &Row408't, dt : f32) -> bool = true
  fn score408 (r : &Row408't) -> f32 = 0.0
§ block_409
  def Row409't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle409 (r : &Row409't) -> bool = true
  fn process409 (r : &Row409't, dt : f32) -> bool = true
  fn score409 (r : &Row409't) -> f32 = 0.0
§ block_410
  def Row410't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle410 (r : &Row410't) -> bool = true
  fn process410 (r : &Row410't, dt : f32) -> bool = true
  fn score410 (r : &Row410't) -> f32 = 0.0
§ block_411
  def Row411't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle411 (r : &Row411't) -> bool = true
  fn process411 (r : &Row411't, dt : f32) -> bool = true
  fn score411 (r : &Row411't) -> f32 = 0.0
§ block_412
  def Row412't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle412 (r : &Row412't) -> bool = true
  fn process412 (r : &Row412't, dt : f32) -> bool = true
  fn score412 (r : &Row412't) -> f32 = 0.0
§ block_413
  def Row413't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle413 (r : &Row413't) -> bool = true
  fn process413 (r : &Row413't, dt : f32) -> bool = true
  fn score413 (r : &Row413't) -> f32 = 0.0
§ block_414
  def Row414't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle414 (r : &Row414't) -> bool = true
  fn process414 (r : &Row414't, dt : f32) -> bool = true
  fn score414 (r : &Row414't) -> f32 = 0.0
§ block_415
  def Row415't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle415 (r : &Row415't) -> bool = true
  fn process415 (r : &Row415't, dt : f32) -> bool = true
  fn score415 (r : &Row415't) -> f32 = 0.0
§ block_416
  def Row416't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle416 (r : &Row416't) -> bool = true
  fn process416 (r : &Row416't, dt : f32) -> bool = true
  fn score416 (r : &Row416't) -> f32 = 0.0
§ block_417
  def Row417't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle417 (r : &Row417't) -> bool = true
  fn process417 (r : &Row417't, dt : f32) -> bool = true
  fn score417 (r : &Row417't) -> f32 = 0.0
§ block_418
  def Row418't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle418 (r : &Row418't) -> bool = true
  fn process418 (r : &Row418't, dt : f32) -> bool = true
  fn score418 (r : &Row418't) -> f32 = 0.0
§ block_419
  def Row419't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle419 (r : &Row419't) -> bool = true
  fn process419 (r : &Row419't, dt : f32) -> bool = true
  fn score419 (r : &Row419't) -> f32 = 0.0
§ block_420
  def Row420't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle420 (r : &Row420't) -> bool = true
  fn process420 (r : &Row420't, dt : f32) -> bool = true
  fn score420 (r : &Row420't) -> f32 = 0.0
§ block_421
  def Row421't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle421 (r : &Row421't) -> bool = true
  fn process421 (r : &Row421't, dt : f32) -> bool = true
  fn score421 (r : &Row421't) -> f32 = 0.0
§ block_422
  def Row422't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle422 (r : &Row422't) -> bool = true
  fn process422 (r : &Row422't, dt : f32) -> bool = true
  fn score422 (r : &Row422't) -> f32 = 0.0
§ block_423
  def Row423't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle423 (r : &Row423't) -> bool = true
  fn process423 (r : &Row423't, dt : f32) -> bool = true
  fn score423 (r : &Row423't) -> f32 = 0.0
§ block_424
  def Row424't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle424 (r : &Row424't) -> bool = true
  fn process424 (r : &Row424't, dt : f32) -> bool = true
  fn score424 (r : &Row424't) -> f32 = 0.0
§ block_425
  def Row425't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle425 (r : &Row425't) -> bool = true
  fn process425 (r : &Row425't, dt : f32) -> bool = true
  fn score425 (r : &Row425't) -> f32 = 0.0
§ block_426
  def Row426't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle426 (r : &Row426't) -> bool = true
  fn process426 (r : &Row426't, dt : f32) -> bool = true
  fn score426 (r : &Row426't) -> f32 = 0.0
§ block_427
  def Row427't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle427 (r : &Row427't) -> bool = true
  fn process427 (r : &Row427't, dt : f32) -> bool = true
  fn score427 (r : &Row427't) -> f32 = 0.0
§ block_428
  def Row428't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle428 (r : &Row428't) -> bool = true
  fn process428 (r : &Row428't, dt : f32) -> bool = true
  fn score428 (r : &Row428't) -> f32 = 0.0
§ block_429
  def Row429't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle429 (r : &Row429't) -> bool = true
  fn process429 (r : &Row429't, dt : f32) -> bool = true
  fn score429 (r : &Row429't) -> f32 = 0.0
§ block_430
  def Row430't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle430 (r : &Row430't) -> bool = true
  fn process430 (r : &Row430't, dt : f32) -> bool = true
  fn score430 (r : &Row430't) -> f32 = 0.0
§ block_431
  def Row431't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle431 (r : &Row431't) -> bool = true
  fn process431 (r : &Row431't, dt : f32) -> bool = true
  fn score431 (r : &Row431't) -> f32 = 0.0
§ block_432
  def Row432't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle432 (r : &Row432't) -> bool = true
  fn process432 (r : &Row432't, dt : f32) -> bool = true
  fn score432 (r : &Row432't) -> f32 = 0.0
§ block_433
  def Row433't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle433 (r : &Row433't) -> bool = true
  fn process433 (r : &Row433't, dt : f32) -> bool = true
  fn score433 (r : &Row433't) -> f32 = 0.0
§ block_434
  def Row434't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle434 (r : &Row434't) -> bool = true
  fn process434 (r : &Row434't, dt : f32) -> bool = true
  fn score434 (r : &Row434't) -> f32 = 0.0
§ block_435
  def Row435't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle435 (r : &Row435't) -> bool = true
  fn process435 (r : &Row435't, dt : f32) -> bool = true
  fn score435 (r : &Row435't) -> f32 = 0.0
§ block_436
  def Row436't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle436 (r : &Row436't) -> bool = true
  fn process436 (r : &Row436't, dt : f32) -> bool = true
  fn score436 (r : &Row436't) -> f32 = 0.0
§ block_437
  def Row437't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle437 (r : &Row437't) -> bool = true
  fn process437 (r : &Row437't, dt : f32) -> bool = true
  fn score437 (r : &Row437't) -> f32 = 0.0
§ block_438
  def Row438't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle438 (r : &Row438't) -> bool = true
  fn process438 (r : &Row438't, dt : f32) -> bool = true
  fn score438 (r : &Row438't) -> f32 = 0.0
§ block_439
  def Row439't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle439 (r : &Row439't) -> bool = true
  fn process439 (r : &Row439't, dt : f32) -> bool = true
  fn score439 (r : &Row439't) -> f32 = 0.0
§ block_440
  def Row440't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle440 (r : &Row440't) -> bool = true
  fn process440 (r : &Row440't, dt : f32) -> bool = true
  fn score440 (r : &Row440't) -> f32 = 0.0
§ block_441
  def Row441't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle441 (r : &Row441't) -> bool = true
  fn process441 (r : &Row441't, dt : f32) -> bool = true
  fn score441 (r : &Row441't) -> f32 = 0.0
§ block_442
  def Row442't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle442 (r : &Row442't) -> bool = true
  fn process442 (r : &Row442't, dt : f32) -> bool = true
  fn score442 (r : &Row442't) -> f32 = 0.0
§ block_443
  def Row443't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle443 (r : &Row443't) -> bool = true
  fn process443 (r : &Row443't, dt : f32) -> bool = true
  fn score443 (r : &Row443't) -> f32 = 0.0
§ block_444
  def Row444't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle444 (r : &Row444't) -> bool = true
  fn process444 (r : &Row444't, dt : f32) -> bool = true
  fn score444 (r : &Row444't) -> f32 = 0.0
§ block_445
  def Row445't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle445 (r : &Row445't) -> bool = true
  fn process445 (r : &Row445't, dt : f32) -> bool = true
  fn score445 (r : &Row445't) -> f32 = 0.0
§ block_446
  def Row446't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle446 (r : &Row446't) -> bool = true
  fn process446 (r : &Row446't, dt : f32) -> bool = true
  fn score446 (r : &Row446't) -> f32 = 0.0
§ block_447
  def Row447't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle447 (r : &Row447't) -> bool = true
  fn process447 (r : &Row447't, dt : f32) -> bool = true
  fn score447 (r : &Row447't) -> f32 = 0.0
§ block_448
  def Row448't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle448 (r : &Row448't) -> bool = true
  fn process448 (r : &Row448't, dt : f32) -> bool = true
  fn score448 (r : &Row448't) -> f32 = 0.0
§ block_449
  def Row449't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle449 (r : &Row449't) -> bool = true
  fn process449 (r : &Row449't, dt : f32) -> bool = true
  fn score449 (r : &Row449't) -> f32 = 0.0
§ block_450
  def Row450't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle450 (r : &Row450't) -> bool = true
  fn process450 (r : &Row450't, dt : f32) -> bool = true
  fn score450 (r : &Row450't) -> f32 = 0.0
§ block_451
  def Row451't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle451 (r : &Row451't) -> bool = true
  fn process451 (r : &Row451't, dt : f32) -> bool = true
  fn score451 (r : &Row451't) -> f32 = 0.0
§ block_452
  def Row452't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle452 (r : &Row452't) -> bool = true
  fn process452 (r : &Row452't, dt : f32) -> bool = true
  fn score452 (r : &Row452't) -> f32 = 0.0
§ block_453
  def Row453't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle453 (r : &Row453't) -> bool = true
  fn process453 (r : &Row453't, dt : f32) -> bool = true
  fn score453 (r : &Row453't) -> f32 = 0.0
§ block_454
  def Row454't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle454 (r : &Row454't) -> bool = true
  fn process454 (r : &Row454't, dt : f32) -> bool = true
  fn score454 (r : &Row454't) -> f32 = 0.0
§ block_455
  def Row455't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle455 (r : &Row455't) -> bool = true
  fn process455 (r : &Row455't, dt : f32) -> bool = true
  fn score455 (r : &Row455't) -> f32 = 0.0
§ block_456
  def Row456't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle456 (r : &Row456't) -> bool = true
  fn process456 (r : &Row456't, dt : f32) -> bool = true
  fn score456 (r : &Row456't) -> f32 = 0.0
§ block_457
  def Row457't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle457 (r : &Row457't) -> bool = true
  fn process457 (r : &Row457't, dt : f32) -> bool = true
  fn score457 (r : &Row457't) -> f32 = 0.0
§ block_458
  def Row458't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle458 (r : &Row458't) -> bool = true
  fn process458 (r : &Row458't, dt : f32) -> bool = true
  fn score458 (r : &Row458't) -> f32 = 0.0
§ block_459
  def Row459't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle459 (r : &Row459't) -> bool = true
  fn process459 (r : &Row459't, dt : f32) -> bool = true
  fn score459 (r : &Row459't) -> f32 = 0.0
§ block_460
  def Row460't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle460 (r : &Row460't) -> bool = true
  fn process460 (r : &Row460't, dt : f32) -> bool = true
  fn score460 (r : &Row460't) -> f32 = 0.0
§ block_461
  def Row461't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle461 (r : &Row461't) -> bool = true
  fn process461 (r : &Row461't, dt : f32) -> bool = true
  fn score461 (r : &Row461't) -> f32 = 0.0
§ block_462
  def Row462't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle462 (r : &Row462't) -> bool = true
  fn process462 (r : &Row462't, dt : f32) -> bool = true
  fn score462 (r : &Row462't) -> f32 = 0.0
§ block_463
  def Row463't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle463 (r : &Row463't) -> bool = true
  fn process463 (r : &Row463't, dt : f32) -> bool = true
  fn score463 (r : &Row463't) -> f32 = 0.0
§ block_464
  def Row464't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle464 (r : &Row464't) -> bool = true
  fn process464 (r : &Row464't, dt : f32) -> bool = true
  fn score464 (r : &Row464't) -> f32 = 0.0
§ block_465
  def Row465't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle465 (r : &Row465't) -> bool = true
  fn process465 (r : &Row465't, dt : f32) -> bool = true
  fn score465 (r : &Row465't) -> f32 = 0.0
§ block_466
  def Row466't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle466 (r : &Row466't) -> bool = true
  fn process466 (r : &Row466't, dt : f32) -> bool = true
  fn score466 (r : &Row466't) -> f32 = 0.0
§ block_467
  def Row467't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle467 (r : &Row467't) -> bool = true
  fn process467 (r : &Row467't, dt : f32) -> bool = true
  fn score467 (r : &Row467't) -> f32 = 0.0
§ block_468
  def Row468't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle468 (r : &Row468't) -> bool = true
  fn process468 (r : &Row468't, dt : f32) -> bool = true
  fn score468 (r : &Row468't) -> f32 = 0.0
§ block_469
  def Row469't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle469 (r : &Row469't) -> bool = true
  fn process469 (r : &Row469't, dt : f32) -> bool = true
  fn score469 (r : &Row469't) -> f32 = 0.0
§ block_470
  def Row470't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle470 (r : &Row470't) -> bool = true
  fn process470 (r : &Row470't, dt : f32) -> bool = true
  fn score470 (r : &Row470't) -> f32 = 0.0
§ block_471
  def Row471't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle471 (r : &Row471't) -> bool = true
  fn process471 (r : &Row471't, dt : f32) -> bool = true
  fn score471 (r : &Row471't) -> f32 = 0.0
§ block_472
  def Row472't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle472 (r : &Row472't) -> bool = true
  fn process472 (r : &Row472't, dt : f32) -> bool = true
  fn score472 (r : &Row472't) -> f32 = 0.0
§ block_473
  def Row473't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle473 (r : &Row473't) -> bool = true
  fn process473 (r : &Row473't, dt : f32) -> bool = true
  fn score473 (r : &Row473't) -> f32 = 0.0
§ block_474
  def Row474't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle474 (r : &Row474't) -> bool = true
  fn process474 (r : &Row474't, dt : f32) -> bool = true
  fn score474 (r : &Row474't) -> f32 = 0.0
§ block_475
  def Row475't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle475 (r : &Row475't) -> bool = true
  fn process475 (r : &Row475't, dt : f32) -> bool = true
  fn score475 (r : &Row475't) -> f32 = 0.0
§ block_476
  def Row476't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle476 (r : &Row476't) -> bool = true
  fn process476 (r : &Row476't, dt : f32) -> bool = true
  fn score476 (r : &Row476't) -> f32 = 0.0
§ block_477
  def Row477't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle477 (r : &Row477't) -> bool = true
  fn process477 (r : &Row477't, dt : f32) -> bool = true
  fn score477 (r : &Row477't) -> f32 = 0.0
§ block_478
  def Row478't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle478 (r : &Row478't) -> bool = true
  fn process478 (r : &Row478't, dt : f32) -> bool = true
  fn score478 (r : &Row478't) -> f32 = 0.0
§ block_479
  def Row479't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle479 (r : &Row479't) -> bool = true
  fn process479 (r : &Row479't, dt : f32) -> bool = true
  fn score479 (r : &Row479't) -> f32 = 0.0
§ block_480
  def Row480't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle480 (r : &Row480't) -> bool = true
  fn process480 (r : &Row480't, dt : f32) -> bool = true
  fn score480 (r : &Row480't) -> f32 = 0.0
§ block_481
  def Row481't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle481 (r : &Row481't) -> bool = true
  fn process481 (r : &Row481't, dt : f32) -> bool = true
  fn score481 (r : &Row481't) -> f32 = 0.0
§ block_482
  def Row482't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle482 (r : &Row482't) -> bool = true
  fn process482 (r : &Row482't, dt : f32) -> bool = true
  fn score482 (r : &Row482't) -> f32 = 0.0
§ block_483
  def Row483't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle483 (r : &Row483't) -> bool = true
  fn process483 (r : &Row483't, dt : f32) -> bool = true
  fn score483 (r : &Row483't) -> f32 = 0.0
§ block_484
  def Row484't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle484 (r : &Row484't) -> bool = true
  fn process484 (r : &Row484't, dt : f32) -> bool = true
  fn score484 (r : &Row484't) -> f32 = 0.0
§ block_485
  def Row485't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle485 (r : &Row485't) -> bool = true
  fn process485 (r : &Row485't, dt : f32) -> bool = true
  fn score485 (r : &Row485't) -> f32 = 0.0
§ block_486
  def Row486't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle486 (r : &Row486't) -> bool = true
  fn process486 (r : &Row486't, dt : f32) -> bool = true
  fn score486 (r : &Row486't) -> f32 = 0.0
§ block_487
  def Row487't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle487 (r : &Row487't) -> bool = true
  fn process487 (r : &Row487't, dt : f32) -> bool = true
  fn score487 (r : &Row487't) -> f32 = 0.0
§ block_488
  def Row488't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle488 (r : &Row488't) -> bool = true
  fn process488 (r : &Row488't, dt : f32) -> bool = true
  fn score488 (r : &Row488't) -> f32 = 0.0
§ block_489
  def Row489't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle489 (r : &Row489't) -> bool = true
  fn process489 (r : &Row489't, dt : f32) -> bool = true
  fn score489 (r : &Row489't) -> f32 = 0.0
§ block_490
  def Row490't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle490 (r : &Row490't) -> bool = true
  fn process490 (r : &Row490't, dt : f32) -> bool = true
  fn score490 (r : &Row490't) -> f32 = 0.0
§ block_491
  def Row491't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle491 (r : &Row491't) -> bool = true
  fn process491 (r : &Row491't, dt : f32) -> bool = true
  fn score491 (r : &Row491't) -> f32 = 0.0
§ block_492
  def Row492't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle492 (r : &Row492't) -> bool = true
  fn process492 (r : &Row492't, dt : f32) -> bool = true
  fn score492 (r : &Row492't) -> f32 = 0.0
§ block_493
  def Row493't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle493 (r : &Row493't) -> bool = true
  fn process493 (r : &Row493't, dt : f32) -> bool = true
  fn score493 (r : &Row493't) -> f32 = 0.0
§ block_494
  def Row494't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle494 (r : &Row494't) -> bool = true
  fn process494 (r : &Row494't, dt : f32) -> bool = true
  fn score494 (r : &Row494't) -> f32 = 0.0
§ block_495
  def Row495't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle495 (r : &Row495't) -> bool = true
  fn process495 (r : &Row495't, dt : f32) -> bool = true
  fn score495 (r : &Row495't) -> f32 = 0.0
§ block_496
  def Row496't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle496 (r : &Row496't) -> bool = true
  fn process496 (r : &Row496't, dt : f32) -> bool = true
  fn score496 (r : &Row496't) -> f32 = 0.0
§ block_497
  def Row497't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle497 (r : &Row497't) -> bool = true
  fn process497 (r : &Row497't, dt : f32) -> bool = true
  fn score497 (r : &Row497't) -> f32 = 0.0
§ block_498
  def Row498't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle498 (r : &Row498't) -> bool = true
  fn process498 (r : &Row498't, dt : f32) -> bool = true
  fn score498 (r : &Row498't) -> f32 = 0.0
§ block_499
  def Row499't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle499 (r : &Row499't) -> bool = true
  fn process499 (r : &Row499't, dt : f32) -> bool = true
  fn score499 (r : &Row499't) -> f32 = 0.0
§ block_500
  def Row500't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle500 (r : &Row500't) -> bool = true
  fn process500 (r : &Row500't, dt : f32) -> bool = true
  fn score500 (r : &Row500't) -> f32 = 0.0
§ block_501
  def Row501't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle501 (r : &Row501't) -> bool = true
  fn process501 (r : &Row501't, dt : f32) -> bool = true
  fn score501 (r : &Row501't) -> f32 = 0.0
§ block_502
  def Row502't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle502 (r : &Row502't) -> bool = true
  fn process502 (r : &Row502't, dt : f32) -> bool = true
  fn score502 (r : &Row502't) -> f32 = 0.0
§ block_503
  def Row503't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle503 (r : &Row503't) -> bool = true
  fn process503 (r : &Row503't, dt : f32) -> bool = true
  fn score503 (r : &Row503't) -> f32 = 0.0
§ block_504
  def Row504't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle504 (r : &Row504't) -> bool = true
  fn process504 (r : &Row504't, dt : f32) -> bool = true
  fn score504 (r : &Row504't) -> f32 = 0.0
§ block_505
  def Row505't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle505 (r : &Row505't) -> bool = true
  fn process505 (r : &Row505't, dt : f32) -> bool = true
  fn score505 (r : &Row505't) -> f32 = 0.0
§ block_506
  def Row506't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle506 (r : &Row506't) -> bool = true
  fn process506 (r : &Row506't, dt : f32) -> bool = true
  fn score506 (r : &Row506't) -> f32 = 0.0
§ block_507
  def Row507't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle507 (r : &Row507't) -> bool = true
  fn process507 (r : &Row507't, dt : f32) -> bool = true
  fn score507 (r : &Row507't) -> f32 = 0.0
§ block_508
  def Row508't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle508 (r : &Row508't) -> bool = true
  fn process508 (r : &Row508't, dt : f32) -> bool = true
  fn score508 (r : &Row508't) -> f32 = 0.0
§ block_509
  def Row509't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle509 (r : &Row509't) -> bool = true
  fn process509 (r : &Row509't, dt : f32) -> bool = true
  fn score509 (r : &Row509't) -> f32 = 0.0
§ block_510
  def Row510't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle510 (r : &Row510't) -> bool = true
  fn process510 (r : &Row510't, dt : f32) -> bool = true
  fn score510 (r : &Row510't) -> f32 = 0.0
§ block_511
  def Row511't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle511 (r : &Row511't) -> bool = true
  fn process511 (r : &Row511't, dt : f32) -> bool = true
  fn score511 (r : &Row511't) -> f32 = 0.0
§ block_512
  def Row512't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle512 (r : &Row512't) -> bool = true
  fn process512 (r : &Row512't, dt : f32) -> bool = true
  fn score512 (r : &Row512't) -> f32 = 0.0
§ block_513
  def Row513't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle513 (r : &Row513't) -> bool = true
  fn process513 (r : &Row513't, dt : f32) -> bool = true
  fn score513 (r : &Row513't) -> f32 = 0.0
§ block_514
  def Row514't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle514 (r : &Row514't) -> bool = true
  fn process514 (r : &Row514't, dt : f32) -> bool = true
  fn score514 (r : &Row514't) -> f32 = 0.0
§ block_515
  def Row515't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle515 (r : &Row515't) -> bool = true
  fn process515 (r : &Row515't, dt : f32) -> bool = true
  fn score515 (r : &Row515't) -> f32 = 0.0
§ block_516
  def Row516't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle516 (r : &Row516't) -> bool = true
  fn process516 (r : &Row516't, dt : f32) -> bool = true
  fn score516 (r : &Row516't) -> f32 = 0.0
§ block_517
  def Row517't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle517 (r : &Row517't) -> bool = true
  fn process517 (r : &Row517't, dt : f32) -> bool = true
  fn score517 (r : &Row517't) -> f32 = 0.0
§ block_518
  def Row518't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle518 (r : &Row518't) -> bool = true
  fn process518 (r : &Row518't, dt : f32) -> bool = true
  fn score518 (r : &Row518't) -> f32 = 0.0
§ block_519
  def Row519't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle519 (r : &Row519't) -> bool = true
  fn process519 (r : &Row519't, dt : f32) -> bool = true
  fn score519 (r : &Row519't) -> f32 = 0.0
§ block_520
  def Row520't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle520 (r : &Row520't) -> bool = true
  fn process520 (r : &Row520't, dt : f32) -> bool = true
  fn score520 (r : &Row520't) -> f32 = 0.0
§ block_521
  def Row521't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle521 (r : &Row521't) -> bool = true
  fn process521 (r : &Row521't, dt : f32) -> bool = true
  fn score521 (r : &Row521't) -> f32 = 0.0
§ block_522
  def Row522't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle522 (r : &Row522't) -> bool = true
  fn process522 (r : &Row522't, dt : f32) -> bool = true
  fn score522 (r : &Row522't) -> f32 = 0.0
§ block_523
  def Row523't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle523 (r : &Row523't) -> bool = true
  fn process523 (r : &Row523't, dt : f32) -> bool = true
  fn score523 (r : &Row523't) -> f32 = 0.0
§ block_524
  def Row524't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle524 (r : &Row524't) -> bool = true
  fn process524 (r : &Row524't, dt : f32) -> bool = true
  fn score524 (r : &Row524't) -> f32 = 0.0
§ block_525
  def Row525't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle525 (r : &Row525't) -> bool = true
  fn process525 (r : &Row525't, dt : f32) -> bool = true
  fn score525 (r : &Row525't) -> f32 = 0.0
§ block_526
  def Row526't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle526 (r : &Row526't) -> bool = true
  fn process526 (r : &Row526't, dt : f32) -> bool = true
  fn score526 (r : &Row526't) -> f32 = 0.0
§ block_527
  def Row527't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle527 (r : &Row527't) -> bool = true
  fn process527 (r : &Row527't, dt : f32) -> bool = true
  fn score527 (r : &Row527't) -> f32 = 0.0
§ block_528
  def Row528't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle528 (r : &Row528't) -> bool = true
  fn process528 (r : &Row528't, dt : f32) -> bool = true
  fn score528 (r : &Row528't) -> f32 = 0.0
§ block_529
  def Row529't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle529 (r : &Row529't) -> bool = true
  fn process529 (r : &Row529't, dt : f32) -> bool = true
  fn score529 (r : &Row529't) -> f32 = 0.0
§ block_530
  def Row530't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle530 (r : &Row530't) -> bool = true
  fn process530 (r : &Row530't, dt : f32) -> bool = true
  fn score530 (r : &Row530't) -> f32 = 0.0
§ block_531
  def Row531't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle531 (r : &Row531't) -> bool = true
  fn process531 (r : &Row531't, dt : f32) -> bool = true
  fn score531 (r : &Row531't) -> f32 = 0.0
§ block_532
  def Row532't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle532 (r : &Row532't) -> bool = true
  fn process532 (r : &Row532't, dt : f32) -> bool = true
  fn score532 (r : &Row532't) -> f32 = 0.0
§ block_533
  def Row533't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle533 (r : &Row533't) -> bool = true
  fn process533 (r : &Row533't, dt : f32) -> bool = true
  fn score533 (r : &Row533't) -> f32 = 0.0
§ block_534
  def Row534't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle534 (r : &Row534't) -> bool = true
  fn process534 (r : &Row534't, dt : f32) -> bool = true
  fn score534 (r : &Row534't) -> f32 = 0.0
§ block_535
  def Row535't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle535 (r : &Row535't) -> bool = true
  fn process535 (r : &Row535't, dt : f32) -> bool = true
  fn score535 (r : &Row535't) -> f32 = 0.0
§ block_536
  def Row536't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle536 (r : &Row536't) -> bool = true
  fn process536 (r : &Row536't, dt : f32) -> bool = true
  fn score536 (r : &Row536't) -> f32 = 0.0
§ block_537
  def Row537't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle537 (r : &Row537't) -> bool = true
  fn process537 (r : &Row537't, dt : f32) -> bool = true
  fn score537 (r : &Row537't) -> f32 = 0.0
§ block_538
  def Row538't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle538 (r : &Row538't) -> bool = true
  fn process538 (r : &Row538't, dt : f32) -> bool = true
  fn score538 (r : &Row538't) -> f32 = 0.0
§ block_539
  def Row539't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle539 (r : &Row539't) -> bool = true
  fn process539 (r : &Row539't, dt : f32) -> bool = true
  fn score539 (r : &Row539't) -> f32 = 0.0
§ block_540
  def Row540't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle540 (r : &Row540't) -> bool = true
  fn process540 (r : &Row540't, dt : f32) -> bool = true
  fn score540 (r : &Row540't) -> f32 = 0.0
§ block_541
  def Row541't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle541 (r : &Row541't) -> bool = true
  fn process541 (r : &Row541't, dt : f32) -> bool = true
  fn score541 (r : &Row541't) -> f32 = 0.0
§ block_542
  def Row542't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle542 (r : &Row542't) -> bool = true
  fn process542 (r : &Row542't, dt : f32) -> bool = true
  fn score542 (r : &Row542't) -> f32 = 0.0
§ block_543
  def Row543't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle543 (r : &Row543't) -> bool = true
  fn process543 (r : &Row543't, dt : f32) -> bool = true
  fn score543 (r : &Row543't) -> f32 = 0.0
§ block_544
  def Row544't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle544 (r : &Row544't) -> bool = true
  fn process544 (r : &Row544't, dt : f32) -> bool = true
  fn score544 (r : &Row544't) -> f32 = 0.0
§ block_545
  def Row545't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle545 (r : &Row545't) -> bool = true
  fn process545 (r : &Row545't, dt : f32) -> bool = true
  fn score545 (r : &Row545't) -> f32 = 0.0
§ block_546
  def Row546't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle546 (r : &Row546't) -> bool = true
  fn process546 (r : &Row546't, dt : f32) -> bool = true
  fn score546 (r : &Row546't) -> f32 = 0.0
§ block_547
  def Row547't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle547 (r : &Row547't) -> bool = true
  fn process547 (r : &Row547't, dt : f32) -> bool = true
  fn score547 (r : &Row547't) -> f32 = 0.0
§ block_548
  def Row548't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle548 (r : &Row548't) -> bool = true
  fn process548 (r : &Row548't, dt : f32) -> bool = true
  fn score548 (r : &Row548't) -> f32 = 0.0
§ block_549
  def Row549't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle549 (r : &Row549't) -> bool = true
  fn process549 (r : &Row549't, dt : f32) -> bool = true
  fn score549 (r : &Row549't) -> f32 = 0.0
§ block_550
  def Row550't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle550 (r : &Row550't) -> bool = true
  fn process550 (r : &Row550't, dt : f32) -> bool = true
  fn score550 (r : &Row550't) -> f32 = 0.0
§ block_551
  def Row551't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle551 (r : &Row551't) -> bool = true
  fn process551 (r : &Row551't, dt : f32) -> bool = true
  fn score551 (r : &Row551't) -> f32 = 0.0
§ block_552
  def Row552't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle552 (r : &Row552't) -> bool = true
  fn process552 (r : &Row552't, dt : f32) -> bool = true
  fn score552 (r : &Row552't) -> f32 = 0.0
§ block_553
  def Row553't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle553 (r : &Row553't) -> bool = true
  fn process553 (r : &Row553't, dt : f32) -> bool = true
  fn score553 (r : &Row553't) -> f32 = 0.0
§ block_554
  def Row554't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle554 (r : &Row554't) -> bool = true
  fn process554 (r : &Row554't, dt : f32) -> bool = true
  fn score554 (r : &Row554't) -> f32 = 0.0
§ block_555
  def Row555't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle555 (r : &Row555't) -> bool = true
  fn process555 (r : &Row555't, dt : f32) -> bool = true
  fn score555 (r : &Row555't) -> f32 = 0.0
§ block_556
  def Row556't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle556 (r : &Row556't) -> bool = true
  fn process556 (r : &Row556't, dt : f32) -> bool = true
  fn score556 (r : &Row556't) -> f32 = 0.0
§ block_557
  def Row557't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle557 (r : &Row557't) -> bool = true
  fn process557 (r : &Row557't, dt : f32) -> bool = true
  fn score557 (r : &Row557't) -> f32 = 0.0
§ block_558
  def Row558't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle558 (r : &Row558't) -> bool = true
  fn process558 (r : &Row558't, dt : f32) -> bool = true
  fn score558 (r : &Row558't) -> f32 = 0.0
§ block_559
  def Row559't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle559 (r : &Row559't) -> bool = true
  fn process559 (r : &Row559't, dt : f32) -> bool = true
  fn score559 (r : &Row559't) -> f32 = 0.0
§ block_560
  def Row560't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle560 (r : &Row560't) -> bool = true
  fn process560 (r : &Row560't, dt : f32) -> bool = true
  fn score560 (r : &Row560't) -> f32 = 0.0
§ block_561
  def Row561't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle561 (r : &Row561't) -> bool = true
  fn process561 (r : &Row561't, dt : f32) -> bool = true
  fn score561 (r : &Row561't) -> f32 = 0.0
§ block_562
  def Row562't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle562 (r : &Row562't) -> bool = true
  fn process562 (r : &Row562't, dt : f32) -> bool = true
  fn score562 (r : &Row562't) -> f32 = 0.0
§ block_563
  def Row563't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle563 (r : &Row563't) -> bool = true
  fn process563 (r : &Row563't, dt : f32) -> bool = true
  fn score563 (r : &Row563't) -> f32 = 0.0
§ block_564
  def Row564't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle564 (r : &Row564't) -> bool = true
  fn process564 (r : &Row564't, dt : f32) -> bool = true
  fn score564 (r : &Row564't) -> f32 = 0.0
§ block_565
  def Row565't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle565 (r : &Row565't) -> bool = true
  fn process565 (r : &Row565't, dt : f32) -> bool = true
  fn score565 (r : &Row565't) -> f32 = 0.0
§ block_566
  def Row566't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle566 (r : &Row566't) -> bool = true
  fn process566 (r : &Row566't, dt : f32) -> bool = true
  fn score566 (r : &Row566't) -> f32 = 0.0
§ block_567
  def Row567't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle567 (r : &Row567't) -> bool = true
  fn process567 (r : &Row567't, dt : f32) -> bool = true
  fn score567 (r : &Row567't) -> f32 = 0.0
§ block_568
  def Row568't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle568 (r : &Row568't) -> bool = true
  fn process568 (r : &Row568't, dt : f32) -> bool = true
  fn score568 (r : &Row568't) -> f32 = 0.0
§ block_569
  def Row569't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle569 (r : &Row569't) -> bool = true
  fn process569 (r : &Row569't, dt : f32) -> bool = true
  fn score569 (r : &Row569't) -> f32 = 0.0
§ block_570
  def Row570't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle570 (r : &Row570't) -> bool = true
  fn process570 (r : &Row570't, dt : f32) -> bool = true
  fn score570 (r : &Row570't) -> f32 = 0.0
§ block_571
  def Row571't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle571 (r : &Row571't) -> bool = true
  fn process571 (r : &Row571't, dt : f32) -> bool = true
  fn score571 (r : &Row571't) -> f32 = 0.0
§ block_572
  def Row572't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle572 (r : &Row572't) -> bool = true
  fn process572 (r : &Row572't, dt : f32) -> bool = true
  fn score572 (r : &Row572't) -> f32 = 0.0
§ block_573
  def Row573't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle573 (r : &Row573't) -> bool = true
  fn process573 (r : &Row573't, dt : f32) -> bool = true
  fn score573 (r : &Row573't) -> f32 = 0.0
§ block_574
  def Row574't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle574 (r : &Row574't) -> bool = true
  fn process574 (r : &Row574't, dt : f32) -> bool = true
  fn score574 (r : &Row574't) -> f32 = 0.0
§ block_575
  def Row575't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle575 (r : &Row575't) -> bool = true
  fn process575 (r : &Row575't, dt : f32) -> bool = true
  fn score575 (r : &Row575't) -> f32 = 0.0
§ block_576
  def Row576't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle576 (r : &Row576't) -> bool = true
  fn process576 (r : &Row576't, dt : f32) -> bool = true
  fn score576 (r : &Row576't) -> f32 = 0.0
§ block_577
  def Row577't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle577 (r : &Row577't) -> bool = true
  fn process577 (r : &Row577't, dt : f32) -> bool = true
  fn score577 (r : &Row577't) -> f32 = 0.0
§ block_578
  def Row578't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle578 (r : &Row578't) -> bool = true
  fn process578 (r : &Row578't, dt : f32) -> bool = true
  fn score578 (r : &Row578't) -> f32 = 0.0
§ block_579
  def Row579't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle579 (r : &Row579't) -> bool = true
  fn process579 (r : &Row579't, dt : f32) -> bool = true
  fn score579 (r : &Row579't) -> f32 = 0.0
§ block_580
  def Row580't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle580 (r : &Row580't) -> bool = true
  fn process580 (r : &Row580't, dt : f32) -> bool = true
  fn score580 (r : &Row580't) -> f32 = 0.0
§ block_581
  def Row581't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle581 (r : &Row581't) -> bool = true
  fn process581 (r : &Row581't, dt : f32) -> bool = true
  fn score581 (r : &Row581't) -> f32 = 0.0
§ block_582
  def Row582't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle582 (r : &Row582't) -> bool = true
  fn process582 (r : &Row582't, dt : f32) -> bool = true
  fn score582 (r : &Row582't) -> f32 = 0.0
§ block_583
  def Row583't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle583 (r : &Row583't) -> bool = true
  fn process583 (r : &Row583't, dt : f32) -> bool = true
  fn score583 (r : &Row583't) -> f32 = 0.0
§ block_584
  def Row584't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle584 (r : &Row584't) -> bool = true
  fn process584 (r : &Row584't, dt : f32) -> bool = true
  fn score584 (r : &Row584't) -> f32 = 0.0
§ block_585
  def Row585't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle585 (r : &Row585't) -> bool = true
  fn process585 (r : &Row585't, dt : f32) -> bool = true
  fn score585 (r : &Row585't) -> f32 = 0.0
§ block_586
  def Row586't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle586 (r : &Row586't) -> bool = true
  fn process586 (r : &Row586't, dt : f32) -> bool = true
  fn score586 (r : &Row586't) -> f32 = 0.0
§ block_587
  def Row587't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle587 (r : &Row587't) -> bool = true
  fn process587 (r : &Row587't, dt : f32) -> bool = true
  fn score587 (r : &Row587't) -> f32 = 0.0
§ block_588
  def Row588't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle588 (r : &Row588't) -> bool = true
  fn process588 (r : &Row588't, dt : f32) -> bool = true
  fn score588 (r : &Row588't) -> f32 = 0.0
§ block_589
  def Row589't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle589 (r : &Row589't) -> bool = true
  fn process589 (r : &Row589't, dt : f32) -> bool = true
  fn score589 (r : &Row589't) -> f32 = 0.0
§ block_590
  def Row590't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle590 (r : &Row590't) -> bool = true
  fn process590 (r : &Row590't, dt : f32) -> bool = true
  fn score590 (r : &Row590't) -> f32 = 0.0
§ block_591
  def Row591't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle591 (r : &Row591't) -> bool = true
  fn process591 (r : &Row591't, dt : f32) -> bool = true
  fn score591 (r : &Row591't) -> f32 = 0.0
§ block_592
  def Row592't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle592 (r : &Row592't) -> bool = true
  fn process592 (r : &Row592't, dt : f32) -> bool = true
  fn score592 (r : &Row592't) -> f32 = 0.0
§ block_593
  def Row593't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle593 (r : &Row593't) -> bool = true
  fn process593 (r : &Row593't, dt : f32) -> bool = true
  fn score593 (r : &Row593't) -> f32 = 0.0
§ block_594
  def Row594't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle594 (r : &Row594't) -> bool = true
  fn process594 (r : &Row594't, dt : f32) -> bool = true
  fn score594 (r : &Row594't) -> f32 = 0.0
§ block_595
  def Row595't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle595 (r : &Row595't) -> bool = true
  fn process595 (r : &Row595't, dt : f32) -> bool = true
  fn score595 (r : &Row595't) -> f32 = 0.0
§ block_596
  def Row596't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle596 (r : &Row596't) -> bool = true
  fn process596 (r : &Row596't, dt : f32) -> bool = true
  fn score596 (r : &Row596't) -> f32 = 0.0
§ block_597
  def Row597't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle597 (r : &Row597't) -> bool = true
  fn process597 (r : &Row597't, dt : f32) -> bool = true
  fn score597 (r : &Row597't) -> f32 = 0.0
§ block_598
  def Row598't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle598 (r : &Row598't) -> bool = true
  fn process598 (r : &Row598't, dt : f32) -> bool = true
  fn score598 (r : &Row598't) -> f32 = 0.0
§ block_599
  def Row599't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle599 (r : &Row599't) -> bool = true
  fn process599 (r : &Row599't, dt : f32) -> bool = true
  fn score599 (r : &Row599't) -> f32 = 0.0
§ block_600
  def Row600't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle600 (r : &Row600't) -> bool = true
  fn process600 (r : &Row600't, dt : f32) -> bool = true
  fn score600 (r : &Row600't) -> f32 = 0.0
§ block_601
  def Row601't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle601 (r : &Row601't) -> bool = true
  fn process601 (r : &Row601't, dt : f32) -> bool = true
  fn score601 (r : &Row601't) -> f32 = 0.0
§ block_602
  def Row602't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle602 (r : &Row602't) -> bool = true
  fn process602 (r : &Row602't, dt : f32) -> bool = true
  fn score602 (r : &Row602't) -> f32 = 0.0
§ block_603
  def Row603't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle603 (r : &Row603't) -> bool = true
  fn process603 (r : &Row603't, dt : f32) -> bool = true
  fn score603 (r : &Row603't) -> f32 = 0.0
§ block_604
  def Row604't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle604 (r : &Row604't) -> bool = true
  fn process604 (r : &Row604't, dt : f32) -> bool = true
  fn score604 (r : &Row604't) -> f32 = 0.0
§ block_605
  def Row605't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle605 (r : &Row605't) -> bool = true
  fn process605 (r : &Row605't, dt : f32) -> bool = true
  fn score605 (r : &Row605't) -> f32 = 0.0
§ block_606
  def Row606't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle606 (r : &Row606't) -> bool = true
  fn process606 (r : &Row606't, dt : f32) -> bool = true
  fn score606 (r : &Row606't) -> f32 = 0.0
§ block_607
  def Row607't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle607 (r : &Row607't) -> bool = true
  fn process607 (r : &Row607't, dt : f32) -> bool = true
  fn score607 (r : &Row607't) -> f32 = 0.0
§ block_608
  def Row608't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle608 (r : &Row608't) -> bool = true
  fn process608 (r : &Row608't, dt : f32) -> bool = true
  fn score608 (r : &Row608't) -> f32 = 0.0
§ block_609
  def Row609't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle609 (r : &Row609't) -> bool = true
  fn process609 (r : &Row609't, dt : f32) -> bool = true
  fn score609 (r : &Row609't) -> f32 = 0.0
§ block_610
  def Row610't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle610 (r : &Row610't) -> bool = true
  fn process610 (r : &Row610't, dt : f32) -> bool = true
  fn score610 (r : &Row610't) -> f32 = 0.0
§ block_611
  def Row611't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle611 (r : &Row611't) -> bool = true
  fn process611 (r : &Row611't, dt : f32) -> bool = true
  fn score611 (r : &Row611't) -> f32 = 0.0
§ block_612
  def Row612't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle612 (r : &Row612't) -> bool = true
  fn process612 (r : &Row612't, dt : f32) -> bool = true
  fn score612 (r : &Row612't) -> f32 = 0.0
§ block_613
  def Row613't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle613 (r : &Row613't) -> bool = true
  fn process613 (r : &Row613't, dt : f32) -> bool = true
  fn score613 (r : &Row613't) -> f32 = 0.0
§ block_614
  def Row614't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle614 (r : &Row614't) -> bool = true
  fn process614 (r : &Row614't, dt : f32) -> bool = true
  fn score614 (r : &Row614't) -> f32 = 0.0
§ block_615
  def Row615't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle615 (r : &Row615't) -> bool = true
  fn process615 (r : &Row615't, dt : f32) -> bool = true
  fn score615 (r : &Row615't) -> f32 = 0.0
§ block_616
  def Row616't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle616 (r : &Row616't) -> bool = true
  fn process616 (r : &Row616't, dt : f32) -> bool = true
  fn score616 (r : &Row616't) -> f32 = 0.0
§ block_617
  def Row617't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle617 (r : &Row617't) -> bool = true
  fn process617 (r : &Row617't, dt : f32) -> bool = true
  fn score617 (r : &Row617't) -> f32 = 0.0
§ block_618
  def Row618't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle618 (r : &Row618't) -> bool = true
  fn process618 (r : &Row618't, dt : f32) -> bool = true
  fn score618 (r : &Row618't) -> f32 = 0.0
§ block_619
  def Row619't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle619 (r : &Row619't) -> bool = true
  fn process619 (r : &Row619't, dt : f32) -> bool = true
  fn score619 (r : &Row619't) -> f32 = 0.0
§ block_620
  def Row620't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle620 (r : &Row620't) -> bool = true
  fn process620 (r : &Row620't, dt : f32) -> bool = true
  fn score620 (r : &Row620't) -> f32 = 0.0
§ block_621
  def Row621't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle621 (r : &Row621't) -> bool = true
  fn process621 (r : &Row621't, dt : f32) -> bool = true
  fn score621 (r : &Row621't) -> f32 = 0.0
§ block_622
  def Row622't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle622 (r : &Row622't) -> bool = true
  fn process622 (r : &Row622't, dt : f32) -> bool = true
  fn score622 (r : &Row622't) -> f32 = 0.0
§ block_623
  def Row623't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle623 (r : &Row623't) -> bool = true
  fn process623 (r : &Row623't, dt : f32) -> bool = true
  fn score623 (r : &Row623't) -> f32 = 0.0
§ block_624
  def Row624't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle624 (r : &Row624't) -> bool = true
  fn process624 (r : &Row624't, dt : f32) -> bool = true
  fn score624 (r : &Row624't) -> f32 = 0.0
§ block_625
  def Row625't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle625 (r : &Row625't) -> bool = true
  fn process625 (r : &Row625't, dt : f32) -> bool = true
  fn score625 (r : &Row625't) -> f32 = 0.0
§ block_626
  def Row626't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle626 (r : &Row626't) -> bool = true
  fn process626 (r : &Row626't, dt : f32) -> bool = true
  fn score626 (r : &Row626't) -> f32 = 0.0
§ block_627
  def Row627't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle627 (r : &Row627't) -> bool = true
  fn process627 (r : &Row627't, dt : f32) -> bool = true
  fn score627 (r : &Row627't) -> f32 = 0.0
§ block_628
  def Row628't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle628 (r : &Row628't) -> bool = true
  fn process628 (r : &Row628't, dt : f32) -> bool = true
  fn score628 (r : &Row628't) -> f32 = 0.0
§ block_629
  def Row629't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle629 (r : &Row629't) -> bool = true
  fn process629 (r : &Row629't, dt : f32) -> bool = true
  fn score629 (r : &Row629't) -> f32 = 0.0
§ block_630
  def Row630't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle630 (r : &Row630't) -> bool = true
  fn process630 (r : &Row630't, dt : f32) -> bool = true
  fn score630 (r : &Row630't) -> f32 = 0.0
§ block_631
  def Row631't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle631 (r : &Row631't) -> bool = true
  fn process631 (r : &Row631't, dt : f32) -> bool = true
  fn score631 (r : &Row631't) -> f32 = 0.0
§ block_632
  def Row632't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle632 (r : &Row632't) -> bool = true
  fn process632 (r : &Row632't, dt : f32) -> bool = true
  fn score632 (r : &Row632't) -> f32 = 0.0
§ block_633
  def Row633't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle633 (r : &Row633't) -> bool = true
  fn process633 (r : &Row633't, dt : f32) -> bool = true
  fn score633 (r : &Row633't) -> f32 = 0.0
§ block_634
  def Row634't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle634 (r : &Row634't) -> bool = true
  fn process634 (r : &Row634't, dt : f32) -> bool = true
  fn score634 (r : &Row634't) -> f32 = 0.0
§ block_635
  def Row635't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle635 (r : &Row635't) -> bool = true
  fn process635 (r : &Row635't, dt : f32) -> bool = true
  fn score635 (r : &Row635't) -> f32 = 0.0
§ block_636
  def Row636't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle636 (r : &Row636't) -> bool = true
  fn process636 (r : &Row636't, dt : f32) -> bool = true
  fn score636 (r : &Row636't) -> f32 = 0.0
§ block_637
  def Row637't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle637 (r : &Row637't) -> bool = true
  fn process637 (r : &Row637't, dt : f32) -> bool = true
  fn score637 (r : &Row637't) -> f32 = 0.0
§ block_638
  def Row638't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle638 (r : &Row638't) -> bool = true
  fn process638 (r : &Row638't, dt : f32) -> bool = true
  fn score638 (r : &Row638't) -> f32 = 0.0
§ block_639
  def Row639't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle639 (r : &Row639't) -> bool = true
  fn process639 (r : &Row639't, dt : f32) -> bool = true
  fn score639 (r : &Row639't) -> f32 = 0.0
§ block_640
  def Row640't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle640 (r : &Row640't) -> bool = true
  fn process640 (r : &Row640't, dt : f32) -> bool = true
  fn score640 (r : &Row640't) -> f32 = 0.0
§ block_641
  def Row641't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle641 (r : &Row641't) -> bool = true
  fn process641 (r : &Row641't, dt : f32) -> bool = true
  fn score641 (r : &Row641't) -> f32 = 0.0
§ block_642
  def Row642't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle642 (r : &Row642't) -> bool = true
  fn process642 (r : &Row642't, dt : f32) -> bool = true
  fn score642 (r : &Row642't) -> f32 = 0.0
§ block_643
  def Row643't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle643 (r : &Row643't) -> bool = true
  fn process643 (r : &Row643't, dt : f32) -> bool = true
  fn score643 (r : &Row643't) -> f32 = 0.0
§ block_644
  def Row644't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle644 (r : &Row644't) -> bool = true
  fn process644 (r : &Row644't, dt : f32) -> bool = true
  fn score644 (r : &Row644't) -> f32 = 0.0
§ block_645
  def Row645't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle645 (r : &Row645't) -> bool = true
  fn process645 (r : &Row645't, dt : f32) -> bool = true
  fn score645 (r : &Row645't) -> f32 = 0.0
§ block_646
  def Row646't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle646 (r : &Row646't) -> bool = true
  fn process646 (r : &Row646't, dt : f32) -> bool = true
  fn score646 (r : &Row646't) -> f32 = 0.0
§ block_647
  def Row647't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle647 (r : &Row647't) -> bool = true
  fn process647 (r : &Row647't, dt : f32) -> bool = true
  fn score647 (r : &Row647't) -> f32 = 0.0
§ block_648
  def Row648't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle648 (r : &Row648't) -> bool = true
  fn process648 (r : &Row648't, dt : f32) -> bool = true
  fn score648 (r : &Row648't) -> f32 = 0.0
§ block_649
  def Row649't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle649 (r : &Row649't) -> bool = true
  fn process649 (r : &Row649't, dt : f32) -> bool = true
  fn score649 (r : &Row649't) -> f32 = 0.0
§ block_650
  def Row650't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle650 (r : &Row650't) -> bool = true
  fn process650 (r : &Row650't, dt : f32) -> bool = true
  fn score650 (r : &Row650't) -> f32 = 0.0
§ block_651
  def Row651't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle651 (r : &Row651't) -> bool = true
  fn process651 (r : &Row651't, dt : f32) -> bool = true
  fn score651 (r : &Row651't) -> f32 = 0.0
§ block_652
  def Row652't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle652 (r : &Row652't) -> bool = true
  fn process652 (r : &Row652't, dt : f32) -> bool = true
  fn score652 (r : &Row652't) -> f32 = 0.0
§ block_653
  def Row653't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle653 (r : &Row653't) -> bool = true
  fn process653 (r : &Row653't, dt : f32) -> bool = true
  fn score653 (r : &Row653't) -> f32 = 0.0
§ block_654
  def Row654't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle654 (r : &Row654't) -> bool = true
  fn process654 (r : &Row654't, dt : f32) -> bool = true
  fn score654 (r : &Row654't) -> f32 = 0.0
§ block_655
  def Row655't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle655 (r : &Row655't) -> bool = true
  fn process655 (r : &Row655't, dt : f32) -> bool = true
  fn score655 (r : &Row655't) -> f32 = 0.0
§ block_656
  def Row656't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle656 (r : &Row656't) -> bool = true
  fn process656 (r : &Row656't, dt : f32) -> bool = true
  fn score656 (r : &Row656't) -> f32 = 0.0
§ block_657
  def Row657't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle657 (r : &Row657't) -> bool = true
  fn process657 (r : &Row657't, dt : f32) -> bool = true
  fn score657 (r : &Row657't) -> f32 = 0.0
§ block_658
  def Row658't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle658 (r : &Row658't) -> bool = true
  fn process658 (r : &Row658't, dt : f32) -> bool = true
  fn score658 (r : &Row658't) -> f32 = 0.0
§ block_659
  def Row659't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle659 (r : &Row659't) -> bool = true
  fn process659 (r : &Row659't, dt : f32) -> bool = true
  fn score659 (r : &Row659't) -> f32 = 0.0
§ block_660
  def Row660't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle660 (r : &Row660't) -> bool = true
  fn process660 (r : &Row660't, dt : f32) -> bool = true
  fn score660 (r : &Row660't) -> f32 = 0.0
§ block_661
  def Row661't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle661 (r : &Row661't) -> bool = true
  fn process661 (r : &Row661't, dt : f32) -> bool = true
  fn score661 (r : &Row661't) -> f32 = 0.0
§ block_662
  def Row662't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle662 (r : &Row662't) -> bool = true
  fn process662 (r : &Row662't, dt : f32) -> bool = true
  fn score662 (r : &Row662't) -> f32 = 0.0
§ block_663
  def Row663't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle663 (r : &Row663't) -> bool = true
  fn process663 (r : &Row663't, dt : f32) -> bool = true
  fn score663 (r : &Row663't) -> f32 = 0.0
§ block_664
  def Row664't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle664 (r : &Row664't) -> bool = true
  fn process664 (r : &Row664't, dt : f32) -> bool = true
  fn score664 (r : &Row664't) -> f32 = 0.0
§ block_665
  def Row665't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle665 (r : &Row665't) -> bool = true
  fn process665 (r : &Row665't, dt : f32) -> bool = true
  fn score665 (r : &Row665't) -> f32 = 0.0
§ block_666
  def Row666't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle666 (r : &Row666't) -> bool = true
  fn process666 (r : &Row666't, dt : f32) -> bool = true
  fn score666 (r : &Row666't) -> f32 = 0.0
§ block_667
  def Row667't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle667 (r : &Row667't) -> bool = true
  fn process667 (r : &Row667't, dt : f32) -> bool = true
  fn score667 (r : &Row667't) -> f32 = 0.0
§ block_668
  def Row668't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle668 (r : &Row668't) -> bool = true
  fn process668 (r : &Row668't, dt : f32) -> bool = true
  fn score668 (r : &Row668't) -> f32 = 0.0
§ block_669
  def Row669't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle669 (r : &Row669't) -> bool = true
  fn process669 (r : &Row669't, dt : f32) -> bool = true
  fn score669 (r : &Row669't) -> f32 = 0.0
§ block_670
  def Row670't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle670 (r : &Row670't) -> bool = true
  fn process670 (r : &Row670't, dt : f32) -> bool = true
  fn score670 (r : &Row670't) -> f32 = 0.0
§ block_671
  def Row671't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle671 (r : &Row671't) -> bool = true
  fn process671 (r : &Row671't, dt : f32) -> bool = true
  fn score671 (r : &Row671't) -> f32 = 0.0
§ block_672
  def Row672't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle672 (r : &Row672't) -> bool = true
  fn process672 (r : &Row672't, dt : f32) -> bool = true
  fn score672 (r : &Row672't) -> f32 = 0.0
§ block_673
  def Row673't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle673 (r : &Row673't) -> bool = true
  fn process673 (r : &Row673't, dt : f32) -> bool = true
  fn score673 (r : &Row673't) -> f32 = 0.0
§ block_674
  def Row674't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle674 (r : &Row674't) -> bool = true
  fn process674 (r : &Row674't, dt : f32) -> bool = true
  fn score674 (r : &Row674't) -> f32 = 0.0
§ block_675
  def Row675't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle675 (r : &Row675't) -> bool = true
  fn process675 (r : &Row675't, dt : f32) -> bool = true
  fn score675 (r : &Row675't) -> f32 = 0.0
§ block_676
  def Row676't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle676 (r : &Row676't) -> bool = true
  fn process676 (r : &Row676't, dt : f32) -> bool = true
  fn score676 (r : &Row676't) -> f32 = 0.0
§ block_677
  def Row677't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle677 (r : &Row677't) -> bool = true
  fn process677 (r : &Row677't, dt : f32) -> bool = true
  fn score677 (r : &Row677't) -> f32 = 0.0
§ block_678
  def Row678't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle678 (r : &Row678't) -> bool = true
  fn process678 (r : &Row678't, dt : f32) -> bool = true
  fn score678 (r : &Row678't) -> f32 = 0.0
§ block_679
  def Row679't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle679 (r : &Row679't) -> bool = true
  fn process679 (r : &Row679't, dt : f32) -> bool = true
  fn score679 (r : &Row679't) -> f32 = 0.0
§ block_680
  def Row680't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle680 (r : &Row680't) -> bool = true
  fn process680 (r : &Row680't, dt : f32) -> bool = true
  fn score680 (r : &Row680't) -> f32 = 0.0
§ block_681
  def Row681't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle681 (r : &Row681't) -> bool = true
  fn process681 (r : &Row681't, dt : f32) -> bool = true
  fn score681 (r : &Row681't) -> f32 = 0.0
§ block_682
  def Row682't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle682 (r : &Row682't) -> bool = true
  fn process682 (r : &Row682't, dt : f32) -> bool = true
  fn score682 (r : &Row682't) -> f32 = 0.0
§ block_683
  def Row683't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle683 (r : &Row683't) -> bool = true
  fn process683 (r : &Row683't, dt : f32) -> bool = true
  fn score683 (r : &Row683't) -> f32 = 0.0
§ block_684
  def Row684't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle684 (r : &Row684't) -> bool = true
  fn process684 (r : &Row684't, dt : f32) -> bool = true
  fn score684 (r : &Row684't) -> f32 = 0.0
§ block_685
  def Row685't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle685 (r : &Row685't) -> bool = true
  fn process685 (r : &Row685't, dt : f32) -> bool = true
  fn score685 (r : &Row685't) -> f32 = 0.0
§ block_686
  def Row686't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle686 (r : &Row686't) -> bool = true
  fn process686 (r : &Row686't, dt : f32) -> bool = true
  fn score686 (r : &Row686't) -> f32 = 0.0
§ block_687
  def Row687't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle687 (r : &Row687't) -> bool = true
  fn process687 (r : &Row687't, dt : f32) -> bool = true
  fn score687 (r : &Row687't) -> f32 = 0.0
§ block_688
  def Row688't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle688 (r : &Row688't) -> bool = true
  fn process688 (r : &Row688't, dt : f32) -> bool = true
  fn score688 (r : &Row688't) -> f32 = 0.0
§ block_689
  def Row689't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle689 (r : &Row689't) -> bool = true
  fn process689 (r : &Row689't, dt : f32) -> bool = true
  fn score689 (r : &Row689't) -> f32 = 0.0
§ block_690
  def Row690't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle690 (r : &Row690't) -> bool = true
  fn process690 (r : &Row690't, dt : f32) -> bool = true
  fn score690 (r : &Row690't) -> f32 = 0.0
§ block_691
  def Row691't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle691 (r : &Row691't) -> bool = true
  fn process691 (r : &Row691't, dt : f32) -> bool = true
  fn score691 (r : &Row691't) -> f32 = 0.0
§ block_692
  def Row692't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle692 (r : &Row692't) -> bool = true
  fn process692 (r : &Row692't, dt : f32) -> bool = true
  fn score692 (r : &Row692't) -> f32 = 0.0
§ block_693
  def Row693't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle693 (r : &Row693't) -> bool = true
  fn process693 (r : &Row693't, dt : f32) -> bool = true
  fn score693 (r : &Row693't) -> f32 = 0.0
§ block_694
  def Row694't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle694 (r : &Row694't) -> bool = true
  fn process694 (r : &Row694't, dt : f32) -> bool = true
  fn score694 (r : &Row694't) -> f32 = 0.0
§ block_695
  def Row695't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle695 (r : &Row695't) -> bool = true
  fn process695 (r : &Row695't, dt : f32) -> bool = true
  fn score695 (r : &Row695't) -> f32 = 0.0
§ block_696
  def Row696't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle696 (r : &Row696't) -> bool = true
  fn process696 (r : &Row696't, dt : f32) -> bool = true
  fn score696 (r : &Row696't) -> f32 = 0.0
§ block_697
  def Row697't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle697 (r : &Row697't) -> bool = true
  fn process697 (r : &Row697't, dt : f32) -> bool = true
  fn score697 (r : &Row697't) -> f32 = 0.0
§ block_698
  def Row698't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle698 (r : &Row698't) -> bool = true
  fn process698 (r : &Row698't, dt : f32) -> bool = true
  fn score698 (r : &Row698't) -> f32 = 0.0
§ block_699
  def Row699't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle699 (r : &Row699't) -> bool = true
  fn process699 (r : &Row699't, dt : f32) -> bool = true
  fn score699 (r : &Row699't) -> f32 = 0.0
§ block_700
  def Row700't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle700 (r : &Row700't) -> bool = true
  fn process700 (r : &Row700't, dt : f32) -> bool = true
  fn score700 (r : &Row700't) -> f32 = 0.0
§ block_701
  def Row701't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle701 (r : &Row701't) -> bool = true
  fn process701 (r : &Row701't, dt : f32) -> bool = true
  fn score701 (r : &Row701't) -> f32 = 0.0
§ block_702
  def Row702't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle702 (r : &Row702't) -> bool = true
  fn process702 (r : &Row702't, dt : f32) -> bool = true
  fn score702 (r : &Row702't) -> f32 = 0.0
§ block_703
  def Row703't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle703 (r : &Row703't) -> bool = true
  fn process703 (r : &Row703't, dt : f32) -> bool = true
  fn score703 (r : &Row703't) -> f32 = 0.0
§ block_704
  def Row704't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle704 (r : &Row704't) -> bool = true
  fn process704 (r : &Row704't, dt : f32) -> bool = true
  fn score704 (r : &Row704't) -> f32 = 0.0
§ block_705
  def Row705't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle705 (r : &Row705't) -> bool = true
  fn process705 (r : &Row705't, dt : f32) -> bool = true
  fn score705 (r : &Row705't) -> f32 = 0.0
§ block_706
  def Row706't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle706 (r : &Row706't) -> bool = true
  fn process706 (r : &Row706't, dt : f32) -> bool = true
  fn score706 (r : &Row706't) -> f32 = 0.0
§ block_707
  def Row707't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle707 (r : &Row707't) -> bool = true
  fn process707 (r : &Row707't, dt : f32) -> bool = true
  fn score707 (r : &Row707't) -> f32 = 0.0
§ block_708
  def Row708't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle708 (r : &Row708't) -> bool = true
  fn process708 (r : &Row708't, dt : f32) -> bool = true
  fn score708 (r : &Row708't) -> f32 = 0.0
§ block_709
  def Row709't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle709 (r : &Row709't) -> bool = true
  fn process709 (r : &Row709't, dt : f32) -> bool = true
  fn score709 (r : &Row709't) -> f32 = 0.0
§ block_710
  def Row710't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle710 (r : &Row710't) -> bool = true
  fn process710 (r : &Row710't, dt : f32) -> bool = true
  fn score710 (r : &Row710't) -> f32 = 0.0
§ block_711
  def Row711't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle711 (r : &Row711't) -> bool = true
  fn process711 (r : &Row711't, dt : f32) -> bool = true
  fn score711 (r : &Row711't) -> f32 = 0.0
§ block_712
  def Row712't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle712 (r : &Row712't) -> bool = true
  fn process712 (r : &Row712't, dt : f32) -> bool = true
  fn score712 (r : &Row712't) -> f32 = 0.0
§ block_713
  def Row713't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle713 (r : &Row713't) -> bool = true
  fn process713 (r : &Row713't, dt : f32) -> bool = true
  fn score713 (r : &Row713't) -> f32 = 0.0
§ block_714
  def Row714't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle714 (r : &Row714't) -> bool = true
  fn process714 (r : &Row714't, dt : f32) -> bool = true
  fn score714 (r : &Row714't) -> f32 = 0.0
§ block_715
  def Row715't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle715 (r : &Row715't) -> bool = true
  fn process715 (r : &Row715't, dt : f32) -> bool = true
  fn score715 (r : &Row715't) -> f32 = 0.0
§ block_716
  def Row716't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle716 (r : &Row716't) -> bool = true
  fn process716 (r : &Row716't, dt : f32) -> bool = true
  fn score716 (r : &Row716't) -> f32 = 0.0
§ block_717
  def Row717't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle717 (r : &Row717't) -> bool = true
  fn process717 (r : &Row717't, dt : f32) -> bool = true
  fn score717 (r : &Row717't) -> f32 = 0.0
§ block_718
  def Row718't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle718 (r : &Row718't) -> bool = true
  fn process718 (r : &Row718't, dt : f32) -> bool = true
  fn score718 (r : &Row718't) -> f32 = 0.0
§ block_719
  def Row719't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle719 (r : &Row719't) -> bool = true
  fn process719 (r : &Row719't, dt : f32) -> bool = true
  fn score719 (r : &Row719't) -> f32 = 0.0
§ block_720
  def Row720't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle720 (r : &Row720't) -> bool = true
  fn process720 (r : &Row720't, dt : f32) -> bool = true
  fn score720 (r : &Row720't) -> f32 = 0.0
§ block_721
  def Row721't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle721 (r : &Row721't) -> bool = true
  fn process721 (r : &Row721't, dt : f32) -> bool = true
  fn score721 (r : &Row721't) -> f32 = 0.0
§ block_722
  def Row722't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle722 (r : &Row722't) -> bool = true
  fn process722 (r : &Row722't, dt : f32) -> bool = true
  fn score722 (r : &Row722't) -> f32 = 0.0
§ block_723
  def Row723't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle723 (r : &Row723't) -> bool = true
  fn process723 (r : &Row723't, dt : f32) -> bool = true
  fn score723 (r : &Row723't) -> f32 = 0.0
§ block_724
  def Row724't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle724 (r : &Row724't) -> bool = true
  fn process724 (r : &Row724't, dt : f32) -> bool = true
  fn score724 (r : &Row724't) -> f32 = 0.0
§ block_725
  def Row725't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle725 (r : &Row725't) -> bool = true
  fn process725 (r : &Row725't, dt : f32) -> bool = true
  fn score725 (r : &Row725't) -> f32 = 0.0
§ block_726
  def Row726't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle726 (r : &Row726't) -> bool = true
  fn process726 (r : &Row726't, dt : f32) -> bool = true
  fn score726 (r : &Row726't) -> f32 = 0.0
§ block_727
  def Row727't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle727 (r : &Row727't) -> bool = true
  fn process727 (r : &Row727't, dt : f32) -> bool = true
  fn score727 (r : &Row727't) -> f32 = 0.0
§ block_728
  def Row728't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle728 (r : &Row728't) -> bool = true
  fn process728 (r : &Row728't, dt : f32) -> bool = true
  fn score728 (r : &Row728't) -> f32 = 0.0
§ block_729
  def Row729't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle729 (r : &Row729't) -> bool = true
  fn process729 (r : &Row729't, dt : f32) -> bool = true
  fn score729 (r : &Row729't) -> f32 = 0.0
§ block_730
  def Row730't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle730 (r : &Row730't) -> bool = true
  fn process730 (r : &Row730't, dt : f32) -> bool = true
  fn score730 (r : &Row730't) -> f32 = 0.0
§ block_731
  def Row731't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle731 (r : &Row731't) -> bool = true
  fn process731 (r : &Row731't, dt : f32) -> bool = true
  fn score731 (r : &Row731't) -> f32 = 0.0
§ block_732
  def Row732't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle732 (r : &Row732't) -> bool = true
  fn process732 (r : &Row732't, dt : f32) -> bool = true
  fn score732 (r : &Row732't) -> f32 = 0.0
§ block_733
  def Row733't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle733 (r : &Row733't) -> bool = true
  fn process733 (r : &Row733't, dt : f32) -> bool = true
  fn score733 (r : &Row733't) -> f32 = 0.0
§ block_734
  def Row734't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle734 (r : &Row734't) -> bool = true
  fn process734 (r : &Row734't, dt : f32) -> bool = true
  fn score734 (r : &Row734't) -> f32 = 0.0
§ block_735
  def Row735't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle735 (r : &Row735't) -> bool = true
  fn process735 (r : &Row735't, dt : f32) -> bool = true
  fn score735 (r : &Row735't) -> f32 = 0.0
§ block_736
  def Row736't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle736 (r : &Row736't) -> bool = true
  fn process736 (r : &Row736't, dt : f32) -> bool = true
  fn score736 (r : &Row736't) -> f32 = 0.0
§ block_737
  def Row737't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle737 (r : &Row737't) -> bool = true
  fn process737 (r : &Row737't, dt : f32) -> bool = true
  fn score737 (r : &Row737't) -> f32 = 0.0
§ block_738
  def Row738't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle738 (r : &Row738't) -> bool = true
  fn process738 (r : &Row738't, dt : f32) -> bool = true
  fn score738 (r : &Row738't) -> f32 = 0.0
§ block_739
  def Row739't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle739 (r : &Row739't) -> bool = true
  fn process739 (r : &Row739't, dt : f32) -> bool = true
  fn score739 (r : &Row739't) -> f32 = 0.0
§ block_740
  def Row740't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle740 (r : &Row740't) -> bool = true
  fn process740 (r : &Row740't, dt : f32) -> bool = true
  fn score740 (r : &Row740't) -> f32 = 0.0
§ block_741
  def Row741't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle741 (r : &Row741't) -> bool = true
  fn process741 (r : &Row741't, dt : f32) -> bool = true
  fn score741 (r : &Row741't) -> f32 = 0.0
§ block_742
  def Row742't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle742 (r : &Row742't) -> bool = true
  fn process742 (r : &Row742't, dt : f32) -> bool = true
  fn score742 (r : &Row742't) -> f32 = 0.0
§ block_743
  def Row743't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle743 (r : &Row743't) -> bool = true
  fn process743 (r : &Row743't, dt : f32) -> bool = true
  fn score743 (r : &Row743't) -> f32 = 0.0
§ block_744
  def Row744't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle744 (r : &Row744't) -> bool = true
  fn process744 (r : &Row744't, dt : f32) -> bool = true
  fn score744 (r : &Row744't) -> f32 = 0.0
§ block_745
  def Row745't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle745 (r : &Row745't) -> bool = true
  fn process745 (r : &Row745't, dt : f32) -> bool = true
  fn score745 (r : &Row745't) -> f32 = 0.0
§ block_746
  def Row746't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle746 (r : &Row746't) -> bool = true
  fn process746 (r : &Row746't, dt : f32) -> bool = true
  fn score746 (r : &Row746't) -> f32 = 0.0
§ block_747
  def Row747't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle747 (r : &Row747't) -> bool = true
  fn process747 (r : &Row747't, dt : f32) -> bool = true
  fn score747 (r : &Row747't) -> f32 = 0.0
§ block_748
  def Row748't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle748 (r : &Row748't) -> bool = true
  fn process748 (r : &Row748't, dt : f32) -> bool = true
  fn score748 (r : &Row748't) -> f32 = 0.0
§ block_749
  def Row749't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle749 (r : &Row749't) -> bool = true
  fn process749 (r : &Row749't, dt : f32) -> bool = true
  fn score749 (r : &Row749't) -> f32 = 0.0
§ block_750
  def Row750't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle750 (r : &Row750't) -> bool = true
  fn process750 (r : &Row750't, dt : f32) -> bool = true
  fn score750 (r : &Row750't) -> f32 = 0.0
§ block_751
  def Row751't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle751 (r : &Row751't) -> bool = true
  fn process751 (r : &Row751't, dt : f32) -> bool = true
  fn score751 (r : &Row751't) -> f32 = 0.0
§ block_752
  def Row752't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle752 (r : &Row752't) -> bool = true
  fn process752 (r : &Row752't, dt : f32) -> bool = true
  fn score752 (r : &Row752't) -> f32 = 0.0
§ block_753
  def Row753't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle753 (r : &Row753't) -> bool = true
  fn process753 (r : &Row753't, dt : f32) -> bool = true
  fn score753 (r : &Row753't) -> f32 = 0.0
§ block_754
  def Row754't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle754 (r : &Row754't) -> bool = true
  fn process754 (r : &Row754't, dt : f32) -> bool = true
  fn score754 (r : &Row754't) -> f32 = 0.0
§ block_755
  def Row755't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle755 (r : &Row755't) -> bool = true
  fn process755 (r : &Row755't, dt : f32) -> bool = true
  fn score755 (r : &Row755't) -> f32 = 0.0
§ block_756
  def Row756't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle756 (r : &Row756't) -> bool = true
  fn process756 (r : &Row756't, dt : f32) -> bool = true
  fn score756 (r : &Row756't) -> f32 = 0.0
§ block_757
  def Row757't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle757 (r : &Row757't) -> bool = true
  fn process757 (r : &Row757't, dt : f32) -> bool = true
  fn score757 (r : &Row757't) -> f32 = 0.0
§ block_758
  def Row758't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle758 (r : &Row758't) -> bool = true
  fn process758 (r : &Row758't, dt : f32) -> bool = true
  fn score758 (r : &Row758't) -> f32 = 0.0
§ block_759
  def Row759't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle759 (r : &Row759't) -> bool = true
  fn process759 (r : &Row759't, dt : f32) -> bool = true
  fn score759 (r : &Row759't) -> f32 = 0.0
§ block_760
  def Row760't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle760 (r : &Row760't) -> bool = true
  fn process760 (r : &Row760't, dt : f32) -> bool = true
  fn score760 (r : &Row760't) -> f32 = 0.0
§ block_761
  def Row761't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle761 (r : &Row761't) -> bool = true
  fn process761 (r : &Row761't, dt : f32) -> bool = true
  fn score761 (r : &Row761't) -> f32 = 0.0
§ block_762
  def Row762't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle762 (r : &Row762't) -> bool = true
  fn process762 (r : &Row762't, dt : f32) -> bool = true
  fn score762 (r : &Row762't) -> f32 = 0.0
§ block_763
  def Row763't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle763 (r : &Row763't) -> bool = true
  fn process763 (r : &Row763't, dt : f32) -> bool = true
  fn score763 (r : &Row763't) -> f32 = 0.0
§ block_764
  def Row764't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle764 (r : &Row764't) -> bool = true
  fn process764 (r : &Row764't, dt : f32) -> bool = true
  fn score764 (r : &Row764't) -> f32 = 0.0
§ block_765
  def Row765't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle765 (r : &Row765't) -> bool = true
  fn process765 (r : &Row765't, dt : f32) -> bool = true
  fn score765 (r : &Row765't) -> f32 = 0.0
§ block_766
  def Row766't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle766 (r : &Row766't) -> bool = true
  fn process766 (r : &Row766't, dt : f32) -> bool = true
  fn score766 (r : &Row766't) -> f32 = 0.0
§ block_767
  def Row767't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle767 (r : &Row767't) -> bool = true
  fn process767 (r : &Row767't, dt : f32) -> bool = true
  fn score767 (r : &Row767't) -> f32 = 0.0
§ block_768
  def Row768't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle768 (r : &Row768't) -> bool = true
  fn process768 (r : &Row768't, dt : f32) -> bool = true
  fn score768 (r : &Row768't) -> f32 = 0.0
§ block_769
  def Row769't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle769 (r : &Row769't) -> bool = true
  fn process769 (r : &Row769't, dt : f32) -> bool = true
  fn score769 (r : &Row769't) -> f32 = 0.0
§ block_770
  def Row770't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle770 (r : &Row770't) -> bool = true
  fn process770 (r : &Row770't, dt : f32) -> bool = true
  fn score770 (r : &Row770't) -> f32 = 0.0
§ block_771
  def Row771't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle771 (r : &Row771't) -> bool = true
  fn process771 (r : &Row771't, dt : f32) -> bool = true
  fn score771 (r : &Row771't) -> f32 = 0.0
§ block_772
  def Row772't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle772 (r : &Row772't) -> bool = true
  fn process772 (r : &Row772't, dt : f32) -> bool = true
  fn score772 (r : &Row772't) -> f32 = 0.0
§ block_773
  def Row773't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle773 (r : &Row773't) -> bool = true
  fn process773 (r : &Row773't, dt : f32) -> bool = true
  fn score773 (r : &Row773't) -> f32 = 0.0
§ block_774
  def Row774't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle774 (r : &Row774't) -> bool = true
  fn process774 (r : &Row774't, dt : f32) -> bool = true
  fn score774 (r : &Row774't) -> f32 = 0.0
§ block_775
  def Row775't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle775 (r : &Row775't) -> bool = true
  fn process775 (r : &Row775't, dt : f32) -> bool = true
  fn score775 (r : &Row775't) -> f32 = 0.0
§ block_776
  def Row776't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle776 (r : &Row776't) -> bool = true
  fn process776 (r : &Row776't, dt : f32) -> bool = true
  fn score776 (r : &Row776't) -> f32 = 0.0
§ block_777
  def Row777't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle777 (r : &Row777't) -> bool = true
  fn process777 (r : &Row777't, dt : f32) -> bool = true
  fn score777 (r : &Row777't) -> f32 = 0.0
§ block_778
  def Row778't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle778 (r : &Row778't) -> bool = true
  fn process778 (r : &Row778't, dt : f32) -> bool = true
  fn score778 (r : &Row778't) -> f32 = 0.0
§ block_779
  def Row779't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle779 (r : &Row779't) -> bool = true
  fn process779 (r : &Row779't, dt : f32) -> bool = true
  fn score779 (r : &Row779't) -> f32 = 0.0
§ block_780
  def Row780't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle780 (r : &Row780't) -> bool = true
  fn process780 (r : &Row780't, dt : f32) -> bool = true
  fn score780 (r : &Row780't) -> f32 = 0.0
§ block_781
  def Row781't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle781 (r : &Row781't) -> bool = true
  fn process781 (r : &Row781't, dt : f32) -> bool = true
  fn score781 (r : &Row781't) -> f32 = 0.0
§ block_782
  def Row782't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle782 (r : &Row782't) -> bool = true
  fn process782 (r : &Row782't, dt : f32) -> bool = true
  fn score782 (r : &Row782't) -> f32 = 0.0
§ block_783
  def Row783't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle783 (r : &Row783't) -> bool = true
  fn process783 (r : &Row783't, dt : f32) -> bool = true
  fn score783 (r : &Row783't) -> f32 = 0.0
§ block_784
  def Row784't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle784 (r : &Row784't) -> bool = true
  fn process784 (r : &Row784't, dt : f32) -> bool = true
  fn score784 (r : &Row784't) -> f32 = 0.0
§ block_785
  def Row785't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle785 (r : &Row785't) -> bool = true
  fn process785 (r : &Row785't, dt : f32) -> bool = true
  fn score785 (r : &Row785't) -> f32 = 0.0
§ block_786
  def Row786't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle786 (r : &Row786't) -> bool = true
  fn process786 (r : &Row786't, dt : f32) -> bool = true
  fn score786 (r : &Row786't) -> f32 = 0.0
§ block_787
  def Row787't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle787 (r : &Row787't) -> bool = true
  fn process787 (r : &Row787't, dt : f32) -> bool = true
  fn score787 (r : &Row787't) -> f32 = 0.0
§ block_788
  def Row788't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle788 (r : &Row788't) -> bool = true
  fn process788 (r : &Row788't, dt : f32) -> bool = true
  fn score788 (r : &Row788't) -> f32 = 0.0
§ block_789
  def Row789't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle789 (r : &Row789't) -> bool = true
  fn process789 (r : &Row789't, dt : f32) -> bool = true
  fn score789 (r : &Row789't) -> f32 = 0.0
§ block_790
  def Row790't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle790 (r : &Row790't) -> bool = true
  fn process790 (r : &Row790't, dt : f32) -> bool = true
  fn score790 (r : &Row790't) -> f32 = 0.0
§ block_791
  def Row791't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle791 (r : &Row791't) -> bool = true
  fn process791 (r : &Row791't, dt : f32) -> bool = true
  fn score791 (r : &Row791't) -> f32 = 0.0
§ block_792
  def Row792't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle792 (r : &Row792't) -> bool = true
  fn process792 (r : &Row792't, dt : f32) -> bool = true
  fn score792 (r : &Row792't) -> f32 = 0.0
§ block_793
  def Row793't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle793 (r : &Row793't) -> bool = true
  fn process793 (r : &Row793't, dt : f32) -> bool = true
  fn score793 (r : &Row793't) -> f32 = 0.0
§ block_794
  def Row794't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle794 (r : &Row794't) -> bool = true
  fn process794 (r : &Row794't, dt : f32) -> bool = true
  fn score794 (r : &Row794't) -> f32 = 0.0
§ block_795
  def Row795't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle795 (r : &Row795't) -> bool = true
  fn process795 (r : &Row795't, dt : f32) -> bool = true
  fn score795 (r : &Row795't) -> f32 = 0.0
§ block_796
  def Row796't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle796 (r : &Row796't) -> bool = true
  fn process796 (r : &Row796't, dt : f32) -> bool = true
  fn score796 (r : &Row796't) -> f32 = 0.0
§ block_797
  def Row797't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle797 (r : &Row797't) -> bool = true
  fn process797 (r : &Row797't, dt : f32) -> bool = true
  fn score797 (r : &Row797't) -> f32 = 0.0
§ block_798
  def Row798't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle798 (r : &Row798't) -> bool = true
  fn process798 (r : &Row798't, dt : f32) -> bool = true
  fn score798 (r : &Row798't) -> f32 = 0.0
§ block_799
  def Row799't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle799 (r : &Row799't) -> bool = true
  fn process799 (r : &Row799't, dt : f32) -> bool = true
  fn score799 (r : &Row799't) -> f32 = 0.0
§ block_800
  def Row800't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle800 (r : &Row800't) -> bool = true
  fn process800 (r : &Row800't, dt : f32) -> bool = true
  fn score800 (r : &Row800't) -> f32 = 0.0
§ block_801
  def Row801't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle801 (r : &Row801't) -> bool = true
  fn process801 (r : &Row801't, dt : f32) -> bool = true
  fn score801 (r : &Row801't) -> f32 = 0.0
§ block_802
  def Row802't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle802 (r : &Row802't) -> bool = true
  fn process802 (r : &Row802't, dt : f32) -> bool = true
  fn score802 (r : &Row802't) -> f32 = 0.0
§ block_803
  def Row803't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle803 (r : &Row803't) -> bool = true
  fn process803 (r : &Row803't, dt : f32) -> bool = true
  fn score803 (r : &Row803't) -> f32 = 0.0
§ block_804
  def Row804't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle804 (r : &Row804't) -> bool = true
  fn process804 (r : &Row804't, dt : f32) -> bool = true
  fn score804 (r : &Row804't) -> f32 = 0.0
§ block_805
  def Row805't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle805 (r : &Row805't) -> bool = true
  fn process805 (r : &Row805't, dt : f32) -> bool = true
  fn score805 (r : &Row805't) -> f32 = 0.0
§ block_806
  def Row806't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle806 (r : &Row806't) -> bool = true
  fn process806 (r : &Row806't, dt : f32) -> bool = true
  fn score806 (r : &Row806't) -> f32 = 0.0
§ block_807
  def Row807't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle807 (r : &Row807't) -> bool = true
  fn process807 (r : &Row807't, dt : f32) -> bool = true
  fn score807 (r : &Row807't) -> f32 = 0.0
§ block_808
  def Row808't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle808 (r : &Row808't) -> bool = true
  fn process808 (r : &Row808't, dt : f32) -> bool = true
  fn score808 (r : &Row808't) -> f32 = 0.0
§ block_809
  def Row809't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle809 (r : &Row809't) -> bool = true
  fn process809 (r : &Row809't, dt : f32) -> bool = true
  fn score809 (r : &Row809't) -> f32 = 0.0
§ block_810
  def Row810't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle810 (r : &Row810't) -> bool = true
  fn process810 (r : &Row810't, dt : f32) -> bool = true
  fn score810 (r : &Row810't) -> f32 = 0.0
§ block_811
  def Row811't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle811 (r : &Row811't) -> bool = true
  fn process811 (r : &Row811't, dt : f32) -> bool = true
  fn score811 (r : &Row811't) -> f32 = 0.0
§ block_812
  def Row812't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle812 (r : &Row812't) -> bool = true
  fn process812 (r : &Row812't, dt : f32) -> bool = true
  fn score812 (r : &Row812't) -> f32 = 0.0
§ block_813
  def Row813't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle813 (r : &Row813't) -> bool = true
  fn process813 (r : &Row813't, dt : f32) -> bool = true
  fn score813 (r : &Row813't) -> f32 = 0.0
§ block_814
  def Row814't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle814 (r : &Row814't) -> bool = true
  fn process814 (r : &Row814't, dt : f32) -> bool = true
  fn score814 (r : &Row814't) -> f32 = 0.0
§ block_815
  def Row815't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle815 (r : &Row815't) -> bool = true
  fn process815 (r : &Row815't, dt : f32) -> bool = true
  fn score815 (r : &Row815't) -> f32 = 0.0
§ block_816
  def Row816't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle816 (r : &Row816't) -> bool = true
  fn process816 (r : &Row816't, dt : f32) -> bool = true
  fn score816 (r : &Row816't) -> f32 = 0.0
§ block_817
  def Row817't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle817 (r : &Row817't) -> bool = true
  fn process817 (r : &Row817't, dt : f32) -> bool = true
  fn score817 (r : &Row817't) -> f32 = 0.0
§ block_818
  def Row818't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle818 (r : &Row818't) -> bool = true
  fn process818 (r : &Row818't, dt : f32) -> bool = true
  fn score818 (r : &Row818't) -> f32 = 0.0
§ block_819
  def Row819't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle819 (r : &Row819't) -> bool = true
  fn process819 (r : &Row819't, dt : f32) -> bool = true
  fn score819 (r : &Row819't) -> f32 = 0.0
§ block_820
  def Row820't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle820 (r : &Row820't) -> bool = true
  fn process820 (r : &Row820't, dt : f32) -> bool = true
  fn score820 (r : &Row820't) -> f32 = 0.0
§ block_821
  def Row821't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle821 (r : &Row821't) -> bool = true
  fn process821 (r : &Row821't, dt : f32) -> bool = true
  fn score821 (r : &Row821't) -> f32 = 0.0
§ block_822
  def Row822't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle822 (r : &Row822't) -> bool = true
  fn process822 (r : &Row822't, dt : f32) -> bool = true
  fn score822 (r : &Row822't) -> f32 = 0.0
§ block_823
  def Row823't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle823 (r : &Row823't) -> bool = true
  fn process823 (r : &Row823't, dt : f32) -> bool = true
  fn score823 (r : &Row823't) -> f32 = 0.0
§ block_824
  def Row824't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle824 (r : &Row824't) -> bool = true
  fn process824 (r : &Row824't, dt : f32) -> bool = true
  fn score824 (r : &Row824't) -> f32 = 0.0
§ block_825
  def Row825't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle825 (r : &Row825't) -> bool = true
  fn process825 (r : &Row825't, dt : f32) -> bool = true
  fn score825 (r : &Row825't) -> f32 = 0.0
§ block_826
  def Row826't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle826 (r : &Row826't) -> bool = true
  fn process826 (r : &Row826't, dt : f32) -> bool = true
  fn score826 (r : &Row826't) -> f32 = 0.0
§ block_827
  def Row827't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle827 (r : &Row827't) -> bool = true
  fn process827 (r : &Row827't, dt : f32) -> bool = true
  fn score827 (r : &Row827't) -> f32 = 0.0
§ block_828
  def Row828't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle828 (r : &Row828't) -> bool = true
  fn process828 (r : &Row828't, dt : f32) -> bool = true
  fn score828 (r : &Row828't) -> f32 = 0.0
§ block_829
  def Row829't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle829 (r : &Row829't) -> bool = true
  fn process829 (r : &Row829't, dt : f32) -> bool = true
  fn score829 (r : &Row829't) -> f32 = 0.0
§ block_830
  def Row830't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle830 (r : &Row830't) -> bool = true
  fn process830 (r : &Row830't, dt : f32) -> bool = true
  fn score830 (r : &Row830't) -> f32 = 0.0
§ block_831
  def Row831't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle831 (r : &Row831't) -> bool = true
  fn process831 (r : &Row831't, dt : f32) -> bool = true
  fn score831 (r : &Row831't) -> f32 = 0.0
§ block_832
  def Row832't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle832 (r : &Row832't) -> bool = true
  fn process832 (r : &Row832't, dt : f32) -> bool = true
  fn score832 (r : &Row832't) -> f32 = 0.0
§ block_833
  def Row833't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle833 (r : &Row833't) -> bool = true
  fn process833 (r : &Row833't, dt : f32) -> bool = true
  fn score833 (r : &Row833't) -> f32 = 0.0
§ block_834
  def Row834't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle834 (r : &Row834't) -> bool = true
  fn process834 (r : &Row834't, dt : f32) -> bool = true
  fn score834 (r : &Row834't) -> f32 = 0.0
§ block_835
  def Row835't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle835 (r : &Row835't) -> bool = true
  fn process835 (r : &Row835't, dt : f32) -> bool = true
  fn score835 (r : &Row835't) -> f32 = 0.0
§ block_836
  def Row836't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle836 (r : &Row836't) -> bool = true
  fn process836 (r : &Row836't, dt : f32) -> bool = true
  fn score836 (r : &Row836't) -> f32 = 0.0
§ block_837
  def Row837't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle837 (r : &Row837't) -> bool = true
  fn process837 (r : &Row837't, dt : f32) -> bool = true
  fn score837 (r : &Row837't) -> f32 = 0.0
§ block_838
  def Row838't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle838 (r : &Row838't) -> bool = true
  fn process838 (r : &Row838't, dt : f32) -> bool = true
  fn score838 (r : &Row838't) -> f32 = 0.0
§ block_839
  def Row839't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle839 (r : &Row839't) -> bool = true
  fn process839 (r : &Row839't, dt : f32) -> bool = true
  fn score839 (r : &Row839't) -> f32 = 0.0
§ block_840
  def Row840't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle840 (r : &Row840't) -> bool = true
  fn process840 (r : &Row840't, dt : f32) -> bool = true
  fn score840 (r : &Row840't) -> f32 = 0.0
§ block_841
  def Row841't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle841 (r : &Row841't) -> bool = true
  fn process841 (r : &Row841't, dt : f32) -> bool = true
  fn score841 (r : &Row841't) -> f32 = 0.0
§ block_842
  def Row842't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle842 (r : &Row842't) -> bool = true
  fn process842 (r : &Row842't, dt : f32) -> bool = true
  fn score842 (r : &Row842't) -> f32 = 0.0
§ block_843
  def Row843't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle843 (r : &Row843't) -> bool = true
  fn process843 (r : &Row843't, dt : f32) -> bool = true
  fn score843 (r : &Row843't) -> f32 = 0.0
§ block_844
  def Row844't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle844 (r : &Row844't) -> bool = true
  fn process844 (r : &Row844't, dt : f32) -> bool = true
  fn score844 (r : &Row844't) -> f32 = 0.0
§ block_845
  def Row845't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle845 (r : &Row845't) -> bool = true
  fn process845 (r : &Row845't, dt : f32) -> bool = true
  fn score845 (r : &Row845't) -> f32 = 0.0
§ block_846
  def Row846't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle846 (r : &Row846't) -> bool = true
  fn process846 (r : &Row846't, dt : f32) -> bool = true
  fn score846 (r : &Row846't) -> f32 = 0.0
§ block_847
  def Row847't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle847 (r : &Row847't) -> bool = true
  fn process847 (r : &Row847't, dt : f32) -> bool = true
  fn score847 (r : &Row847't) -> f32 = 0.0
§ block_848
  def Row848't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle848 (r : &Row848't) -> bool = true
  fn process848 (r : &Row848't, dt : f32) -> bool = true
  fn score848 (r : &Row848't) -> f32 = 0.0
§ block_849
  def Row849't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle849 (r : &Row849't) -> bool = true
  fn process849 (r : &Row849't, dt : f32) -> bool = true
  fn score849 (r : &Row849't) -> f32 = 0.0
§ block_850
  def Row850't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle850 (r : &Row850't) -> bool = true
  fn process850 (r : &Row850't, dt : f32) -> bool = true
  fn score850 (r : &Row850't) -> f32 = 0.0
§ block_851
  def Row851't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle851 (r : &Row851't) -> bool = true
  fn process851 (r : &Row851't, dt : f32) -> bool = true
  fn score851 (r : &Row851't) -> f32 = 0.0
§ block_852
  def Row852't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle852 (r : &Row852't) -> bool = true
  fn process852 (r : &Row852't, dt : f32) -> bool = true
  fn score852 (r : &Row852't) -> f32 = 0.0
§ block_853
  def Row853't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle853 (r : &Row853't) -> bool = true
  fn process853 (r : &Row853't, dt : f32) -> bool = true
  fn score853 (r : &Row853't) -> f32 = 0.0
§ block_854
  def Row854't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle854 (r : &Row854't) -> bool = true
  fn process854 (r : &Row854't, dt : f32) -> bool = true
  fn score854 (r : &Row854't) -> f32 = 0.0
§ block_855
  def Row855't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle855 (r : &Row855't) -> bool = true
  fn process855 (r : &Row855't, dt : f32) -> bool = true
  fn score855 (r : &Row855't) -> f32 = 0.0
§ block_856
  def Row856't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle856 (r : &Row856't) -> bool = true
  fn process856 (r : &Row856't, dt : f32) -> bool = true
  fn score856 (r : &Row856't) -> f32 = 0.0
§ block_857
  def Row857't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle857 (r : &Row857't) -> bool = true
  fn process857 (r : &Row857't, dt : f32) -> bool = true
  fn score857 (r : &Row857't) -> f32 = 0.0
§ block_858
  def Row858't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle858 (r : &Row858't) -> bool = true
  fn process858 (r : &Row858't, dt : f32) -> bool = true
  fn score858 (r : &Row858't) -> f32 = 0.0
§ block_859
  def Row859't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle859 (r : &Row859't) -> bool = true
  fn process859 (r : &Row859't, dt : f32) -> bool = true
  fn score859 (r : &Row859't) -> f32 = 0.0
§ block_860
  def Row860't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle860 (r : &Row860't) -> bool = true
  fn process860 (r : &Row860't, dt : f32) -> bool = true
  fn score860 (r : &Row860't) -> f32 = 0.0
§ block_861
  def Row861't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle861 (r : &Row861't) -> bool = true
  fn process861 (r : &Row861't, dt : f32) -> bool = true
  fn score861 (r : &Row861't) -> f32 = 0.0
§ block_862
  def Row862't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle862 (r : &Row862't) -> bool = true
  fn process862 (r : &Row862't, dt : f32) -> bool = true
  fn score862 (r : &Row862't) -> f32 = 0.0
§ block_863
  def Row863't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle863 (r : &Row863't) -> bool = true
  fn process863 (r : &Row863't, dt : f32) -> bool = true
  fn score863 (r : &Row863't) -> f32 = 0.0
§ block_864
  def Row864't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle864 (r : &Row864't) -> bool = true
  fn process864 (r : &Row864't, dt : f32) -> bool = true
  fn score864 (r : &Row864't) -> f32 = 0.0
§ block_865
  def Row865't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle865 (r : &Row865't) -> bool = true
  fn process865 (r : &Row865't, dt : f32) -> bool = true
  fn score865 (r : &Row865't) -> f32 = 0.0
§ block_866
  def Row866't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle866 (r : &Row866't) -> bool = true
  fn process866 (r : &Row866't, dt : f32) -> bool = true
  fn score866 (r : &Row866't) -> f32 = 0.0
§ block_867
  def Row867't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle867 (r : &Row867't) -> bool = true
  fn process867 (r : &Row867't, dt : f32) -> bool = true
  fn score867 (r : &Row867't) -> f32 = 0.0
§ block_868
  def Row868't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle868 (r : &Row868't) -> bool = true
  fn process868 (r : &Row868't, dt : f32) -> bool = true
  fn score868 (r : &Row868't) -> f32 = 0.0
§ block_869
  def Row869't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle869 (r : &Row869't) -> bool = true
  fn process869 (r : &Row869't, dt : f32) -> bool = true
  fn score869 (r : &Row869't) -> f32 = 0.0
§ block_870
  def Row870't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle870 (r : &Row870't) -> bool = true
  fn process870 (r : &Row870't, dt : f32) -> bool = true
  fn score870 (r : &Row870't) -> f32 = 0.0
§ block_871
  def Row871't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle871 (r : &Row871't) -> bool = true
  fn process871 (r : &Row871't, dt : f32) -> bool = true
  fn score871 (r : &Row871't) -> f32 = 0.0
§ block_872
  def Row872't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle872 (r : &Row872't) -> bool = true
  fn process872 (r : &Row872't, dt : f32) -> bool = true
  fn score872 (r : &Row872't) -> f32 = 0.0
§ block_873
  def Row873't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle873 (r : &Row873't) -> bool = true
  fn process873 (r : &Row873't, dt : f32) -> bool = true
  fn score873 (r : &Row873't) -> f32 = 0.0
§ block_874
  def Row874't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle874 (r : &Row874't) -> bool = true
  fn process874 (r : &Row874't, dt : f32) -> bool = true
  fn score874 (r : &Row874't) -> f32 = 0.0
§ block_875
  def Row875't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle875 (r : &Row875't) -> bool = true
  fn process875 (r : &Row875't, dt : f32) -> bool = true
  fn score875 (r : &Row875't) -> f32 = 0.0
§ block_876
  def Row876't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle876 (r : &Row876't) -> bool = true
  fn process876 (r : &Row876't, dt : f32) -> bool = true
  fn score876 (r : &Row876't) -> f32 = 0.0
§ block_877
  def Row877't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle877 (r : &Row877't) -> bool = true
  fn process877 (r : &Row877't, dt : f32) -> bool = true
  fn score877 (r : &Row877't) -> f32 = 0.0
§ block_878
  def Row878't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle878 (r : &Row878't) -> bool = true
  fn process878 (r : &Row878't, dt : f32) -> bool = true
  fn score878 (r : &Row878't) -> f32 = 0.0
§ block_879
  def Row879't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle879 (r : &Row879't) -> bool = true
  fn process879 (r : &Row879't, dt : f32) -> bool = true
  fn score879 (r : &Row879't) -> f32 = 0.0
§ block_880
  def Row880't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle880 (r : &Row880't) -> bool = true
  fn process880 (r : &Row880't, dt : f32) -> bool = true
  fn score880 (r : &Row880't) -> f32 = 0.0
§ block_881
  def Row881't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle881 (r : &Row881't) -> bool = true
  fn process881 (r : &Row881't, dt : f32) -> bool = true
  fn score881 (r : &Row881't) -> f32 = 0.0
§ block_882
  def Row882't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle882 (r : &Row882't) -> bool = true
  fn process882 (r : &Row882't, dt : f32) -> bool = true
  fn score882 (r : &Row882't) -> f32 = 0.0
§ block_883
  def Row883't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle883 (r : &Row883't) -> bool = true
  fn process883 (r : &Row883't, dt : f32) -> bool = true
  fn score883 (r : &Row883't) -> f32 = 0.0
§ block_884
  def Row884't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle884 (r : &Row884't) -> bool = true
  fn process884 (r : &Row884't, dt : f32) -> bool = true
  fn score884 (r : &Row884't) -> f32 = 0.0
§ block_885
  def Row885't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle885 (r : &Row885't) -> bool = true
  fn process885 (r : &Row885't, dt : f32) -> bool = true
  fn score885 (r : &Row885't) -> f32 = 0.0
§ block_886
  def Row886't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle886 (r : &Row886't) -> bool = true
  fn process886 (r : &Row886't, dt : f32) -> bool = true
  fn score886 (r : &Row886't) -> f32 = 0.0
§ block_887
  def Row887't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle887 (r : &Row887't) -> bool = true
  fn process887 (r : &Row887't, dt : f32) -> bool = true
  fn score887 (r : &Row887't) -> f32 = 0.0
§ block_888
  def Row888't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle888 (r : &Row888't) -> bool = true
  fn process888 (r : &Row888't, dt : f32) -> bool = true
  fn score888 (r : &Row888't) -> f32 = 0.0
§ block_889
  def Row889't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle889 (r : &Row889't) -> bool = true
  fn process889 (r : &Row889't, dt : f32) -> bool = true
  fn score889 (r : &Row889't) -> f32 = 0.0
§ block_890
  def Row890't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle890 (r : &Row890't) -> bool = true
  fn process890 (r : &Row890't, dt : f32) -> bool = true
  fn score890 (r : &Row890't) -> f32 = 0.0
§ block_891
  def Row891't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle891 (r : &Row891't) -> bool = true
  fn process891 (r : &Row891't, dt : f32) -> bool = true
  fn score891 (r : &Row891't) -> f32 = 0.0
§ block_892
  def Row892't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle892 (r : &Row892't) -> bool = true
  fn process892 (r : &Row892't, dt : f32) -> bool = true
  fn score892 (r : &Row892't) -> f32 = 0.0
§ block_893
  def Row893't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle893 (r : &Row893't) -> bool = true
  fn process893 (r : &Row893't, dt : f32) -> bool = true
  fn score893 (r : &Row893't) -> f32 = 0.0
§ block_894
  def Row894't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle894 (r : &Row894't) -> bool = true
  fn process894 (r : &Row894't, dt : f32) -> bool = true
  fn score894 (r : &Row894't) -> f32 = 0.0
§ block_895
  def Row895't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle895 (r : &Row895't) -> bool = true
  fn process895 (r : &Row895't, dt : f32) -> bool = true
  fn score895 (r : &Row895't) -> f32 = 0.0
§ block_896
  def Row896't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle896 (r : &Row896't) -> bool = true
  fn process896 (r : &Row896't, dt : f32) -> bool = true
  fn score896 (r : &Row896't) -> f32 = 0.0
§ block_897
  def Row897't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle897 (r : &Row897't) -> bool = true
  fn process897 (r : &Row897't, dt : f32) -> bool = true
  fn score897 (r : &Row897't) -> f32 = 0.0
§ block_898
  def Row898't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle898 (r : &Row898't) -> bool = true
  fn process898 (r : &Row898't, dt : f32) -> bool = true
  fn score898 (r : &Row898't) -> f32 = 0.0
§ block_899
  def Row899't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle899 (r : &Row899't) -> bool = true
  fn process899 (r : &Row899't, dt : f32) -> bool = true
  fn score899 (r : &Row899't) -> f32 = 0.0
§ block_900
  def Row900't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle900 (r : &Row900't) -> bool = true
  fn process900 (r : &Row900't, dt : f32) -> bool = true
  fn score900 (r : &Row900't) -> f32 = 0.0
§ block_901
  def Row901't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle901 (r : &Row901't) -> bool = true
  fn process901 (r : &Row901't, dt : f32) -> bool = true
  fn score901 (r : &Row901't) -> f32 = 0.0
§ block_902
  def Row902't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle902 (r : &Row902't) -> bool = true
  fn process902 (r : &Row902't, dt : f32) -> bool = true
  fn score902 (r : &Row902't) -> f32 = 0.0
§ block_903
  def Row903't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle903 (r : &Row903't) -> bool = true
  fn process903 (r : &Row903't, dt : f32) -> bool = true
  fn score903 (r : &Row903't) -> f32 = 0.0
§ block_904
  def Row904't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle904 (r : &Row904't) -> bool = true
  fn process904 (r : &Row904't, dt : f32) -> bool = true
  fn score904 (r : &Row904't) -> f32 = 0.0
§ block_905
  def Row905't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle905 (r : &Row905't) -> bool = true
  fn process905 (r : &Row905't, dt : f32) -> bool = true
  fn score905 (r : &Row905't) -> f32 = 0.0
§ block_906
  def Row906't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle906 (r : &Row906't) -> bool = true
  fn process906 (r : &Row906't, dt : f32) -> bool = true
  fn score906 (r : &Row906't) -> f32 = 0.0
§ block_907
  def Row907't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle907 (r : &Row907't) -> bool = true
  fn process907 (r : &Row907't, dt : f32) -> bool = true
  fn score907 (r : &Row907't) -> f32 = 0.0
§ block_908
  def Row908't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle908 (r : &Row908't) -> bool = true
  fn process908 (r : &Row908't, dt : f32) -> bool = true
  fn score908 (r : &Row908't) -> f32 = 0.0
§ block_909
  def Row909't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle909 (r : &Row909't) -> bool = true
  fn process909 (r : &Row909't, dt : f32) -> bool = true
  fn score909 (r : &Row909't) -> f32 = 0.0
§ block_910
  def Row910't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle910 (r : &Row910't) -> bool = true
  fn process910 (r : &Row910't, dt : f32) -> bool = true
  fn score910 (r : &Row910't) -> f32 = 0.0
§ block_911
  def Row911't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle911 (r : &Row911't) -> bool = true
  fn process911 (r : &Row911't, dt : f32) -> bool = true
  fn score911 (r : &Row911't) -> f32 = 0.0
§ block_912
  def Row912't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle912 (r : &Row912't) -> bool = true
  fn process912 (r : &Row912't, dt : f32) -> bool = true
  fn score912 (r : &Row912't) -> f32 = 0.0
§ block_913
  def Row913't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle913 (r : &Row913't) -> bool = true
  fn process913 (r : &Row913't, dt : f32) -> bool = true
  fn score913 (r : &Row913't) -> f32 = 0.0
§ block_914
  def Row914't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle914 (r : &Row914't) -> bool = true
  fn process914 (r : &Row914't, dt : f32) -> bool = true
  fn score914 (r : &Row914't) -> f32 = 0.0
§ block_915
  def Row915't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle915 (r : &Row915't) -> bool = true
  fn process915 (r : &Row915't, dt : f32) -> bool = true
  fn score915 (r : &Row915't) -> f32 = 0.0
§ block_916
  def Row916't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle916 (r : &Row916't) -> bool = true
  fn process916 (r : &Row916't, dt : f32) -> bool = true
  fn score916 (r : &Row916't) -> f32 = 0.0
§ block_917
  def Row917't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle917 (r : &Row917't) -> bool = true
  fn process917 (r : &Row917't, dt : f32) -> bool = true
  fn score917 (r : &Row917't) -> f32 = 0.0
§ block_918
  def Row918't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle918 (r : &Row918't) -> bool = true
  fn process918 (r : &Row918't, dt : f32) -> bool = true
  fn score918 (r : &Row918't) -> f32 = 0.0
§ block_919
  def Row919't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle919 (r : &Row919't) -> bool = true
  fn process919 (r : &Row919't, dt : f32) -> bool = true
  fn score919 (r : &Row919't) -> f32 = 0.0
§ block_920
  def Row920't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle920 (r : &Row920't) -> bool = true
  fn process920 (r : &Row920't, dt : f32) -> bool = true
  fn score920 (r : &Row920't) -> f32 = 0.0
§ block_921
  def Row921't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle921 (r : &Row921't) -> bool = true
  fn process921 (r : &Row921't, dt : f32) -> bool = true
  fn score921 (r : &Row921't) -> f32 = 0.0
§ block_922
  def Row922't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle922 (r : &Row922't) -> bool = true
  fn process922 (r : &Row922't, dt : f32) -> bool = true
  fn score922 (r : &Row922't) -> f32 = 0.0
§ block_923
  def Row923't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle923 (r : &Row923't) -> bool = true
  fn process923 (r : &Row923't, dt : f32) -> bool = true
  fn score923 (r : &Row923't) -> f32 = 0.0
§ block_924
  def Row924't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle924 (r : &Row924't) -> bool = true
  fn process924 (r : &Row924't, dt : f32) -> bool = true
  fn score924 (r : &Row924't) -> f32 = 0.0
§ block_925
  def Row925't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle925 (r : &Row925't) -> bool = true
  fn process925 (r : &Row925't, dt : f32) -> bool = true
  fn score925 (r : &Row925't) -> f32 = 0.0
§ block_926
  def Row926't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle926 (r : &Row926't) -> bool = true
  fn process926 (r : &Row926't, dt : f32) -> bool = true
  fn score926 (r : &Row926't) -> f32 = 0.0
§ block_927
  def Row927't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle927 (r : &Row927't) -> bool = true
  fn process927 (r : &Row927't, dt : f32) -> bool = true
  fn score927 (r : &Row927't) -> f32 = 0.0
§ block_928
  def Row928't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle928 (r : &Row928't) -> bool = true
  fn process928 (r : &Row928't, dt : f32) -> bool = true
  fn score928 (r : &Row928't) -> f32 = 0.0
§ block_929
  def Row929't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle929 (r : &Row929't) -> bool = true
  fn process929 (r : &Row929't, dt : f32) -> bool = true
  fn score929 (r : &Row929't) -> f32 = 0.0
§ block_930
  def Row930't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle930 (r : &Row930't) -> bool = true
  fn process930 (r : &Row930't, dt : f32) -> bool = true
  fn score930 (r : &Row930't) -> f32 = 0.0
§ block_931
  def Row931't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle931 (r : &Row931't) -> bool = true
  fn process931 (r : &Row931't, dt : f32) -> bool = true
  fn score931 (r : &Row931't) -> f32 = 0.0
§ block_932
  def Row932't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle932 (r : &Row932't) -> bool = true
  fn process932 (r : &Row932't, dt : f32) -> bool = true
  fn score932 (r : &Row932't) -> f32 = 0.0
§ block_933
  def Row933't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle933 (r : &Row933't) -> bool = true
  fn process933 (r : &Row933't, dt : f32) -> bool = true
  fn score933 (r : &Row933't) -> f32 = 0.0
§ block_934
  def Row934't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle934 (r : &Row934't) -> bool = true
  fn process934 (r : &Row934't, dt : f32) -> bool = true
  fn score934 (r : &Row934't) -> f32 = 0.0
§ block_935
  def Row935't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle935 (r : &Row935't) -> bool = true
  fn process935 (r : &Row935't, dt : f32) -> bool = true
  fn score935 (r : &Row935't) -> f32 = 0.0
§ block_936
  def Row936't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle936 (r : &Row936't) -> bool = true
  fn process936 (r : &Row936't, dt : f32) -> bool = true
  fn score936 (r : &Row936't) -> f32 = 0.0
§ block_937
  def Row937't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle937 (r : &Row937't) -> bool = true
  fn process937 (r : &Row937't, dt : f32) -> bool = true
  fn score937 (r : &Row937't) -> f32 = 0.0
§ block_938
  def Row938't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle938 (r : &Row938't) -> bool = true
  fn process938 (r : &Row938't, dt : f32) -> bool = true
  fn score938 (r : &Row938't) -> f32 = 0.0
§ block_939
  def Row939't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle939 (r : &Row939't) -> bool = true
  fn process939 (r : &Row939't, dt : f32) -> bool = true
  fn score939 (r : &Row939't) -> f32 = 0.0
§ block_940
  def Row940't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle940 (r : &Row940't) -> bool = true
  fn process940 (r : &Row940't, dt : f32) -> bool = true
  fn score940 (r : &Row940't) -> f32 = 0.0
§ block_941
  def Row941't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle941 (r : &Row941't) -> bool = true
  fn process941 (r : &Row941't, dt : f32) -> bool = true
  fn score941 (r : &Row941't) -> f32 = 0.0
§ block_942
  def Row942't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle942 (r : &Row942't) -> bool = true
  fn process942 (r : &Row942't, dt : f32) -> bool = true
  fn score942 (r : &Row942't) -> f32 = 0.0
§ block_943
  def Row943't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle943 (r : &Row943't) -> bool = true
  fn process943 (r : &Row943't, dt : f32) -> bool = true
  fn score943 (r : &Row943't) -> f32 = 0.0
§ block_944
  def Row944't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle944 (r : &Row944't) -> bool = true
  fn process944 (r : &Row944't, dt : f32) -> bool = true
  fn score944 (r : &Row944't) -> f32 = 0.0
§ block_945
  def Row945't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle945 (r : &Row945't) -> bool = true
  fn process945 (r : &Row945't, dt : f32) -> bool = true
  fn score945 (r : &Row945't) -> f32 = 0.0
§ block_946
  def Row946't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle946 (r : &Row946't) -> bool = true
  fn process946 (r : &Row946't, dt : f32) -> bool = true
  fn score946 (r : &Row946't) -> f32 = 0.0
§ block_947
  def Row947't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle947 (r : &Row947't) -> bool = true
  fn process947 (r : &Row947't, dt : f32) -> bool = true
  fn score947 (r : &Row947't) -> f32 = 0.0
§ block_948
  def Row948't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle948 (r : &Row948't) -> bool = true
  fn process948 (r : &Row948't, dt : f32) -> bool = true
  fn score948 (r : &Row948't) -> f32 = 0.0
§ block_949
  def Row949't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle949 (r : &Row949't) -> bool = true
  fn process949 (r : &Row949't, dt : f32) -> bool = true
  fn score949 (r : &Row949't) -> f32 = 0.0
§ block_950
  def Row950't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle950 (r : &Row950't) -> bool = true
  fn process950 (r : &Row950't, dt : f32) -> bool = true
  fn score950 (r : &Row950't) -> f32 = 0.0
§ block_951
  def Row951't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle951 (r : &Row951't) -> bool = true
  fn process951 (r : &Row951't, dt : f32) -> bool = true
  fn score951 (r : &Row951't) -> f32 = 0.0
§ block_952
  def Row952't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle952 (r : &Row952't) -> bool = true
  fn process952 (r : &Row952't, dt : f32) -> bool = true
  fn score952 (r : &Row952't) -> f32 = 0.0
§ block_953
  def Row953't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle953 (r : &Row953't) -> bool = true
  fn process953 (r : &Row953't, dt : f32) -> bool = true
  fn score953 (r : &Row953't) -> f32 = 0.0
§ block_954
  def Row954't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle954 (r : &Row954't) -> bool = true
  fn process954 (r : &Row954't, dt : f32) -> bool = true
  fn score954 (r : &Row954't) -> f32 = 0.0
§ block_955
  def Row955't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle955 (r : &Row955't) -> bool = true
  fn process955 (r : &Row955't, dt : f32) -> bool = true
  fn score955 (r : &Row955't) -> f32 = 0.0
§ block_956
  def Row956't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle956 (r : &Row956't) -> bool = true
  fn process956 (r : &Row956't, dt : f32) -> bool = true
  fn score956 (r : &Row956't) -> f32 = 0.0
§ block_957
  def Row957't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle957 (r : &Row957't) -> bool = true
  fn process957 (r : &Row957't, dt : f32) -> bool = true
  fn score957 (r : &Row957't) -> f32 = 0.0
§ block_958
  def Row958't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle958 (r : &Row958't) -> bool = true
  fn process958 (r : &Row958't, dt : f32) -> bool = true
  fn score958 (r : &Row958't) -> f32 = 0.0
§ block_959
  def Row959't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle959 (r : &Row959't) -> bool = true
  fn process959 (r : &Row959't, dt : f32) -> bool = true
  fn score959 (r : &Row959't) -> f32 = 0.0
§ block_960
  def Row960't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle960 (r : &Row960't) -> bool = true
  fn process960 (r : &Row960't, dt : f32) -> bool = true
  fn score960 (r : &Row960't) -> f32 = 0.0
§ block_961
  def Row961't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle961 (r : &Row961't) -> bool = true
  fn process961 (r : &Row961't, dt : f32) -> bool = true
  fn score961 (r : &Row961't) -> f32 = 0.0
§ block_962
  def Row962't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle962 (r : &Row962't) -> bool = true
  fn process962 (r : &Row962't, dt : f32) -> bool = true
  fn score962 (r : &Row962't) -> f32 = 0.0
§ block_963
  def Row963't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle963 (r : &Row963't) -> bool = true
  fn process963 (r : &Row963't, dt : f32) -> bool = true
  fn score963 (r : &Row963't) -> f32 = 0.0
§ block_964
  def Row964't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle964 (r : &Row964't) -> bool = true
  fn process964 (r : &Row964't, dt : f32) -> bool = true
  fn score964 (r : &Row964't) -> f32 = 0.0
§ block_965
  def Row965't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle965 (r : &Row965't) -> bool = true
  fn process965 (r : &Row965't, dt : f32) -> bool = true
  fn score965 (r : &Row965't) -> f32 = 0.0
§ block_966
  def Row966't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle966 (r : &Row966't) -> bool = true
  fn process966 (r : &Row966't, dt : f32) -> bool = true
  fn score966 (r : &Row966't) -> f32 = 0.0
§ block_967
  def Row967't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle967 (r : &Row967't) -> bool = true
  fn process967 (r : &Row967't, dt : f32) -> bool = true
  fn score967 (r : &Row967't) -> f32 = 0.0
§ block_968
  def Row968't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle968 (r : &Row968't) -> bool = true
  fn process968 (r : &Row968't, dt : f32) -> bool = true
  fn score968 (r : &Row968't) -> f32 = 0.0
§ block_969
  def Row969't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle969 (r : &Row969't) -> bool = true
  fn process969 (r : &Row969't, dt : f32) -> bool = true
  fn score969 (r : &Row969't) -> f32 = 0.0
§ block_970
  def Row970't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle970 (r : &Row970't) -> bool = true
  fn process970 (r : &Row970't, dt : f32) -> bool = true
  fn score970 (r : &Row970't) -> f32 = 0.0
§ block_971
  def Row971't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle971 (r : &Row971't) -> bool = true
  fn process971 (r : &Row971't, dt : f32) -> bool = true
  fn score971 (r : &Row971't) -> f32 = 0.0
§ block_972
  def Row972't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle972 (r : &Row972't) -> bool = true
  fn process972 (r : &Row972't, dt : f32) -> bool = true
  fn score972 (r : &Row972't) -> f32 = 0.0
§ block_973
  def Row973't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle973 (r : &Row973't) -> bool = true
  fn process973 (r : &Row973't, dt : f32) -> bool = true
  fn score973 (r : &Row973't) -> f32 = 0.0
§ block_974
  def Row974't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle974 (r : &Row974't) -> bool = true
  fn process974 (r : &Row974't, dt : f32) -> bool = true
  fn score974 (r : &Row974't) -> f32 = 0.0
§ block_975
  def Row975't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle975 (r : &Row975't) -> bool = true
  fn process975 (r : &Row975't, dt : f32) -> bool = true
  fn score975 (r : &Row975't) -> f32 = 0.0
§ block_976
  def Row976't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle976 (r : &Row976't) -> bool = true
  fn process976 (r : &Row976't, dt : f32) -> bool = true
  fn score976 (r : &Row976't) -> f32 = 0.0
§ block_977
  def Row977't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle977 (r : &Row977't) -> bool = true
  fn process977 (r : &Row977't, dt : f32) -> bool = true
  fn score977 (r : &Row977't) -> f32 = 0.0
§ block_978
  def Row978't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle978 (r : &Row978't) -> bool = true
  fn process978 (r : &Row978't, dt : f32) -> bool = true
  fn score978 (r : &Row978't) -> f32 = 0.0
§ block_979
  def Row979't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle979 (r : &Row979't) -> bool = true
  fn process979 (r : &Row979't, dt : f32) -> bool = true
  fn score979 (r : &Row979't) -> f32 = 0.0
§ block_980
  def Row980't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle980 (r : &Row980't) -> bool = true
  fn process980 (r : &Row980't, dt : f32) -> bool = true
  fn score980 (r : &Row980't) -> f32 = 0.0
§ block_981
  def Row981't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle981 (r : &Row981't) -> bool = true
  fn process981 (r : &Row981't, dt : f32) -> bool = true
  fn score981 (r : &Row981't) -> f32 = 0.0
§ block_982
  def Row982't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle982 (r : &Row982't) -> bool = true
  fn process982 (r : &Row982't, dt : f32) -> bool = true
  fn score982 (r : &Row982't) -> f32 = 0.0
§ block_983
  def Row983't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle983 (r : &Row983't) -> bool = true
  fn process983 (r : &Row983't, dt : f32) -> bool = true
  fn score983 (r : &Row983't) -> f32 = 0.0
§ block_984
  def Row984't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle984 (r : &Row984't) -> bool = true
  fn process984 (r : &Row984't, dt : f32) -> bool = true
  fn score984 (r : &Row984't) -> f32 = 0.0
§ block_985
  def Row985't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle985 (r : &Row985't) -> bool = true
  fn process985 (r : &Row985't, dt : f32) -> bool = true
  fn score985 (r : &Row985't) -> f32 = 0.0
§ block_986
  def Row986't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle986 (r : &Row986't) -> bool = true
  fn process986 (r : &Row986't, dt : f32) -> bool = true
  fn score986 (r : &Row986't) -> f32 = 0.0
§ block_987
  def Row987't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle987 (r : &Row987't) -> bool = true
  fn process987 (r : &Row987't, dt : f32) -> bool = true
  fn score987 (r : &Row987't) -> f32 = 0.0
§ block_988
  def Row988't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle988 (r : &Row988't) -> bool = true
  fn process988 (r : &Row988't, dt : f32) -> bool = true
  fn score988 (r : &Row988't) -> f32 = 0.0
§ block_989
  def Row989't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle989 (r : &Row989't) -> bool = true
  fn process989 (r : &Row989't, dt : f32) -> bool = true
  fn score989 (r : &Row989't) -> f32 = 0.0
§ block_990
  def Row990't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle990 (r : &Row990't) -> bool = true
  fn process990 (r : &Row990't, dt : f32) -> bool = true
  fn score990 (r : &Row990't) -> f32 = 0.0
§ block_991
  def Row991't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle991 (r : &Row991't) -> bool = true
  fn process991 (r : &Row991't, dt : f32) -> bool = true
  fn score991 (r : &Row991't) -> f32 = 0.0
§ block_992
  def Row992't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle992 (r : &Row992't) -> bool = true
  fn process992 (r : &Row992't, dt : f32) -> bool = true
  fn score992 (r : &Row992't) -> f32 = 0.0
§ block_993
  def Row993't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle993 (r : &Row993't) -> bool = true
  fn process993 (r : &Row993't, dt : f32) -> bool = true
  fn score993 (r : &Row993't) -> f32 = 0.0
§ block_994
  def Row994't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle994 (r : &Row994't) -> bool = true
  fn process994 (r : &Row994't, dt : f32) -> bool = true
  fn score994 (r : &Row994't) -> f32 = 0.0
§ block_995
  def Row995't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle995 (r : &Row995't) -> bool = true
  fn process995 (r : &Row995't, dt : f32) -> bool = true
  fn score995 (r : &Row995't) -> f32 = 0.0
§ block_996
  def Row996't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle996 (r : &Row996't) -> bool = true
  fn process996 (r : &Row996't, dt : f32) -> bool = true
  fn score996 (r : &Row996't) -> f32 = 0.0
§ block_997
  def Row997't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle997 (r : &Row997't) -> bool = true
  fn process997 (r : &Row997't, dt : f32) -> bool = true
  fn score997 (r : &Row997't) -> f32 = 0.0
§ block_998
  def Row998't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle998 (r : &Row998't) -> bool = true
  fn process998 (r : &Row998't, dt : f32) -> bool = true
  fn score998 (r : &Row998't) -> f32 = 0.0
§ block_999
  def Row999't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle999 (r : &Row999't) -> bool = true
  fn process999 (r : &Row999't, dt : f32) -> bool = true
  fn score999 (r : &Row999't) -> f32 = 0.0
§ block_1000
  def Row1000't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1000 (r : &Row1000't) -> bool = true
  fn process1000 (r : &Row1000't, dt : f32) -> bool = true
  fn score1000 (r : &Row1000't) -> f32 = 0.0
§ block_1001
  def Row1001't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1001 (r : &Row1001't) -> bool = true
  fn process1001 (r : &Row1001't, dt : f32) -> bool = true
  fn score1001 (r : &Row1001't) -> f32 = 0.0
§ block_1002
  def Row1002't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1002 (r : &Row1002't) -> bool = true
  fn process1002 (r : &Row1002't, dt : f32) -> bool = true
  fn score1002 (r : &Row1002't) -> f32 = 0.0
§ block_1003
  def Row1003't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1003 (r : &Row1003't) -> bool = true
  fn process1003 (r : &Row1003't, dt : f32) -> bool = true
  fn score1003 (r : &Row1003't) -> f32 = 0.0
§ block_1004
  def Row1004't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1004 (r : &Row1004't) -> bool = true
  fn process1004 (r : &Row1004't, dt : f32) -> bool = true
  fn score1004 (r : &Row1004't) -> f32 = 0.0
§ block_1005
  def Row1005't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1005 (r : &Row1005't) -> bool = true
  fn process1005 (r : &Row1005't, dt : f32) -> bool = true
  fn score1005 (r : &Row1005't) -> f32 = 0.0
§ block_1006
  def Row1006't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1006 (r : &Row1006't) -> bool = true
  fn process1006 (r : &Row1006't, dt : f32) -> bool = true
  fn score1006 (r : &Row1006't) -> f32 = 0.0
§ block_1007
  def Row1007't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1007 (r : &Row1007't) -> bool = true
  fn process1007 (r : &Row1007't, dt : f32) -> bool = true
  fn score1007 (r : &Row1007't) -> f32 = 0.0
§ block_1008
  def Row1008't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1008 (r : &Row1008't) -> bool = true
  fn process1008 (r : &Row1008't, dt : f32) -> bool = true
  fn score1008 (r : &Row1008't) -> f32 = 0.0
§ block_1009
  def Row1009't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1009 (r : &Row1009't) -> bool = true
  fn process1009 (r : &Row1009't, dt : f32) -> bool = true
  fn score1009 (r : &Row1009't) -> f32 = 0.0
§ block_1010
  def Row1010't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1010 (r : &Row1010't) -> bool = true
  fn process1010 (r : &Row1010't, dt : f32) -> bool = true
  fn score1010 (r : &Row1010't) -> f32 = 0.0
§ block_1011
  def Row1011't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1011 (r : &Row1011't) -> bool = true
  fn process1011 (r : &Row1011't, dt : f32) -> bool = true
  fn score1011 (r : &Row1011't) -> f32 = 0.0
§ block_1012
  def Row1012't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1012 (r : &Row1012't) -> bool = true
  fn process1012 (r : &Row1012't, dt : f32) -> bool = true
  fn score1012 (r : &Row1012't) -> f32 = 0.0
§ block_1013
  def Row1013't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1013 (r : &Row1013't) -> bool = true
  fn process1013 (r : &Row1013't, dt : f32) -> bool = true
  fn score1013 (r : &Row1013't) -> f32 = 0.0
§ block_1014
  def Row1014't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1014 (r : &Row1014't) -> bool = true
  fn process1014 (r : &Row1014't, dt : f32) -> bool = true
  fn score1014 (r : &Row1014't) -> f32 = 0.0
§ block_1015
  def Row1015't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1015 (r : &Row1015't) -> bool = true
  fn process1015 (r : &Row1015't, dt : f32) -> bool = true
  fn score1015 (r : &Row1015't) -> f32 = 0.0
§ block_1016
  def Row1016't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1016 (r : &Row1016't) -> bool = true
  fn process1016 (r : &Row1016't, dt : f32) -> bool = true
  fn score1016 (r : &Row1016't) -> f32 = 0.0
§ block_1017
  def Row1017't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1017 (r : &Row1017't) -> bool = true
  fn process1017 (r : &Row1017't, dt : f32) -> bool = true
  fn score1017 (r : &Row1017't) -> f32 = 0.0
§ block_1018
  def Row1018't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1018 (r : &Row1018't) -> bool = true
  fn process1018 (r : &Row1018't, dt : f32) -> bool = true
  fn score1018 (r : &Row1018't) -> f32 = 0.0
§ block_1019
  def Row1019't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1019 (r : &Row1019't) -> bool = true
  fn process1019 (r : &Row1019't, dt : f32) -> bool = true
  fn score1019 (r : &Row1019't) -> f32 = 0.0
§ block_1020
  def Row1020't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1020 (r : &Row1020't) -> bool = true
  fn process1020 (r : &Row1020't, dt : f32) -> bool = true
  fn score1020 (r : &Row1020't) -> f32 = 0.0
§ block_1021
  def Row1021't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1021 (r : &Row1021't) -> bool = true
  fn process1021 (r : &Row1021't, dt : f32) -> bool = true
  fn score1021 (r : &Row1021't) -> f32 = 0.0
§ block_1022
  def Row1022't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1022 (r : &Row1022't) -> bool = true
  fn process1022 (r : &Row1022't, dt : f32) -> bool = true
  fn score1022 (r : &Row1022't) -> f32 = 0.0
§ block_1023
  def Row1023't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1023 (r : &Row1023't) -> bool = true
  fn process1023 (r : &Row1023't, dt : f32) -> bool = true
  fn score1023 (r : &Row1023't) -> f32 = 0.0
§ block_1024
  def Row1024't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1024 (r : &Row1024't) -> bool = true
  fn process1024 (r : &Row1024't, dt : f32) -> bool = true
  fn score1024 (r : &Row1024't) -> f32 = 0.0
§ block_1025
  def Row1025't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1025 (r : &Row1025't) -> bool = true
  fn process1025 (r : &Row1025't, dt : f32) -> bool = true
  fn score1025 (r : &Row1025't) -> f32 = 0.0
§ block_1026
  def Row1026't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1026 (r : &Row1026't) -> bool = true
  fn process1026 (r : &Row1026't, dt : f32) -> bool = true
  fn score1026 (r : &Row1026't) -> f32 = 0.0
§ block_1027
  def Row1027't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1027 (r : &Row1027't) -> bool = true
  fn process1027 (r : &Row1027't, dt : f32) -> bool = true
  fn score1027 (r : &Row1027't) -> f32 = 0.0
§ block_1028
  def Row1028't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1028 (r : &Row1028't) -> bool = true
  fn process1028 (r : &Row1028't, dt : f32) -> bool = true
  fn score1028 (r : &Row1028't) -> f32 = 0.0
§ block_1029
  def Row1029't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1029 (r : &Row1029't) -> bool = true
  fn process1029 (r : &Row1029't, dt : f32) -> bool = true
  fn score1029 (r : &Row1029't) -> f32 = 0.0
§ block_1030
  def Row1030't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1030 (r : &Row1030't) -> bool = true
  fn process1030 (r : &Row1030't, dt : f32) -> bool = true
  fn score1030 (r : &Row1030't) -> f32 = 0.0
§ block_1031
  def Row1031't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1031 (r : &Row1031't) -> bool = true
  fn process1031 (r : &Row1031't, dt : f32) -> bool = true
  fn score1031 (r : &Row1031't) -> f32 = 0.0
§ block_1032
  def Row1032't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1032 (r : &Row1032't) -> bool = true
  fn process1032 (r : &Row1032't, dt : f32) -> bool = true
  fn score1032 (r : &Row1032't) -> f32 = 0.0
§ block_1033
  def Row1033't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1033 (r : &Row1033't) -> bool = true
  fn process1033 (r : &Row1033't, dt : f32) -> bool = true
  fn score1033 (r : &Row1033't) -> f32 = 0.0
§ block_1034
  def Row1034't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1034 (r : &Row1034't) -> bool = true
  fn process1034 (r : &Row1034't, dt : f32) -> bool = true
  fn score1034 (r : &Row1034't) -> f32 = 0.0
§ block_1035
  def Row1035't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1035 (r : &Row1035't) -> bool = true
  fn process1035 (r : &Row1035't, dt : f32) -> bool = true
  fn score1035 (r : &Row1035't) -> f32 = 0.0
§ block_1036
  def Row1036't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1036 (r : &Row1036't) -> bool = true
  fn process1036 (r : &Row1036't, dt : f32) -> bool = true
  fn score1036 (r : &Row1036't) -> f32 = 0.0
§ block_1037
  def Row1037't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1037 (r : &Row1037't) -> bool = true
  fn process1037 (r : &Row1037't, dt : f32) -> bool = true
  fn score1037 (r : &Row1037't) -> f32 = 0.0
§ block_1038
  def Row1038't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1038 (r : &Row1038't) -> bool = true
  fn process1038 (r : &Row1038't, dt : f32) -> bool = true
  fn score1038 (r : &Row1038't) -> f32 = 0.0
§ block_1039
  def Row1039't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1039 (r : &Row1039't) -> bool = true
  fn process1039 (r : &Row1039't, dt : f32) -> bool = true
  fn score1039 (r : &Row1039't) -> f32 = 0.0
§ block_1040
  def Row1040't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1040 (r : &Row1040't) -> bool = true
  fn process1040 (r : &Row1040't, dt : f32) -> bool = true
  fn score1040 (r : &Row1040't) -> f32 = 0.0
§ block_1041
  def Row1041't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1041 (r : &Row1041't) -> bool = true
  fn process1041 (r : &Row1041't, dt : f32) -> bool = true
  fn score1041 (r : &Row1041't) -> f32 = 0.0
§ block_1042
  def Row1042't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1042 (r : &Row1042't) -> bool = true
  fn process1042 (r : &Row1042't, dt : f32) -> bool = true
  fn score1042 (r : &Row1042't) -> f32 = 0.0
§ block_1043
  def Row1043't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1043 (r : &Row1043't) -> bool = true
  fn process1043 (r : &Row1043't, dt : f32) -> bool = true
  fn score1043 (r : &Row1043't) -> f32 = 0.0
§ block_1044
  def Row1044't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1044 (r : &Row1044't) -> bool = true
  fn process1044 (r : &Row1044't, dt : f32) -> bool = true
  fn score1044 (r : &Row1044't) -> f32 = 0.0
§ block_1045
  def Row1045't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1045 (r : &Row1045't) -> bool = true
  fn process1045 (r : &Row1045't, dt : f32) -> bool = true
  fn score1045 (r : &Row1045't) -> f32 = 0.0
§ block_1046
  def Row1046't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1046 (r : &Row1046't) -> bool = true
  fn process1046 (r : &Row1046't, dt : f32) -> bool = true
  fn score1046 (r : &Row1046't) -> f32 = 0.0
§ block_1047
  def Row1047't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1047 (r : &Row1047't) -> bool = true
  fn process1047 (r : &Row1047't, dt : f32) -> bool = true
  fn score1047 (r : &Row1047't) -> f32 = 0.0
§ block_1048
  def Row1048't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1048 (r : &Row1048't) -> bool = true
  fn process1048 (r : &Row1048't, dt : f32) -> bool = true
  fn score1048 (r : &Row1048't) -> f32 = 0.0
§ block_1049
  def Row1049't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1049 (r : &Row1049't) -> bool = true
  fn process1049 (r : &Row1049't, dt : f32) -> bool = true
  fn score1049 (r : &Row1049't) -> f32 = 0.0
§ block_1050
  def Row1050't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1050 (r : &Row1050't) -> bool = true
  fn process1050 (r : &Row1050't, dt : f32) -> bool = true
  fn score1050 (r : &Row1050't) -> f32 = 0.0
§ block_1051
  def Row1051't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1051 (r : &Row1051't) -> bool = true
  fn process1051 (r : &Row1051't, dt : f32) -> bool = true
  fn score1051 (r : &Row1051't) -> f32 = 0.0
§ block_1052
  def Row1052't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1052 (r : &Row1052't) -> bool = true
  fn process1052 (r : &Row1052't, dt : f32) -> bool = true
  fn score1052 (r : &Row1052't) -> f32 = 0.0
§ block_1053
  def Row1053't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1053 (r : &Row1053't) -> bool = true
  fn process1053 (r : &Row1053't, dt : f32) -> bool = true
  fn score1053 (r : &Row1053't) -> f32 = 0.0
§ block_1054
  def Row1054't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1054 (r : &Row1054't) -> bool = true
  fn process1054 (r : &Row1054't, dt : f32) -> bool = true
  fn score1054 (r : &Row1054't) -> f32 = 0.0
§ block_1055
  def Row1055't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1055 (r : &Row1055't) -> bool = true
  fn process1055 (r : &Row1055't, dt : f32) -> bool = true
  fn score1055 (r : &Row1055't) -> f32 = 0.0
§ block_1056
  def Row1056't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1056 (r : &Row1056't) -> bool = true
  fn process1056 (r : &Row1056't, dt : f32) -> bool = true
  fn score1056 (r : &Row1056't) -> f32 = 0.0
§ block_1057
  def Row1057't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1057 (r : &Row1057't) -> bool = true
  fn process1057 (r : &Row1057't, dt : f32) -> bool = true
  fn score1057 (r : &Row1057't) -> f32 = 0.0
§ block_1058
  def Row1058't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1058 (r : &Row1058't) -> bool = true
  fn process1058 (r : &Row1058't, dt : f32) -> bool = true
  fn score1058 (r : &Row1058't) -> f32 = 0.0
§ block_1059
  def Row1059't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1059 (r : &Row1059't) -> bool = true
  fn process1059 (r : &Row1059't, dt : f32) -> bool = true
  fn score1059 (r : &Row1059't) -> f32 = 0.0
§ block_1060
  def Row1060't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1060 (r : &Row1060't) -> bool = true
  fn process1060 (r : &Row1060't, dt : f32) -> bool = true
  fn score1060 (r : &Row1060't) -> f32 = 0.0
§ block_1061
  def Row1061't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1061 (r : &Row1061't) -> bool = true
  fn process1061 (r : &Row1061't, dt : f32) -> bool = true
  fn score1061 (r : &Row1061't) -> f32 = 0.0
§ block_1062
  def Row1062't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1062 (r : &Row1062't) -> bool = true
  fn process1062 (r : &Row1062't, dt : f32) -> bool = true
  fn score1062 (r : &Row1062't) -> f32 = 0.0
§ block_1063
  def Row1063't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1063 (r : &Row1063't) -> bool = true
  fn process1063 (r : &Row1063't, dt : f32) -> bool = true
  fn score1063 (r : &Row1063't) -> f32 = 0.0
§ block_1064
  def Row1064't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1064 (r : &Row1064't) -> bool = true
  fn process1064 (r : &Row1064't, dt : f32) -> bool = true
  fn score1064 (r : &Row1064't) -> f32 = 0.0
§ block_1065
  def Row1065't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1065 (r : &Row1065't) -> bool = true
  fn process1065 (r : &Row1065't, dt : f32) -> bool = true
  fn score1065 (r : &Row1065't) -> f32 = 0.0
§ block_1066
  def Row1066't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1066 (r : &Row1066't) -> bool = true
  fn process1066 (r : &Row1066't, dt : f32) -> bool = true
  fn score1066 (r : &Row1066't) -> f32 = 0.0
§ block_1067
  def Row1067't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1067 (r : &Row1067't) -> bool = true
  fn process1067 (r : &Row1067't, dt : f32) -> bool = true
  fn score1067 (r : &Row1067't) -> f32 = 0.0
§ block_1068
  def Row1068't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1068 (r : &Row1068't) -> bool = true
  fn process1068 (r : &Row1068't, dt : f32) -> bool = true
  fn score1068 (r : &Row1068't) -> f32 = 0.0
§ block_1069
  def Row1069't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1069 (r : &Row1069't) -> bool = true
  fn process1069 (r : &Row1069't, dt : f32) -> bool = true
  fn score1069 (r : &Row1069't) -> f32 = 0.0
§ block_1070
  def Row1070't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1070 (r : &Row1070't) -> bool = true
  fn process1070 (r : &Row1070't, dt : f32) -> bool = true
  fn score1070 (r : &Row1070't) -> f32 = 0.0
§ block_1071
  def Row1071't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1071 (r : &Row1071't) -> bool = true
  fn process1071 (r : &Row1071't, dt : f32) -> bool = true
  fn score1071 (r : &Row1071't) -> f32 = 0.0
§ block_1072
  def Row1072't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1072 (r : &Row1072't) -> bool = true
  fn process1072 (r : &Row1072't, dt : f32) -> bool = true
  fn score1072 (r : &Row1072't) -> f32 = 0.0
§ block_1073
  def Row1073't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1073 (r : &Row1073't) -> bool = true
  fn process1073 (r : &Row1073't, dt : f32) -> bool = true
  fn score1073 (r : &Row1073't) -> f32 = 0.0
§ block_1074
  def Row1074't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1074 (r : &Row1074't) -> bool = true
  fn process1074 (r : &Row1074't, dt : f32) -> bool = true
  fn score1074 (r : &Row1074't) -> f32 = 0.0
§ block_1075
  def Row1075't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1075 (r : &Row1075't) -> bool = true
  fn process1075 (r : &Row1075't, dt : f32) -> bool = true
  fn score1075 (r : &Row1075't) -> f32 = 0.0
§ block_1076
  def Row1076't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1076 (r : &Row1076't) -> bool = true
  fn process1076 (r : &Row1076't, dt : f32) -> bool = true
  fn score1076 (r : &Row1076't) -> f32 = 0.0
§ block_1077
  def Row1077't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1077 (r : &Row1077't) -> bool = true
  fn process1077 (r : &Row1077't, dt : f32) -> bool = true
  fn score1077 (r : &Row1077't) -> f32 = 0.0
§ block_1078
  def Row1078't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1078 (r : &Row1078't) -> bool = true
  fn process1078 (r : &Row1078't, dt : f32) -> bool = true
  fn score1078 (r : &Row1078't) -> f32 = 0.0
§ block_1079
  def Row1079't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1079 (r : &Row1079't) -> bool = true
  fn process1079 (r : &Row1079't, dt : f32) -> bool = true
  fn score1079 (r : &Row1079't) -> f32 = 0.0
§ block_1080
  def Row1080't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1080 (r : &Row1080't) -> bool = true
  fn process1080 (r : &Row1080't, dt : f32) -> bool = true
  fn score1080 (r : &Row1080't) -> f32 = 0.0
§ block_1081
  def Row1081't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1081 (r : &Row1081't) -> bool = true
  fn process1081 (r : &Row1081't, dt : f32) -> bool = true
  fn score1081 (r : &Row1081't) -> f32 = 0.0
§ block_1082
  def Row1082't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1082 (r : &Row1082't) -> bool = true
  fn process1082 (r : &Row1082't, dt : f32) -> bool = true
  fn score1082 (r : &Row1082't) -> f32 = 0.0
§ block_1083
  def Row1083't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1083 (r : &Row1083't) -> bool = true
  fn process1083 (r : &Row1083't, dt : f32) -> bool = true
  fn score1083 (r : &Row1083't) -> f32 = 0.0
§ block_1084
  def Row1084't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1084 (r : &Row1084't) -> bool = true
  fn process1084 (r : &Row1084't, dt : f32) -> bool = true
  fn score1084 (r : &Row1084't) -> f32 = 0.0
§ block_1085
  def Row1085't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1085 (r : &Row1085't) -> bool = true
  fn process1085 (r : &Row1085't, dt : f32) -> bool = true
  fn score1085 (r : &Row1085't) -> f32 = 0.0
§ block_1086
  def Row1086't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1086 (r : &Row1086't) -> bool = true
  fn process1086 (r : &Row1086't, dt : f32) -> bool = true
  fn score1086 (r : &Row1086't) -> f32 = 0.0
§ block_1087
  def Row1087't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1087 (r : &Row1087't) -> bool = true
  fn process1087 (r : &Row1087't, dt : f32) -> bool = true
  fn score1087 (r : &Row1087't) -> f32 = 0.0
§ block_1088
  def Row1088't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1088 (r : &Row1088't) -> bool = true
  fn process1088 (r : &Row1088't, dt : f32) -> bool = true
  fn score1088 (r : &Row1088't) -> f32 = 0.0
§ block_1089
  def Row1089't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1089 (r : &Row1089't) -> bool = true
  fn process1089 (r : &Row1089't, dt : f32) -> bool = true
  fn score1089 (r : &Row1089't) -> f32 = 0.0
§ block_1090
  def Row1090't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1090 (r : &Row1090't) -> bool = true
  fn process1090 (r : &Row1090't, dt : f32) -> bool = true
  fn score1090 (r : &Row1090't) -> f32 = 0.0
§ block_1091
  def Row1091't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1091 (r : &Row1091't) -> bool = true
  fn process1091 (r : &Row1091't, dt : f32) -> bool = true
  fn score1091 (r : &Row1091't) -> f32 = 0.0
§ block_1092
  def Row1092't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1092 (r : &Row1092't) -> bool = true
  fn process1092 (r : &Row1092't, dt : f32) -> bool = true
  fn score1092 (r : &Row1092't) -> f32 = 0.0
§ block_1093
  def Row1093't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1093 (r : &Row1093't) -> bool = true
  fn process1093 (r : &Row1093't, dt : f32) -> bool = true
  fn score1093 (r : &Row1093't) -> f32 = 0.0
§ block_1094
  def Row1094't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1094 (r : &Row1094't) -> bool = true
  fn process1094 (r : &Row1094't, dt : f32) -> bool = true
  fn score1094 (r : &Row1094't) -> f32 = 0.0
§ block_1095
  def Row1095't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1095 (r : &Row1095't) -> bool = true
  fn process1095 (r : &Row1095't, dt : f32) -> bool = true
  fn score1095 (r : &Row1095't) -> f32 = 0.0
§ block_1096
  def Row1096't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1096 (r : &Row1096't) -> bool = true
  fn process1096 (r : &Row1096't, dt : f32) -> bool = true
  fn score1096 (r : &Row1096't) -> f32 = 0.0
§ block_1097
  def Row1097't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1097 (r : &Row1097't) -> bool = true
  fn process1097 (r : &Row1097't, dt : f32) -> bool = true
  fn score1097 (r : &Row1097't) -> f32 = 0.0
§ block_1098
  def Row1098't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1098 (r : &Row1098't) -> bool = true
  fn process1098 (r : &Row1098't, dt : f32) -> bool = true
  fn score1098 (r : &Row1098't) -> f32 = 0.0
§ block_1099
  def Row1099't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1099 (r : &Row1099't) -> bool = true
  fn process1099 (r : &Row1099't, dt : f32) -> bool = true
  fn score1099 (r : &Row1099't) -> f32 = 0.0
§ block_1100
  def Row1100't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1100 (r : &Row1100't) -> bool = true
  fn process1100 (r : &Row1100't, dt : f32) -> bool = true
  fn score1100 (r : &Row1100't) -> f32 = 0.0
§ block_1101
  def Row1101't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1101 (r : &Row1101't) -> bool = true
  fn process1101 (r : &Row1101't, dt : f32) -> bool = true
  fn score1101 (r : &Row1101't) -> f32 = 0.0
§ block_1102
  def Row1102't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1102 (r : &Row1102't) -> bool = true
  fn process1102 (r : &Row1102't, dt : f32) -> bool = true
  fn score1102 (r : &Row1102't) -> f32 = 0.0
§ block_1103
  def Row1103't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1103 (r : &Row1103't) -> bool = true
  fn process1103 (r : &Row1103't, dt : f32) -> bool = true
  fn score1103 (r : &Row1103't) -> f32 = 0.0
§ block_1104
  def Row1104't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1104 (r : &Row1104't) -> bool = true
  fn process1104 (r : &Row1104't, dt : f32) -> bool = true
  fn score1104 (r : &Row1104't) -> f32 = 0.0
§ block_1105
  def Row1105't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1105 (r : &Row1105't) -> bool = true
  fn process1105 (r : &Row1105't, dt : f32) -> bool = true
  fn score1105 (r : &Row1105't) -> f32 = 0.0
§ block_1106
  def Row1106't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1106 (r : &Row1106't) -> bool = true
  fn process1106 (r : &Row1106't, dt : f32) -> bool = true
  fn score1106 (r : &Row1106't) -> f32 = 0.0
§ block_1107
  def Row1107't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1107 (r : &Row1107't) -> bool = true
  fn process1107 (r : &Row1107't, dt : f32) -> bool = true
  fn score1107 (r : &Row1107't) -> f32 = 0.0
§ block_1108
  def Row1108't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1108 (r : &Row1108't) -> bool = true
  fn process1108 (r : &Row1108't, dt : f32) -> bool = true
  fn score1108 (r : &Row1108't) -> f32 = 0.0
§ block_1109
  def Row1109't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1109 (r : &Row1109't) -> bool = true
  fn process1109 (r : &Row1109't, dt : f32) -> bool = true
  fn score1109 (r : &Row1109't) -> f32 = 0.0
§ block_1110
  def Row1110't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1110 (r : &Row1110't) -> bool = true
  fn process1110 (r : &Row1110't, dt : f32) -> bool = true
  fn score1110 (r : &Row1110't) -> f32 = 0.0
§ block_1111
  def Row1111't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1111 (r : &Row1111't) -> bool = true
  fn process1111 (r : &Row1111't, dt : f32) -> bool = true
  fn score1111 (r : &Row1111't) -> f32 = 0.0
§ block_1112
  def Row1112't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1112 (r : &Row1112't) -> bool = true
  fn process1112 (r : &Row1112't, dt : f32) -> bool = true
  fn score1112 (r : &Row1112't) -> f32 = 0.0
§ block_1113
  def Row1113't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1113 (r : &Row1113't) -> bool = true
  fn process1113 (r : &Row1113't, dt : f32) -> bool = true
  fn score1113 (r : &Row1113't) -> f32 = 0.0
§ block_1114
  def Row1114't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1114 (r : &Row1114't) -> bool = true
  fn process1114 (r : &Row1114't, dt : f32) -> bool = true
  fn score1114 (r : &Row1114't) -> f32 = 0.0
§ block_1115
  def Row1115't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1115 (r : &Row1115't) -> bool = true
  fn process1115 (r : &Row1115't, dt : f32) -> bool = true
  fn score1115 (r : &Row1115't) -> f32 = 0.0
§ block_1116
  def Row1116't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1116 (r : &Row1116't) -> bool = true
  fn process1116 (r : &Row1116't, dt : f32) -> bool = true
  fn score1116 (r : &Row1116't) -> f32 = 0.0
§ block_1117
  def Row1117't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1117 (r : &Row1117't) -> bool = true
  fn process1117 (r : &Row1117't, dt : f32) -> bool = true
  fn score1117 (r : &Row1117't) -> f32 = 0.0
§ block_1118
  def Row1118't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1118 (r : &Row1118't) -> bool = true
  fn process1118 (r : &Row1118't, dt : f32) -> bool = true
  fn score1118 (r : &Row1118't) -> f32 = 0.0
§ block_1119
  def Row1119't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1119 (r : &Row1119't) -> bool = true
  fn process1119 (r : &Row1119't, dt : f32) -> bool = true
  fn score1119 (r : &Row1119't) -> f32 = 0.0
§ block_1120
  def Row1120't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1120 (r : &Row1120't) -> bool = true
  fn process1120 (r : &Row1120't, dt : f32) -> bool = true
  fn score1120 (r : &Row1120't) -> f32 = 0.0
§ block_1121
  def Row1121't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1121 (r : &Row1121't) -> bool = true
  fn process1121 (r : &Row1121't, dt : f32) -> bool = true
  fn score1121 (r : &Row1121't) -> f32 = 0.0
§ block_1122
  def Row1122't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1122 (r : &Row1122't) -> bool = true
  fn process1122 (r : &Row1122't, dt : f32) -> bool = true
  fn score1122 (r : &Row1122't) -> f32 = 0.0
§ block_1123
  def Row1123't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1123 (r : &Row1123't) -> bool = true
  fn process1123 (r : &Row1123't, dt : f32) -> bool = true
  fn score1123 (r : &Row1123't) -> f32 = 0.0
§ block_1124
  def Row1124't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1124 (r : &Row1124't) -> bool = true
  fn process1124 (r : &Row1124't, dt : f32) -> bool = true
  fn score1124 (r : &Row1124't) -> f32 = 0.0
§ block_1125
  def Row1125't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1125 (r : &Row1125't) -> bool = true
  fn process1125 (r : &Row1125't, dt : f32) -> bool = true
  fn score1125 (r : &Row1125't) -> f32 = 0.0
§ block_1126
  def Row1126't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1126 (r : &Row1126't) -> bool = true
  fn process1126 (r : &Row1126't, dt : f32) -> bool = true
  fn score1126 (r : &Row1126't) -> f32 = 0.0
§ block_1127
  def Row1127't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1127 (r : &Row1127't) -> bool = true
  fn process1127 (r : &Row1127't, dt : f32) -> bool = true
  fn score1127 (r : &Row1127't) -> f32 = 0.0
§ block_1128
  def Row1128't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1128 (r : &Row1128't) -> bool = true
  fn process1128 (r : &Row1128't, dt : f32) -> bool = true
  fn score1128 (r : &Row1128't) -> f32 = 0.0
§ block_1129
  def Row1129't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1129 (r : &Row1129't) -> bool = true
  fn process1129 (r : &Row1129't, dt : f32) -> bool = true
  fn score1129 (r : &Row1129't) -> f32 = 0.0
§ block_1130
  def Row1130't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1130 (r : &Row1130't) -> bool = true
  fn process1130 (r : &Row1130't, dt : f32) -> bool = true
  fn score1130 (r : &Row1130't) -> f32 = 0.0
§ block_1131
  def Row1131't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1131 (r : &Row1131't) -> bool = true
  fn process1131 (r : &Row1131't, dt : f32) -> bool = true
  fn score1131 (r : &Row1131't) -> f32 = 0.0
§ block_1132
  def Row1132't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1132 (r : &Row1132't) -> bool = true
  fn process1132 (r : &Row1132't, dt : f32) -> bool = true
  fn score1132 (r : &Row1132't) -> f32 = 0.0
§ block_1133
  def Row1133't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1133 (r : &Row1133't) -> bool = true
  fn process1133 (r : &Row1133't, dt : f32) -> bool = true
  fn score1133 (r : &Row1133't) -> f32 = 0.0
§ block_1134
  def Row1134't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1134 (r : &Row1134't) -> bool = true
  fn process1134 (r : &Row1134't, dt : f32) -> bool = true
  fn score1134 (r : &Row1134't) -> f32 = 0.0
§ block_1135
  def Row1135't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1135 (r : &Row1135't) -> bool = true
  fn process1135 (r : &Row1135't, dt : f32) -> bool = true
  fn score1135 (r : &Row1135't) -> f32 = 0.0
§ block_1136
  def Row1136't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1136 (r : &Row1136't) -> bool = true
  fn process1136 (r : &Row1136't, dt : f32) -> bool = true
  fn score1136 (r : &Row1136't) -> f32 = 0.0
§ block_1137
  def Row1137't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1137 (r : &Row1137't) -> bool = true
  fn process1137 (r : &Row1137't, dt : f32) -> bool = true
  fn score1137 (r : &Row1137't) -> f32 = 0.0
§ block_1138
  def Row1138't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1138 (r : &Row1138't) -> bool = true
  fn process1138 (r : &Row1138't, dt : f32) -> bool = true
  fn score1138 (r : &Row1138't) -> f32 = 0.0
§ block_1139
  def Row1139't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1139 (r : &Row1139't) -> bool = true
  fn process1139 (r : &Row1139't, dt : f32) -> bool = true
  fn score1139 (r : &Row1139't) -> f32 = 0.0
§ block_1140
  def Row1140't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1140 (r : &Row1140't) -> bool = true
  fn process1140 (r : &Row1140't, dt : f32) -> bool = true
  fn score1140 (r : &Row1140't) -> f32 = 0.0
§ block_1141
  def Row1141't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1141 (r : &Row1141't) -> bool = true
  fn process1141 (r : &Row1141't, dt : f32) -> bool = true
  fn score1141 (r : &Row1141't) -> f32 = 0.0
§ block_1142
  def Row1142't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1142 (r : &Row1142't) -> bool = true
  fn process1142 (r : &Row1142't, dt : f32) -> bool = true
  fn score1142 (r : &Row1142't) -> f32 = 0.0
§ block_1143
  def Row1143't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1143 (r : &Row1143't) -> bool = true
  fn process1143 (r : &Row1143't, dt : f32) -> bool = true
  fn score1143 (r : &Row1143't) -> f32 = 0.0
§ block_1144
  def Row1144't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1144 (r : &Row1144't) -> bool = true
  fn process1144 (r : &Row1144't, dt : f32) -> bool = true
  fn score1144 (r : &Row1144't) -> f32 = 0.0
§ block_1145
  def Row1145't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1145 (r : &Row1145't) -> bool = true
  fn process1145 (r : &Row1145't, dt : f32) -> bool = true
  fn score1145 (r : &Row1145't) -> f32 = 0.0
§ block_1146
  def Row1146't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1146 (r : &Row1146't) -> bool = true
  fn process1146 (r : &Row1146't, dt : f32) -> bool = true
  fn score1146 (r : &Row1146't) -> f32 = 0.0
§ block_1147
  def Row1147't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1147 (r : &Row1147't) -> bool = true
  fn process1147 (r : &Row1147't, dt : f32) -> bool = true
  fn score1147 (r : &Row1147't) -> f32 = 0.0
§ block_1148
  def Row1148't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1148 (r : &Row1148't) -> bool = true
  fn process1148 (r : &Row1148't, dt : f32) -> bool = true
  fn score1148 (r : &Row1148't) -> f32 = 0.0
§ block_1149
  def Row1149't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1149 (r : &Row1149't) -> bool = true
  fn process1149 (r : &Row1149't, dt : f32) -> bool = true
  fn score1149 (r : &Row1149't) -> f32 = 0.0
§ block_1150
  def Row1150't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1150 (r : &Row1150't) -> bool = true
  fn process1150 (r : &Row1150't, dt : f32) -> bool = true
  fn score1150 (r : &Row1150't) -> f32 = 0.0
§ block_1151
  def Row1151't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1151 (r : &Row1151't) -> bool = true
  fn process1151 (r : &Row1151't, dt : f32) -> bool = true
  fn score1151 (r : &Row1151't) -> f32 = 0.0
§ block_1152
  def Row1152't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1152 (r : &Row1152't) -> bool = true
  fn process1152 (r : &Row1152't, dt : f32) -> bool = true
  fn score1152 (r : &Row1152't) -> f32 = 0.0
§ block_1153
  def Row1153't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1153 (r : &Row1153't) -> bool = true
  fn process1153 (r : &Row1153't, dt : f32) -> bool = true
  fn score1153 (r : &Row1153't) -> f32 = 0.0
§ block_1154
  def Row1154't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1154 (r : &Row1154't) -> bool = true
  fn process1154 (r : &Row1154't, dt : f32) -> bool = true
  fn score1154 (r : &Row1154't) -> f32 = 0.0
§ block_1155
  def Row1155't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1155 (r : &Row1155't) -> bool = true
  fn process1155 (r : &Row1155't, dt : f32) -> bool = true
  fn score1155 (r : &Row1155't) -> f32 = 0.0
§ block_1156
  def Row1156't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1156 (r : &Row1156't) -> bool = true
  fn process1156 (r : &Row1156't, dt : f32) -> bool = true
  fn score1156 (r : &Row1156't) -> f32 = 0.0
§ block_1157
  def Row1157't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1157 (r : &Row1157't) -> bool = true
  fn process1157 (r : &Row1157't, dt : f32) -> bool = true
  fn score1157 (r : &Row1157't) -> f32 = 0.0
§ block_1158
  def Row1158't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1158 (r : &Row1158't) -> bool = true
  fn process1158 (r : &Row1158't, dt : f32) -> bool = true
  fn score1158 (r : &Row1158't) -> f32 = 0.0
§ block_1159
  def Row1159't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1159 (r : &Row1159't) -> bool = true
  fn process1159 (r : &Row1159't, dt : f32) -> bool = true
  fn score1159 (r : &Row1159't) -> f32 = 0.0
§ block_1160
  def Row1160't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1160 (r : &Row1160't) -> bool = true
  fn process1160 (r : &Row1160't, dt : f32) -> bool = true
  fn score1160 (r : &Row1160't) -> f32 = 0.0
§ block_1161
  def Row1161't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1161 (r : &Row1161't) -> bool = true
  fn process1161 (r : &Row1161't, dt : f32) -> bool = true
  fn score1161 (r : &Row1161't) -> f32 = 0.0
§ block_1162
  def Row1162't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1162 (r : &Row1162't) -> bool = true
  fn process1162 (r : &Row1162't, dt : f32) -> bool = true
  fn score1162 (r : &Row1162't) -> f32 = 0.0
§ block_1163
  def Row1163't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1163 (r : &Row1163't) -> bool = true
  fn process1163 (r : &Row1163't, dt : f32) -> bool = true
  fn score1163 (r : &Row1163't) -> f32 = 0.0
§ block_1164
  def Row1164't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1164 (r : &Row1164't) -> bool = true
  fn process1164 (r : &Row1164't, dt : f32) -> bool = true
  fn score1164 (r : &Row1164't) -> f32 = 0.0
§ block_1165
  def Row1165't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1165 (r : &Row1165't) -> bool = true
  fn process1165 (r : &Row1165't, dt : f32) -> bool = true
  fn score1165 (r : &Row1165't) -> f32 = 0.0
§ block_1166
  def Row1166't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1166 (r : &Row1166't) -> bool = true
  fn process1166 (r : &Row1166't, dt : f32) -> bool = true
  fn score1166 (r : &Row1166't) -> f32 = 0.0
§ block_1167
  def Row1167't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1167 (r : &Row1167't) -> bool = true
  fn process1167 (r : &Row1167't, dt : f32) -> bool = true
  fn score1167 (r : &Row1167't) -> f32 = 0.0
§ block_1168
  def Row1168't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1168 (r : &Row1168't) -> bool = true
  fn process1168 (r : &Row1168't, dt : f32) -> bool = true
  fn score1168 (r : &Row1168't) -> f32 = 0.0
§ block_1169
  def Row1169't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1169 (r : &Row1169't) -> bool = true
  fn process1169 (r : &Row1169't, dt : f32) -> bool = true
  fn score1169 (r : &Row1169't) -> f32 = 0.0
§ block_1170
  def Row1170't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1170 (r : &Row1170't) -> bool = true
  fn process1170 (r : &Row1170't, dt : f32) -> bool = true
  fn score1170 (r : &Row1170't) -> f32 = 0.0
§ block_1171
  def Row1171't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1171 (r : &Row1171't) -> bool = true
  fn process1171 (r : &Row1171't, dt : f32) -> bool = true
  fn score1171 (r : &Row1171't) -> f32 = 0.0
§ block_1172
  def Row1172't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1172 (r : &Row1172't) -> bool = true
  fn process1172 (r : &Row1172't, dt : f32) -> bool = true
  fn score1172 (r : &Row1172't) -> f32 = 0.0
§ block_1173
  def Row1173't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1173 (r : &Row1173't) -> bool = true
  fn process1173 (r : &Row1173't, dt : f32) -> bool = true
  fn score1173 (r : &Row1173't) -> f32 = 0.0
§ block_1174
  def Row1174't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1174 (r : &Row1174't) -> bool = true
  fn process1174 (r : &Row1174't, dt : f32) -> bool = true
  fn score1174 (r : &Row1174't) -> f32 = 0.0
§ block_1175
  def Row1175't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1175 (r : &Row1175't) -> bool = true
  fn process1175 (r : &Row1175't, dt : f32) -> bool = true
  fn score1175 (r : &Row1175't) -> f32 = 0.0
§ block_1176
  def Row1176't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1176 (r : &Row1176't) -> bool = true
  fn process1176 (r : &Row1176't, dt : f32) -> bool = true
  fn score1176 (r : &Row1176't) -> f32 = 0.0
§ block_1177
  def Row1177't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1177 (r : &Row1177't) -> bool = true
  fn process1177 (r : &Row1177't, dt : f32) -> bool = true
  fn score1177 (r : &Row1177't) -> f32 = 0.0
§ block_1178
  def Row1178't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1178 (r : &Row1178't) -> bool = true
  fn process1178 (r : &Row1178't, dt : f32) -> bool = true
  fn score1178 (r : &Row1178't) -> f32 = 0.0
§ block_1179
  def Row1179't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1179 (r : &Row1179't) -> bool = true
  fn process1179 (r : &Row1179't, dt : f32) -> bool = true
  fn score1179 (r : &Row1179't) -> f32 = 0.0
§ block_1180
  def Row1180't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1180 (r : &Row1180't) -> bool = true
  fn process1180 (r : &Row1180't, dt : f32) -> bool = true
  fn score1180 (r : &Row1180't) -> f32 = 0.0
§ block_1181
  def Row1181't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1181 (r : &Row1181't) -> bool = true
  fn process1181 (r : &Row1181't, dt : f32) -> bool = true
  fn score1181 (r : &Row1181't) -> f32 = 0.0
§ block_1182
  def Row1182't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1182 (r : &Row1182't) -> bool = true
  fn process1182 (r : &Row1182't, dt : f32) -> bool = true
  fn score1182 (r : &Row1182't) -> f32 = 0.0
§ block_1183
  def Row1183't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1183 (r : &Row1183't) -> bool = true
  fn process1183 (r : &Row1183't, dt : f32) -> bool = true
  fn score1183 (r : &Row1183't) -> f32 = 0.0
§ block_1184
  def Row1184't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1184 (r : &Row1184't) -> bool = true
  fn process1184 (r : &Row1184't, dt : f32) -> bool = true
  fn score1184 (r : &Row1184't) -> f32 = 0.0
§ block_1185
  def Row1185't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1185 (r : &Row1185't) -> bool = true
  fn process1185 (r : &Row1185't, dt : f32) -> bool = true
  fn score1185 (r : &Row1185't) -> f32 = 0.0
§ block_1186
  def Row1186't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1186 (r : &Row1186't) -> bool = true
  fn process1186 (r : &Row1186't, dt : f32) -> bool = true
  fn score1186 (r : &Row1186't) -> f32 = 0.0
§ block_1187
  def Row1187't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1187 (r : &Row1187't) -> bool = true
  fn process1187 (r : &Row1187't, dt : f32) -> bool = true
  fn score1187 (r : &Row1187't) -> f32 = 0.0
§ block_1188
  def Row1188't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1188 (r : &Row1188't) -> bool = true
  fn process1188 (r : &Row1188't, dt : f32) -> bool = true
  fn score1188 (r : &Row1188't) -> f32 = 0.0
§ block_1189
  def Row1189't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1189 (r : &Row1189't) -> bool = true
  fn process1189 (r : &Row1189't, dt : f32) -> bool = true
  fn score1189 (r : &Row1189't) -> f32 = 0.0
§ block_1190
  def Row1190't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1190 (r : &Row1190't) -> bool = true
  fn process1190 (r : &Row1190't, dt : f32) -> bool = true
  fn score1190 (r : &Row1190't) -> f32 = 0.0
§ block_1191
  def Row1191't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1191 (r : &Row1191't) -> bool = true
  fn process1191 (r : &Row1191't, dt : f32) -> bool = true
  fn score1191 (r : &Row1191't) -> f32 = 0.0
§ block_1192
  def Row1192't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1192 (r : &Row1192't) -> bool = true
  fn process1192 (r : &Row1192't, dt : f32) -> bool = true
  fn score1192 (r : &Row1192't) -> f32 = 0.0
§ block_1193
  def Row1193't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1193 (r : &Row1193't) -> bool = true
  fn process1193 (r : &Row1193't, dt : f32) -> bool = true
  fn score1193 (r : &Row1193't) -> f32 = 0.0
§ block_1194
  def Row1194't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1194 (r : &Row1194't) -> bool = true
  fn process1194 (r : &Row1194't, dt : f32) -> bool = true
  fn score1194 (r : &Row1194't) -> f32 = 0.0
§ block_1195
  def Row1195't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1195 (r : &Row1195't) -> bool = true
  fn process1195 (r : &Row1195't, dt : f32) -> bool = true
  fn score1195 (r : &Row1195't) -> f32 = 0.0
§ block_1196
  def Row1196't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1196 (r : &Row1196't) -> bool = true
  fn process1196 (r : &Row1196't, dt : f32) -> bool = true
  fn score1196 (r : &Row1196't) -> f32 = 0.0
§ block_1197
  def Row1197't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1197 (r : &Row1197't) -> bool = true
  fn process1197 (r : &Row1197't, dt : f32) -> bool = true
  fn score1197 (r : &Row1197't) -> f32 = 0.0
§ block_1198
  def Row1198't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1198 (r : &Row1198't) -> bool = true
  fn process1198 (r : &Row1198't, dt : f32) -> bool = true
  fn score1198 (r : &Row1198't) -> f32 = 0.0
§ block_1199
  def Row1199't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1199 (r : &Row1199't) -> bool = true
  fn process1199 (r : &Row1199't, dt : f32) -> bool = true
  fn score1199 (r : &Row1199't) -> f32 = 0.0
§ block_1200
  def Row1200't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1200 (r : &Row1200't) -> bool = true
  fn process1200 (r : &Row1200't, dt : f32) -> bool = true
  fn score1200 (r : &Row1200't) -> f32 = 0.0
§ block_1201
  def Row1201't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1201 (r : &Row1201't) -> bool = true
  fn process1201 (r : &Row1201't, dt : f32) -> bool = true
  fn score1201 (r : &Row1201't) -> f32 = 0.0
§ block_1202
  def Row1202't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1202 (r : &Row1202't) -> bool = true
  fn process1202 (r : &Row1202't, dt : f32) -> bool = true
  fn score1202 (r : &Row1202't) -> f32 = 0.0
§ block_1203
  def Row1203't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1203 (r : &Row1203't) -> bool = true
  fn process1203 (r : &Row1203't, dt : f32) -> bool = true
  fn score1203 (r : &Row1203't) -> f32 = 0.0
§ block_1204
  def Row1204't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1204 (r : &Row1204't) -> bool = true
  fn process1204 (r : &Row1204't, dt : f32) -> bool = true
  fn score1204 (r : &Row1204't) -> f32 = 0.0
§ block_1205
  def Row1205't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1205 (r : &Row1205't) -> bool = true
  fn process1205 (r : &Row1205't, dt : f32) -> bool = true
  fn score1205 (r : &Row1205't) -> f32 = 0.0
§ block_1206
  def Row1206't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1206 (r : &Row1206't) -> bool = true
  fn process1206 (r : &Row1206't, dt : f32) -> bool = true
  fn score1206 (r : &Row1206't) -> f32 = 0.0
§ block_1207
  def Row1207't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1207 (r : &Row1207't) -> bool = true
  fn process1207 (r : &Row1207't, dt : f32) -> bool = true
  fn score1207 (r : &Row1207't) -> f32 = 0.0
§ block_1208
  def Row1208't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1208 (r : &Row1208't) -> bool = true
  fn process1208 (r : &Row1208't, dt : f32) -> bool = true
  fn score1208 (r : &Row1208't) -> f32 = 0.0
§ block_1209
  def Row1209't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1209 (r : &Row1209't) -> bool = true
  fn process1209 (r : &Row1209't, dt : f32) -> bool = true
  fn score1209 (r : &Row1209't) -> f32 = 0.0
§ block_1210
  def Row1210't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1210 (r : &Row1210't) -> bool = true
  fn process1210 (r : &Row1210't, dt : f32) -> bool = true
  fn score1210 (r : &Row1210't) -> f32 = 0.0
§ block_1211
  def Row1211't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1211 (r : &Row1211't) -> bool = true
  fn process1211 (r : &Row1211't, dt : f32) -> bool = true
  fn score1211 (r : &Row1211't) -> f32 = 0.0
§ block_1212
  def Row1212't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1212 (r : &Row1212't) -> bool = true
  fn process1212 (r : &Row1212't, dt : f32) -> bool = true
  fn score1212 (r : &Row1212't) -> f32 = 0.0
§ block_1213
  def Row1213't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1213 (r : &Row1213't) -> bool = true
  fn process1213 (r : &Row1213't, dt : f32) -> bool = true
  fn score1213 (r : &Row1213't) -> f32 = 0.0
§ block_1214
  def Row1214't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1214 (r : &Row1214't) -> bool = true
  fn process1214 (r : &Row1214't, dt : f32) -> bool = true
  fn score1214 (r : &Row1214't) -> f32 = 0.0
§ block_1215
  def Row1215't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1215 (r : &Row1215't) -> bool = true
  fn process1215 (r : &Row1215't, dt : f32) -> bool = true
  fn score1215 (r : &Row1215't) -> f32 = 0.0
§ block_1216
  def Row1216't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1216 (r : &Row1216't) -> bool = true
  fn process1216 (r : &Row1216't, dt : f32) -> bool = true
  fn score1216 (r : &Row1216't) -> f32 = 0.0
§ block_1217
  def Row1217't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1217 (r : &Row1217't) -> bool = true
  fn process1217 (r : &Row1217't, dt : f32) -> bool = true
  fn score1217 (r : &Row1217't) -> f32 = 0.0
§ block_1218
  def Row1218't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1218 (r : &Row1218't) -> bool = true
  fn process1218 (r : &Row1218't, dt : f32) -> bool = true
  fn score1218 (r : &Row1218't) -> f32 = 0.0
§ block_1219
  def Row1219't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1219 (r : &Row1219't) -> bool = true
  fn process1219 (r : &Row1219't, dt : f32) -> bool = true
  fn score1219 (r : &Row1219't) -> f32 = 0.0
§ block_1220
  def Row1220't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1220 (r : &Row1220't) -> bool = true
  fn process1220 (r : &Row1220't, dt : f32) -> bool = true
  fn score1220 (r : &Row1220't) -> f32 = 0.0
§ block_1221
  def Row1221't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1221 (r : &Row1221't) -> bool = true
  fn process1221 (r : &Row1221't, dt : f32) -> bool = true
  fn score1221 (r : &Row1221't) -> f32 = 0.0
§ block_1222
  def Row1222't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1222 (r : &Row1222't) -> bool = true
  fn process1222 (r : &Row1222't, dt : f32) -> bool = true
  fn score1222 (r : &Row1222't) -> f32 = 0.0
§ block_1223
  def Row1223't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1223 (r : &Row1223't) -> bool = true
  fn process1223 (r : &Row1223't, dt : f32) -> bool = true
  fn score1223 (r : &Row1223't) -> f32 = 0.0
§ block_1224
  def Row1224't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1224 (r : &Row1224't) -> bool = true
  fn process1224 (r : &Row1224't, dt : f32) -> bool = true
  fn score1224 (r : &Row1224't) -> f32 = 0.0
§ block_1225
  def Row1225't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1225 (r : &Row1225't) -> bool = true
  fn process1225 (r : &Row1225't, dt : f32) -> bool = true
  fn score1225 (r : &Row1225't) -> f32 = 0.0
§ block_1226
  def Row1226't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1226 (r : &Row1226't) -> bool = true
  fn process1226 (r : &Row1226't, dt : f32) -> bool = true
  fn score1226 (r : &Row1226't) -> f32 = 0.0
§ block_1227
  def Row1227't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1227 (r : &Row1227't) -> bool = true
  fn process1227 (r : &Row1227't, dt : f32) -> bool = true
  fn score1227 (r : &Row1227't) -> f32 = 0.0
§ block_1228
  def Row1228't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1228 (r : &Row1228't) -> bool = true
  fn process1228 (r : &Row1228't, dt : f32) -> bool = true
  fn score1228 (r : &Row1228't) -> f32 = 0.0
§ block_1229
  def Row1229't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1229 (r : &Row1229't) -> bool = true
  fn process1229 (r : &Row1229't, dt : f32) -> bool = true
  fn score1229 (r : &Row1229't) -> f32 = 0.0
§ block_1230
  def Row1230't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1230 (r : &Row1230't) -> bool = true
  fn process1230 (r : &Row1230't, dt : f32) -> bool = true
  fn score1230 (r : &Row1230't) -> f32 = 0.0
§ block_1231
  def Row1231't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1231 (r : &Row1231't) -> bool = true
  fn process1231 (r : &Row1231't, dt : f32) -> bool = true
  fn score1231 (r : &Row1231't) -> f32 = 0.0
§ block_1232
  def Row1232't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1232 (r : &Row1232't) -> bool = true
  fn process1232 (r : &Row1232't, dt : f32) -> bool = true
  fn score1232 (r : &Row1232't) -> f32 = 0.0
§ block_1233
  def Row1233't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1233 (r : &Row1233't) -> bool = true
  fn process1233 (r : &Row1233't, dt : f32) -> bool = true
  fn score1233 (r : &Row1233't) -> f32 = 0.0
§ block_1234
  def Row1234't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1234 (r : &Row1234't) -> bool = true
  fn process1234 (r : &Row1234't, dt : f32) -> bool = true
  fn score1234 (r : &Row1234't) -> f32 = 0.0
§ block_1235
  def Row1235't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1235 (r : &Row1235't) -> bool = true
  fn process1235 (r : &Row1235't, dt : f32) -> bool = true
  fn score1235 (r : &Row1235't) -> f32 = 0.0
§ block_1236
  def Row1236't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1236 (r : &Row1236't) -> bool = true
  fn process1236 (r : &Row1236't, dt : f32) -> bool = true
  fn score1236 (r : &Row1236't) -> f32 = 0.0
§ block_1237
  def Row1237't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1237 (r : &Row1237't) -> bool = true
  fn process1237 (r : &Row1237't, dt : f32) -> bool = true
  fn score1237 (r : &Row1237't) -> f32 = 0.0
§ block_1238
  def Row1238't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1238 (r : &Row1238't) -> bool = true
  fn process1238 (r : &Row1238't, dt : f32) -> bool = true
  fn score1238 (r : &Row1238't) -> f32 = 0.0
§ block_1239
  def Row1239't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1239 (r : &Row1239't) -> bool = true
  fn process1239 (r : &Row1239't, dt : f32) -> bool = true
  fn score1239 (r : &Row1239't) -> f32 = 0.0
§ block_1240
  def Row1240't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1240 (r : &Row1240't) -> bool = true
  fn process1240 (r : &Row1240't, dt : f32) -> bool = true
  fn score1240 (r : &Row1240't) -> f32 = 0.0
§ block_1241
  def Row1241't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1241 (r : &Row1241't) -> bool = true
  fn process1241 (r : &Row1241't, dt : f32) -> bool = true
  fn score1241 (r : &Row1241't) -> f32 = 0.0
§ block_1242
  def Row1242't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1242 (r : &Row1242't) -> bool = true
  fn process1242 (r : &Row1242't, dt : f32) -> bool = true
  fn score1242 (r : &Row1242't) -> f32 = 0.0
§ block_1243
  def Row1243't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1243 (r : &Row1243't) -> bool = true
  fn process1243 (r : &Row1243't, dt : f32) -> bool = true
  fn score1243 (r : &Row1243't) -> f32 = 0.0
§ block_1244
  def Row1244't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1244 (r : &Row1244't) -> bool = true
  fn process1244 (r : &Row1244't, dt : f32) -> bool = true
  fn score1244 (r : &Row1244't) -> f32 = 0.0
§ block_1245
  def Row1245't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1245 (r : &Row1245't) -> bool = true
  fn process1245 (r : &Row1245't, dt : f32) -> bool = true
  fn score1245 (r : &Row1245't) -> f32 = 0.0
§ block_1246
  def Row1246't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1246 (r : &Row1246't) -> bool = true
  fn process1246 (r : &Row1246't, dt : f32) -> bool = true
  fn score1246 (r : &Row1246't) -> f32 = 0.0
§ block_1247
  def Row1247't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1247 (r : &Row1247't) -> bool = true
  fn process1247 (r : &Row1247't, dt : f32) -> bool = true
  fn score1247 (r : &Row1247't) -> f32 = 0.0
§ block_1248
  def Row1248't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1248 (r : &Row1248't) -> bool = true
  fn process1248 (r : &Row1248't, dt : f32) -> bool = true
  fn score1248 (r : &Row1248't) -> f32 = 0.0
§ block_1249
  def Row1249't ⟨
    id       : u32
    name     : str
    value    : f32 ⌈0..1⌉
    active   : bool
  ⟩
  fn handle1249 (r : &Row1249't) -> bool = true
  fn process1249 (r : &Row1249't, dt : f32) -> bool = true
  fn score1249 (r : &Row1249't) -> f32 = 0.0
