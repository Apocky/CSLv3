% schema: latex-v1
\documentclass[11pt]{article}
\usepackage{cslv3}
\usepackage[margin=1in]{geometry}
\usepackage{xunicode}
\usepackage{xltxtra}
\title%!(MISSING CLOSE BRACE)s}
%!(EXTRA C7\_morpheme\_stack)\author{CSLv3 auto-emit}
\date{\today}

\begin{document}
\maketitle
\tableofcontents

\section%!(MISSING CLOSE BRACE)s}\label%!(MISSING CLOSE BRACE)s}
%!(EXTRA morpheme.stacking, sec:morphemestacking)\section%!(MISSING CLOSE BRACE)s}\label%!(MISSING CLOSE BRACE)s}
%!(EXTRA ASPECT-ONLY, sec:aspect-only)\begin{lstlisting}[language=csl]
# tests: morpheme stacking BASE.aspect.modality.certainty.scope (§03)
# glyph-class: Dot compound, dotted suffix chains
# features: .prog .perf .iter .inch .term, .must .may .cant, .cert .prob, .loc .glob
render.prog : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
merge.perf : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
spawn.iter : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
compact.inch : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
sync.term : bool
\end{lstlisting}
\section%!(MISSING CLOSE BRACE)s}\label%!(MISSING CLOSE BRACE)s}
%!(EXTRA MODALITY-ONLY, sec:modality-only)\begin{lstlisting}[language=csl]
commit.must : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
retry.may : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
reorder.cant : bool
\end{lstlisting}
\section%!(MISSING CLOSE BRACE)s}\label%!(MISSING CLOSE BRACE)s}
%!(EXTRA CERTAINTY-ONLY, sec:certainty-only)\begin{lstlisting}[language=csl]
pass.cert : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
drift.prob : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
stall.poss : bool
\end{lstlisting}
\section%!(MISSING CLOSE BRACE)s}\label%!(MISSING CLOSE BRACE)s}
%!(EXTRA SCOPE-ONLY, sec:scope-only)\begin{lstlisting}[language=csl]
cache.loc : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
cache.glob : bool
\end{lstlisting}
\section%!(MISSING CLOSE BRACE)s}\label%!(MISSING CLOSE BRACE)s}
%!(EXTRA TWO-STACK, sec:two-stack)\begin{lstlisting}[language=csl]
render.prog.cert : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
merge.inch.must : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
spawn.iter.may : bool
\end{lstlisting}
\section%!(MISSING CLOSE BRACE)s}\label%!(MISSING CLOSE BRACE)s}
%!(EXTRA THREE-STACK, sec:three-stack)\begin{lstlisting}[language=csl]
allocate.prog.prob.loc : bool
\end{lstlisting}
\begin{lstlisting}[language=csl]
cleanup.perf.must.glob : bool
\end{lstlisting}
\section%!(MISSING CLOSE BRACE)s}\label%!(MISSING CLOSE BRACE)s}
%!(EXTRA FOUR-STACK, sec:four-stack)\begin{lstlisting}[language=csl]
reconcile.iter.may.doubt.ctx : bool
\end{lstlisting}
\section%!(MISSING CLOSE BRACE)s}\label%!(MISSING CLOSE BRACE)s}
%!(EXTRA FIVE-STACK, sec:five-stack)\begin{lstlisting}[language=csl]
transmit.prog.may.poss.loc : bool
\end{lstlisting}

\end{document}
