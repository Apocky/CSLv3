§ CSLv3 ROOT
I> density=sovereignty • token↓ → cognition↑
I> v3 = reconciliation of 3 divergent v2 sources → unified spec

§ SCOPE
  T1: human↔AI spec R/W (Apocky↔AI-collaborators)
  T2: agent reason substrate (replace EN in CoT)
  T3: CSLv3 reference compiler input (→ x86-64 + SPIR-V)

§ DESIGN AXIOMS
  A0: density=sovereignty — token saved = reasoning reclaimed
  A1: position IS meaning — slot grammar, nesting = scope
  A2: juxtaposition = conjunction — co-present items AND'd
  A3: zero-distortion — implementation-relevant info preserved
  A4: tokenizer-aware — optimize for BPE token count
  A5: LL(2) parseable — unambiguous, minimal lookahead
  A6: self-specifying — CSLv3 grammar expressed in CSLv3
  A7: trilingual — human readable, AI reasoning-ready, compiler ingestible
  A8: dual-glyph — every unicode glyph W! have ASCII alias

§ ANCESTRY
  CSLv1 (ad hoc caveman stripping)
  + CSLv2-pasted (11-file CSL-native suite: glyphs, morph, spatial, reason, compiler)
  + CSLv2-root (Egyptian determinatives, PEG grammar, flora example)
  + CSLv2-specs (slot grammar, type suffixes, BNF, tokenizer notes)
  + APL (1 glyph = 1 op) + Ithkuil (slots) + Sanskrit (compounds)
  + Peirce (spatial logic) + de Bruijn (positional ref)
  + Lojban (unambiguous parse) + J (ASCII fallback)

§ FILES
  00_MANIFEST    § this
  01_GLYPHS      § unified glyph inventory (3 sources merged)
  02_GRAMMAR     § syntax, parse rules, slot template, BNF
  03_MORPH       § 5 compound types + morpheme stacking
  04_SPATIAL     § 2D operators (Peirce + tensor + dataflow)
  05_REASON      § CoT substrate (§P/§D/§T/§S/§C think-blocks)
  06_SPEC        § spec-writing conventions + template
  07_TYPESYS     § unified type system (primitives → dependent)
  08_COMPILER    § CSLv3 reference compiler (Forth+APL+Agda lineage)
  09_BRIDGE      § EN↔CSL translation rules + conversion guide
  10_EVAL        § compression bounds, test corpus, metrics
  11_RESEARCH    § synthesis from 436-source run (reconciled)
  12_TOKENIZER   § BPE cost tables, anti-patterns, ASCII aliases

§ v2→v3 RECONCILIATION LOG
  ✓ glyph table: merged 3 divergent tables → unified w/ ASCII aliases
  ✓ compounds: 4 types → 5 (added Karmadhāraya from root)
  ✓ compound ops: resolved `.` `+` `-` `⊗` `@` (was inconsistent across v2s)
  ✓ morpheme stacking: adopted from pasted (novel, no v2 conflict)
  ✓ evidence markers: merged ✓◐○✗ + ⊘△▽ + ‼ → unified 8-marker system
  ✓ type suffixes: adopted 'd 'f 's 't 'e 'm 'p 'g 'r (ASCII determinatives)
  ✓ domain determinatives: adopted § ∫ ⊞ ⟨⟩ ⌈⌉ ⟦⟧ «» ⟪⟫ (fullest set)
  ✓ slot grammar: formalized from specs/ + pasted parse priority
  ✓ type system: elevated to own file (was scattered)
  ✓ tokenizer: elevated to own file (was §12 appendix)
  ✓ CSLv3 compiler: full reference compiler spec (pasted depth + specs/ examples)
  ✓ grammar: unified BNF (3 sources had PEG, BNF, CFG separately)
  ✓ reasoning: structured think-blocks (§P/§D/§T/§S/§C) adopted
  ✓ arrows: → preferred, -> as ASCII alias (was inconsistent)

§ NEXT (from v2, carried forward)
  ○ run §10 eval on C1-C5 corpus → measure m₁-m₅
  ○ generate full ASCII-alias table (§12)
  ✓ write CSLv3 parser prototype (Odin, per LoA v10 stack)
  ○ propagate CLAUDE.md to all active repos
  ○ rewrite LoA v10 specs in CSLv3 → measure compression
∎
