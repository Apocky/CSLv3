§ SPECS INDEX ← v1.0 freeze
  version : 1.0.0
  frozen  : 2026-04-16
  policy  : post-v1.0 breaking changes → MAJOR bump (see STABILITY.md)

§§ FILES
  00_MANIFEST.csl        ← file index + reconciliation log (v2 → v3)
  01_GLYPHS.csl          ← 74-glyph master (Unicode + ASCII alias)
  02_GRAMMAR.csl         ← slot template + parse rules + precedence
  03_MORPH.csl           ← compound formation + morpheme stacking
  04_SPATIAL.csl         ← spatial ops + discrete/continuous domains
  05_REASON.csl          ← reasoning substrate (§P §D §T §S §C)
  06_SPEC.csl            ← spec-writing conventions
  07_TYPESYS.csl         ← type system foundations (HM + rows + refine)
  08_COMPILER.csl        ← reference-compiler boundaries (parser.exe)
  09_BRIDGE.csl          ← EN↔CSL bridge + paraphrase protocols
  10_EVAL.csl            ← m₁/m₂ evaluation methodology
  11_RESEARCH.csl        ← research synthesis (Sanskrit + Leibniz + Lojban)
  12_TOKENIZER.csl       ← BPE-aware optimization + alias decision table
  13_GRAMMAR_SELF.csl    ← self-parsing grammar (LL(2) canonical form)
  14_CSSLv3_BRIDGE.csl   ← cssllint JSON contract + MIR-export schema

§§ READ-ORDER ← for new contributors
  1 PRIME_DIRECTIVE.md   ← ethical foundation (load first)
  2 00_MANIFEST.csl      ← file index
  3 01_GLYPHS.csl        ← 74-glyph master
  4 02_GRAMMAR.csl       ← parse rules
  5 03_MORPH.csl         ← compounds + morphemes
  6 12_TOKENIZER.csl     ← BPE cost table
  then: as needed per task

§§ LIVING-DOCS ← not frozen ; evolve with the codebase
  parser/DECISIONS.md        ← rolling design log (append-only)
  SESSION_N_HANDOFF.csl      ← per-session transition context
  CHANGELOG.md               ← per-release changes
  MIGRATION_GUIDE.md         ← version-to-version migration

§§ RELATED-REPOS
  CSSLv3                     ← programming-language (separate project)
  ¬-conflate CSSLv3 ≠ CSLv3  ← see UNCONFLATION.md

§§ STABILITY ← v1.0 per-spec commitment
  01 02 03 04 05 06 07 09 10 12 13 14    stable ← breaking = MAJOR
  00                                      stable (index + manifest)
  08                                      stable (compiler boundary fixed)
  11                                      living (research synthesis grows)

∎ INDEX v1.0
